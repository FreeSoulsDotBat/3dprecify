**/node_modules/**
**/dist/**
**/coverage/**
**/.venv/**
backend/**
pnpm-lock.yaml
# Generated API client (Orval, A8) + the server-authoritative OpenAPI — exempt from format/lint.
apps/web/src/shared/api/**
contracts/openapi.json
# Markdown is hand-authored (SDD docs, specs, ADRs) — not auto-formatted.
**/*.md
# Browser-MCP session artifacts (tool output, like graphify-out/) — never format-checked.
.playwright-mcp/**
# Arquivos de design importados do Claude Design (018+): são ARTEFATO, cópia verbatim do que o dono
# desenhou. Reformatá-los faria o repositório divergir da fonte e destruiria a comparação byte a byte.
specs/*/design/**
docs/design/handoff-019/tf-components.css

# Saída bruta da homologação automatizada — artefato de execução (relatórios do Playwright e os
# JSONL incrementais por worker). É evidência, não código: formatá-lo só produziria diff a cada rodada.
docs/homologacao/automatizada/resultados/
# 017/T009 — a semente é GERADA (`pnpm fee:build` → `projetarSemente` + `serializarSemente`) e seus
# bytes são asseridos contra o gerador. Se o prettier a reformatasse, o arquivo commitado divergiria
# da saída do gerador e o guarda de paridade nasceria vermelho — a mesma classe de defeito que o
# artefato servido já evita por `backend/**` estar aqui.
apps/web/src/shared/fee-catalog/seed.data.json
