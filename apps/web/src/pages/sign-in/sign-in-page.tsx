import { SignInScreen } from "@/features/auth/sign-in-screen";

// Foundational page stub (T024): hosts the 001 sign-in unchanged. US2 (T040)
// re-skins it with DS primitives and adds return-to-intent.
export function SignInPage() {
  return <SignInScreen />;
}
