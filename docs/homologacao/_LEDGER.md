# Ledger da homologação (append-only)

| data | fase | o que foi feito |
| --- | --- | --- |
| 2026-08-02 | F00 | Li `.specify/memory/constitution.md`, as 14 specs de `specs/`, `CLAUDE.md`, `package.json`, `backend/pyproject.toml` e a árvore de diretórios. **Verifiquei que o stack do prompt (Expo/Supabase/RevenueCat/RLS/`app.json`/`eas.json`) não existe aqui** — `ls` retorna vazio para os três arquivos e nenhuma dependência os menciona — e readaptei o catálogo ao stack real (FastAPI + Postgres + Alembic + Vite PWA + TanStack Router). Registrei que a premissa "todas as features implementadas" é falsa (E6 31/44, PR #36 aberto, fatia ML da 014 não iniciada). Criei o esqueleto de `docs/homologacao/` e o `_PLANO.md` com 19 fases (as 3 mais pesadas quebradas em sub-fases por R2). Quatro pendências abertas. **PAREI** — nada foi auditado e nada de produção foi tocado. |
| 2026-08-02 | F01 | Inventario delegado a subagente mecanico (R9, `sonnet`): `achados/F01-inventario.md`, 308 linhas. Mapeado: 11 rotas, ~42 endpoints em 11 modulos, 11 tabelas + 5 migracoes, 2 pacotes (12 + 26 exports), fronteira cliente-backend, caches de aparelho, superficies externas, 157 arquivos de teste. **Verificacao do main loop encontrou UM FATO FALSO no relatorio** (bytes NUL em `fee-catalog.ts`, de uma leitura errada de `grep -c`): corrigido no proprio achado, com o metodo que o gerou registrado. Tres dos cinco "nao determinados" eram falta de leitura, nao indeterminacao — resolvidos. Sobra 1 (transcricao de constraints). PAREI. |
| 2026-08-02 | F02 | Drift das **14 specs** contra o codigo do head do PR #36, em tres subagentes paralelos (R9) gravando `F02-drift-{A,B,C}.md` (R3), mais a consolidacao `F02-drift.md` feita pelo main loop. ~238 FR/SC avaliados. **Zero AUSENTE sem decisao** e **zero casos de "tasks.md diz FEITO e o codigo nao faz"** — este ultimo procurado explicitamente e reconferido em 9 remediacoes da `audit-confirmation`. As formulas de preco batem linha a linha com `pricing-core` (o que NAO substitui a F03: bater com a spec e uma coisa, estar numericamente correto e outra). O achado transversal e documental: **specs antigas nunca emendadas quando uma decisao posterior as superou** — tres instancias medidas (a `001` ainda exige login na calculadora; a `007`/`008` prometem "sem preco em teaser" depois do E6 ter acendido os quatro; a `011` diz `*(empty slot)*` na linha 262 e traz o veredito fechado na 309). Sete afirmacoes de subagente verificadas pelo main loop antes de consolidar, todas confirmadas. Nova pendencia P-005. PAREI. |
| 2026-08-02 | F03a | Motor de preco, nucleo — **nao delegado** (raciocinio numerico + dinheiro). Li a formula inteira (`index.ts:168-215`, `rounding.ts`), projetei SEIS casos-ouro mirando uma propriedade cada, calculei a saida a mao e so entao executei: **os seis batem digito a digito**. Confirmadas por construcao: half-up na fronteira (0,005 -> 0,01, e nao 0,00 de banqueiro), dinheiro sem ponto flutuante (0,1+0,2 = 0,30 exato), e o detalhamento FECHA (as sete linhas somam o total exibido, porque cada agregado soma linhas ja arredondadas). Divisao por zero impossivel nos dois denominadores. **Um achado** [F03a-001]: o pacote nao carrega sob `node` puro (imports relativos sem extensao) — MESMA classe consertada no `fee-ingest` na 014/US4 e nao aplicada ao irmao; Baixo, nao bloqueia. **Duas observacoes** viraram P-006 (sem teto em `failurePct` e nos markups; e nada impede varejo < atacado). **Desvio de R8 declarado e revertido**: para EXECUTAR os casos foi preciso um arquivo temporario dentro do pacote (consequencia direta de [F03a-001]) — criado, executado e apagado na mesma fase, com `git status` verificado vazio depois. Evidencia bruta em `evidencias/F03a-casos-ouro.json`. PAREI. |
| 2026-08-02 | F03b | Motor de preco, canais — **nao delegado**. Nove casos-ouro de canal calculados a mao e passados TODOS num unico calculo (para testar o isolamento por slot): **os nove batem**. Respondida a pergunta central do prompt com aritmetica: **a taxa incide sobre o ANUNCIO** (`channels.ts:90`, `list = (base+fixo)/(1-comissao%)`), e o erro evitado e mensuravel — a alternativa errada pagaria 97,75 em vez de 100,00, **2,25% do faturamento em toda venda**. Confirmados tambem: o fixo entra ANTES da divisao; o piso por item da Amazon liga e desliga na fronteira certa (C3/C4); `PROGRESSIVE` cobra por fatia e da resultado DIFERENTE de `SELECTION` nas mesmas bandas (116,67 x 111,11 — R$ 5,56, que e o risco que a ADR-0024 nomeia); o frete deduz do liquido e **nunca** entra no custo (senao o markup incidiria sobre o proprio frete); comissao 100 e recusada (divisao por zero) e comissao negativa tambem, com os SETE slots validos computando ao lado. **Nenhum achado novo.** Veredito da A1-r: aberta, sem sintoma reproduzivel (zero pontos dominados em 6 tabelas x ~12k bases, duas delas construidas para serem patologicas), guardada por teste de PROPRIEDADE provado nao-vacuo, e **nao bloqueia** — registrar severidade Alta num defeito que 71 mil pontos nao reproduzem seria inventar severidade. Nova pendencia P-008 (falta tabela real de voucher da Shopee para medir FIDELIDADE, nao so mecanica). Mesmo desvio de R8 da F03a, declarado e revertido. PAREI. |
| 2026-08-02 | F04a | Entitlement e ledger — **nao delegado**. A derivacao do ADR-0012 esta EXATA (`entitlement/__init__.py:64-66`), avaliada agora e nunca cacheada, com dois portoes distintos (escrita exige ativo; leitura aceita lapsed — o congelamento do Q3) e nenhuma rota HTTP de concessao. Matriz de 8 estados montada. **PRIMEIRO ACHADO ALTO da auditoria, provado por EXECUCAO** `[F04a-001]`: `read_entitlement_state` devolve o `expires_at` do grant CONCEDIDO por ultimo (`max(..., key=granted_at)`, linha 69), nao o que EXPIRA por ultimo — num caso construido a diferenca foi de **370 dias**. Duas consequencias: (a) a API diz ao vendedor que o premium acaba em 30 dias quando dura mais de um ano; (b) a linha da §4.3 que o dono ratificou em 2026-08-02 esta **MORTA no caso canonico**, porque o `cortesiaSobrevive` que eu escrevi no PR-B compara contra esse campo assumindo — no meu proprio comentario, e errado — que ele e o mais distante; como o grant de pagamento nasce com `expires_at = period_end = sub.current_period_end` e e sempre o mais recente, a comparacao vira `x > x` e da sempre falso. Nenhum teste pegou porque os testes de `plan-view` alimentam entitlement e subscription como dados INDEPENDENTES — a mesma familia do fake-active: combinacoes coerentes por construcao nao testam a coerencia. Conserto NAO aplicado (R8): sao duas linhas mas mudam um campo de contrato ja consumido, entao vira spec na F15. Evidencia em `evidencias/F04a-expires-at.txt`. Desvio de R8 declarado e revertido (pytest temporario). PAREI. |
| 2026-08-02 | F04b | Billing MP + superficie do Play — **nao delegado**. Verificacao de assinatura auditada linha a linha e **solida**: falha fechada em todo caminho, regex estrita, janela de 300s com `abs()` (futuro tambem barrado), `compare_digest`, `data.id` minusculizado. Idempotencia **estrutural** (o `UNIQUE` do inbox nos tres caminhos de escrita), `live_mode` como segundo portao independente, e replay dentro da janela neutralizado pela camada seguinte — defesa em profundidade real. Play: rotas nao REGISTRADAS com a flag off, com controle positivo. **ACHADO BLOQUEANTE** `[F04b-001]`: o `None` de `_get` carrega DOIS significados incompativeis — "consultei e nao existe" (ack 200 e certo) e "nao consegui consultar" (ack 200 e errado). Uma indisponibilidade transitoria da API do MP durante o webhook vira `200 "ignored"`, o MP nao reentrega, a reconciliacao **nao esta agendada** (CLI manual; varri os workflows — o unico `schedule` e o da sonda da Amazon) e **nada e logado** (grep por logger na rota: zero). O vendedor paga e nao recebe, sem erro, sem alarme, sem reentrega. A causa raiz JA foi nomeada nesta casa: `amazon-parse.ts:55` documenta o mesmo `null` com dois significados e foi reescrito para torna-los irrepresentaveis — a licao existe, noutro pacote. Nenhum teste pega porque nenhum injeta falha de TRANSPORTE no meio de um evento legitimo; o stub sempre responde. Severidade depende de P-009 (a politica de retry do MP na fonte — li no comentario da casa, nao na doc do MP): se o MP reentrega apos 200, cai para Medio. PAREI. |
| 2026-08-02 | F05 | Auth, dados e seguranca — **nao delegado**. Varri as **31** consultas de `app/api`, `app/services` e `app/billing` por script: 25 filtram por dono na propria consulta; as 6 restantes examinei uma a uma — 4 sao de billing (resolvidas por referencia do PSP, sem uid, que e o desenho correto da SEC-204) e 2 sao de `bom_lines`, cuja protecao e HERDADA do pai e cuja disciplina verifiquei nos **tres** chamadores (`list_boms`, `_rendered`, e o precedido de `_owned`). **Nenhum vazamento entre contas.** Tabela-a-tabela montada para as 11 tabelas. Cliente sem segredo: tudo `VITE_*`, nenhum `.env` versionado (so `.env.example`, que alerta por escrito), grep por secret/service-role/private-key em `apps/web/src` volta vazio. Poda de PII do billing por LISTA BRANCA (`_RAW_AUDIT_FIELDS`) — a forma certa, porque lista negra deixaria passar campo novo do MP. **ACHADO ALTO** `[F05-001]`: **nao existe exclusao de conta** — nem rota, nem CLI, nem procedimento; o produto guarda e-mail e `payer_ref` e vai cobrar de brasileiros (LGPD art. 18, VI). Agravante que precisa de decisao ANTES do primeiro pedido: o historico e imutavel por gatilho PL/pgSQL, entao a saida (anonimizar? reter por obrigacao fiscal?) e juridica e cruza com o handoff fiscal ja aberto. Virou P-010. **Observacao** `[F05-002]` (Baixo, sem defeito hoje): `bom_lines` e a UNICA tabela do vendedor cujo isolamento depende de disciplina do chamador e nao do filtro na consulta. PAREI. |
| 2026-08-02 | F06 | Ingestao de tarifas (014) — **nao delegado**. Auditei os oito guardas e montei o que cada um cobre E o que nao cobre. Sao densos e reais: a canaria checa o PAR (comissao **e** minimo por item — um deslocamento de coluna que mantivesse o percentual e zerasse o minimo passaria por uma canaria ingenua); o piso e a direcao certa do risco (leitura errada ENCOLHE; crescimento vai para `addedCategories` e o `mayAutoMerge` exige todas as listas vazias, entao nao aborta mas tambem nao passa sem humano); e uma mudanca de UNIDADE na fonte (fracao em vez de percentual) faz o parse LANCAR em vez de publicar 0,15% sob selo. Cada guarda carrega no comentario o defeito que o originou e como foi medido — `validarData` documenta que `COLLECTED_AT=banana` saia com exit 0 escrevendo `lastReviewed: "banana"` em 78 entradas com a suite passando. **Nenhum achado de correcao.** **ACHADO MEDIO datado** `[F06-001]`: a janela e de 45 dias e o artefato tem duas datas (`2026-07-07` e `2026-07-28`), entao o selo comeca a avisar "pode estar desatualizada" em **2026-08-21** (19 dias apos esta auditoria) e **2026-09-11** — e o laco que renovaria o `lastReviewed` **nao dispara** (sem `fee-refresh.yml`, e o `schedule` le do branch default que o corte de release adiado nao alcancou). O selo esta CERTO; o mundo que ele descreve e que nao esta, e o momento e ruim porque a segunda data cai perto do provisionamento provavel. Nao e conserto de codigo: e ligar o laco, recolher a mao, ou aceitar o aviso — virou P-011. PAREI. |
| 2026-08-02 | F07 | Durabilidade e offline — **nao delegado**. As quatro garantias se sustentam, e duas por MECANISMO: (1) o gatilho de imutabilidade protege **13 colunas** com `IS DISTINCT FROM` (seguro a nulo — `=` deixaria passar `NULL -> valor`), so libera `label`/`deleted_at`/`updated_at`, e esta em **`ENABLE ALWAYS`**, entao dispara mesmo com `session_replication_role = replica` (em modo `ENABLE` padrao a garantia teria um buraco em replicas e restauracoes); (2) o outbox e chaveado por conta, tem trava Web Locks, estado `blocked` visivel para o 403-no-sync, e o sign-out com fila e **guardado** — o codigo o trata explicitamente como "a UNICA copia de uma cotacao", que e a razao certa. Caches todos por uid, com a unica excecao sendo o catalogo de TARIFAS (chave unica — dado publico, correto). Degradacao D3/D6 confirmada **read-time** em todo caminho de leitura, e o rollup registra ser "uma leitura pura que nunca escreve lastKnown". **Nenhum achado de correcao.** **Observacao** `[F07-001]`: o gatilho e `BEFORE UPDATE` e nao cobre `DELETE`. Coerente hoje (varri `backend/app`: nao existe `delete(Snapshot)`), mas vira colisao com o `[F05-001]` — quando a exclusao de conta for construida, o caminho disponivel sera o MENOS auditavel (sumir com a linha), enquanto o mais auditavel (anonimizar o payload mantendo o registro) esta BLOQUEADO pelo gatilho. Isso precisa estar na mesa da decisao P-010. Nova P-012: o `ENABLE ALWAYS` pode fazer um `pg_restore` em modo replica falhar, e descobrir isso durante uma recuperacao e o pior momento. PAREI. |
| 2026-08-02 | F08 | Estados, erros e resiliencia — **nao delegado**. **Nenhuma mensagem tecnica chega ao vendedor**, e a garantia e por TIPO: `MESSAGE_BY_CODE` e um `Record<ApiErrorCode, string>`, entao um `ErrorCode` novo na uniao gerada e **erro de compilacao** ate ganhar frase, e um teste fecha o outro lado em runtime. Varreduras: `{err.message}`/`String(error)` em JSX -> **zero**; a `ErrorPage` mostra copia amigavel + `correlationId` (identificador de suporte, nao stack); nenhum codigo de status cru. O `honestWriteError` distingue falha de TRANSPORTE (`status === 0`) de erro codificado — verdades diferentes, frases diferentes. Cobertura de estados medida por tela (calcular 7/2/9/7, historico 5/4/5/9, conta 1/0/3/2 — o "vazio" ausente na conta e correto: ela nunca esta vazia) e ha `OfflineBanner` global no shell. **ACHADO BAIXO** `[F08-001]`: seis pre-carregamentos de cache sao `void ...then()` **sem `.catch`** (bom, catalog, history x2, scenario, entitlement). O efeito na tela e benigno — a consulta online assume —, mas a degradacao e graciosa por ACIDENTE da arquitetura e nao por tratamento, e a falha e invisivel. Assimetria reveladora: o `outbox.ts:121` usa `then(run, run)` com os dois handlers — quem escreveu o outbox sabia, e os seis nao herdaram. **P-013 vale mais que o achado**: nao confirmei se `integrations: [...]` na v8 do Sentry MESCLA com os padroes ou os SUBSTITUI; se substituir, **nenhum** erro nao tratado do app chega ao painel, o DSN estaria configurado, o painel vazio, e a leitura natural de painel vazio e "nao ha erros". Recusei inferir (R6). PAREI. |
| 2026-08-03 | F09 | Build e prontidao de deploy — **nao delegado**. Certo: deploy so por `workflow_dispatch`, com guarda `DEPLOY_ENABLED != true` -> `exit 1`, e autenticacao **WIF sem chave** (nenhum JSON de service account no repo); `.env` nao versionado. Bundle medido: **1,3 MB** em `dist/`, chunk maior de **902 KB**; a vulnerabilidade moderada que o `pnpm audit --prod` aponta (`protobufjs` via `firebase`) **NAO e servida** — contei no bundle: `protobuf=0`, `grpc=0`, e as 2 mencoes a `firestore` sao residuo de string (o app so importa o Auth e o tree-shaking funcionou). **ACHADO ALTO E BLOQUEANTE** `[F09-001]`: **0 de 33** usos de `uses:` nos 5 workflows estao fixados por SHA — inclusive `trufflesecurity/trufflehog@main`, terceiro por BRANCH mutavel. **Nao e achado novo: e achado NAO REMEDIADO** — `seguranca-ci-first.md:315` registra `SEC-014-02`, severidade ALTA, marcado `[VERIFICADO]`, com a acao escrita "fixar `trufflehog` por SHA **imediatamente**". Nada foi feito. Agravante que o parecer nao cobria: ele olhava a ingestao (`fee-refresh.yml`, que nem existe), e o alvo de maior valor e o **`deploy.yml`**, que roda com `id-token: write` e os segredos `WIF_PROVIDER`/`WIF_SERVICE_ACCOUNT` — direito de publicar em producao. Tag e ponteiro mutavel: repontar `@v2` poe codigo arbitrario dentro do job de deploy com as credenciais de producao. Marquei como bloqueante por CONVICCAO e nao por medicao, e disse isso: hoje o `deploy.yml` nao roda (guarda desligada), mas **provisionar E o ato de ligar essa guarda** — o momento de fixar e antes. Nova P-014 (auditar tambem a arvore de dev, que roda no CI ao lado de segredos). PAREI. |
| 2026-08-03 | F10 | Qualidade e cobertura — **nao delegado**. **Zero `any` explicito** em `apps/web/src` fora de teste e do cliente gerado (medido); os `Any` do backend estao todos em fronteira de JSON, que e o unico lugar honesto. Ratchet explicito e justificado: 100% em `packages/*`, piso MEDIDO na baseline real em `apps/web` (um alvo aspiracional viraria excecao permanente; um piso na baseline pega regressao sem mentir). **ACHADO BAIXO** `[F10-001]`: o `MIN_ROWS = 28` — o piso que aceita ou rejeita um parse do catalogo de TARIFAS — mora em `build-amazon.mjs`, o unico arquivo ISENTO de cobertura. A equipe sabia da isencao e migrou varias regras para fora dela por esse motivo exato (os comentarios dizem: "a regra que decide o rotulo do dinheiro nao pode morar num lugar isento"); a migracao ficou incompleta neste ponto. `checkParseSanity` recebe `minRows` por parametro — a funcao esta coberta, **o valor nao**. **ACHADO DE METODO** `[F10-002]`, que vale mais que os dois: varri todos os padroes mecanicos plausiveis (`>= 0`, `expect(true)`, `assert True`, `toBeDefined`, `is not None`) e **nao achei nenhuma** das CINCO instancias reais desta sessao — o que a varredura pega e assercao fraca e estreitamento de tipo legitimo, ruido e nao sinal. As cinco nao tem forma sintatica em comum: sao verdadeiras por um motivo diferente do que o nome do teste afirma, e nenhum linter alcanca isso. Os dois detectores que funcionaram: **ler os VERDES da rodada vermelha** (4 das 5) e **mutar o codigo** (os tres guardas novos). Virou P-015 como possivel linha de DoD — o Principio III ja manda observar falhar, mas nao manda LER os que nao falharam, que e onde as cinco moravam. Nova P-016 (deteccao de codigo morto exige instalar ferramenta; pedi autorizacao em vez de fazer). PAREI. |
| 2026-08-03 | F11a | Homologacao visual do fluxo GRATUITO (`qa-produto`, opus) — **84 screenshots** em `evidencias/f11a/`, 5 rotas x 3 larguras (360/768/1440) mais free-logado, sem-rede, erro 500 e carregando onde alcancaveis. **7 achados: 2 Alto (ambos bloqueiam), 3 Medio, 2 Baixo.** O mais grave `[F11a-001]`: a 360px o campo **Tarifa de energia** (o unico com prefixo `R$` E sufixo `/kWh`) colapsa para **33px** uteis e, alinhado a direita sem `maxLength`, mostra a CAUDA do valor — digitado `1000,00`, a tela exibiu `R$ 0,00 /kWh` enquanto a conta cobrou `Energia R$ 600,00`. Com `0,89321` (o formato da conta de luz brasileira) a tela mostra `)321`. **Dinheiro mentindo na tela, na largura minima declarada.** O segundo `[F11a-002]`: precos >= R$ 10 mil quebram NO MEIO DO NUMERO (`18.130` -> `18.13` / `0`), e `scrollWidth === clientWidth` — **a assercao geometrica passa**, so a imagem denuncia; o comentario do `price-hero.css` mostra que a quebra foi introduzida DE PROPOSITO para matar um transbordo horizontal, ou seja, trocou-se um defeito visivel por um invisivel e a troca nunca foi homologada com imagem. **Verifiquei 6 afirmacoes do agente antes de consolidar**: 5 conferem no fonte (`schema:304`, `min-width: 0`, `text-align: right`, ausencia de `maxLength`, e o `overflow-wrap: anywhere` do preco com o comentario que confirma a intencao); a sexta (`overflow-x: clip`) o proprio achado rotula como **computada** e nao declarada — nao verifiquei, e nao muda o diagnostico. O agente registrou **4 hipoteses proprias derrubadas ao medir**, o que e sinal de metodo. Nao alcancado e dito: teclado virtual (headless nao instancia) e carregando/erro em `/catalogo`, que **nao existem** no fluxo gratuito porque o teaser nao faz requisicao — comprovado com 500 e com 9s de atraso. `git status`: so `docs/homologacao/`. PAREI. |
| 2026-08-03 | F11b | Homologacao visual do fluxo PREMIUM (`qa-produto`, opus) — **124 screenshots**, 5 superficies x 3 larguras, com kit de 11 linhas, nomes de 68-70 chars e valores ate R$ 87.885,95; os 6 estados de cobranca alcancados com assinatura real pelo stub, webhooks assinados e expiracao forcada. **9 achados: 3 Alto (2 bloqueiam), 3 Medio, 3 Baixo.** O mais grave `[F11b-001]`: no detalhe CONGELADO do historico o `R$` desgruda do valor — simbolo numa linha, `70.867,77` na outra — **a 1440px tambem** (2 de 11 pecas; a 360px, 7 de 11), com `scrollWidth === clientWidth` e `toContainText` passando. E o documento que a ADR-0019 torna imutavel e que o vendedor mostra ao cliente. **Rastreei a causa raiz e ela e minha** `[F11b-000]`: `formatBRL` (`decimal-ptbr.ts:146`, **19 chamadores**, comentario dizendo ser "uma regra so") monta `R$ ` com **espaco comum**. Na T038/D1, ONTEM, eu diagnostiquei exatamente isso ("separar o simbolo do valor e a unica quebra que nao se permite numa linha de preco") e apliquei NBSP a **tres constantes de copy** — nao ao formatador. Verifiquei: `messages.pt-br.ts` tem 3 NBSP, `decimal-ptbr.ts` tem 0. **Quarta vez nesta sessao que uma cura existe num modulo e nao foi aplicada ao irmao**, e desta vez o irmao fui eu — o que torna o padrao mais convincente: consertar o sintoma que a homologacao apontou nao e consertar o defeito. **Os dois herdados PIORARAM**: o `[F11a-002]` quebra o total do kit nas TRES larguras (cartao de largura fixa) e o limiar caiu de R$ 10 mil para **R$ 1.000**; o `[F11a-001]` alcanca o formulario de produto, onde o valor e **salvo** e nao so exibido. Nao alcancado e declarado: tema escuro, offline dentro das superficies, retorno do checkout, PDF/CSV baixados, recalculo confirmado. `git status`: so `docs/homologacao/`. PAREI. |
| 2026-08-03 | F12 | A11y e localizacao — **nao delegado**. **Localizacao limpa, medido**: TODAS as datas usam `toLocaleDateString("pt-BR")` (zero excecoes), **zero** texto de interface cravado em JSX fora de `messages.pt-br`, e o parser decimal RECUSA lixo interior em vez de concatenar (a versao anterior fazia `"5x3" -> 53`; numa calculadora de preco, adivinhar o numero do vendedor e pior que recusa-lo). Todo input tem nome acessivel — os tres duvidosos da varredura estao rotulados por `<label>` implicito ou pelo `<Field>` do DS. 34 `aria-label`, teste de foco-ao-titulo. A unica falha de localizacao e a do `[F11b-000]` (o `R$` com espaco comum), ja registrada. **Nenhum achado novo de a11y**, mas **ACHADO MEDIO de COBERTURA** `[F12-001]` com tres pontos cegos que explicam achados de outras fases: (1) os testes rodam a **390px** e o produto declara suportar **360** — 30 pixels separam "tudo verde" de dois Altos bloqueantes; (2) o contraste medido e so o de **texto de status**, nao o do texto corrente nem do `--text-muted`, que e o tom de toda legenda; (3) o mais caro: o teste "no horizontal overflow with giant values" e **SATISFEITO pela quebra** — o CSS `overflow-wrap: anywhere` foi posto para matar o transbordo, e o preco partido ao meio e o efeito colateral que a assercao NAO PODE ver, porque `scrollWidth === clientWidth` quando o conteudo quebra. **O teste e o defeito sao a mesma decisao vista de dois lados**: nao e teste vacuo, e teste CUMPLICE — primeira instancia dessa forma nesta auditoria. Nao verificado e dito: leitor de tela (exige execucao assistiva real, e a lacuna mais seria desta fase), teclado ponta a ponta, `prefers-reduced-motion`. PAREI. |
| 2026-08-03 | F13 | Desempenho — **nao delegado**; medicao em navegador real (Playwright + CDP) e cronometragem do `pricing-core` empacotado por esbuild **no scratchpad** (zero arquivo criado ou alterado no repositorio, R8 preservada). **O gargalo NAO e a matematica, e isso deixou de ser suposicao**: precificar 3 canais custa **0,11ms** (0,68% de um quadro), validar o catalogo de 56 KB com Zod custa **0,27ms**, a escala e **linear** (1->10 canais = 0,039->0,399ms, sem N2), e ate um kit de 50 pecas x 3 canais cabe num quadro (6,64ms). O backend e **imune a N+1 por construcao** — **zero `relationship()` declarada**, zero consulta dentro de laco, e os indices parciais compostos `(owner_uid, created_at, id) WHERE deleted_at IS NULL` batem exatamente com a paginacao keyset; o `boms.py:546` ate documenta ter achatado a contagem de consultas de proposito. **SEIS achados: 4 Medios, 2 Baixos, nenhum bloqueante.** Os dois que importam: **(1)** a PRIMEIRA visita e **7.234ms de tela em branco** em celular fraco (chunk unico de 903 KB, **zero divisao de codigo** nas 11 rotas, `<div id=root>` vazio — nada aparece antes do bundle terminar, e `/` custa o mesmo que `/calcular`, o que prova empiricamente o chunk unico); **mas o PWA resgata**: 2a visita **157ms (-98%)** e abre **offline em 271ms**, entao o problema e SO a aquisicao. **(2)** cada tecla na calculadora custa **15,6ms de mediana em celular medio** (93% de um quadro) e **25,2ms em celular fraco**, com pico de 42,7ms — e como o calculo e 0,11ms, **99,3% e reconciliacao do React**, nao a conta. **A licao de metodo desta fase, e ela quase me pegou**: a primeira medicao de digitacao deu **33,3ms identicos a 1x e a 6x** — eu estava setando `el.value` direto, que o React **ignora** em input controlado, e os 33,3ms eram o piso do meu proprio instrumento (dois `requestAnimationFrame`). **Um numero que nao muda quando a variavel muda nao e um resultado, e um no-op.** Refiz com o setter NATIVO e um **controle positivo** (a tela mudou? SIM) — so ai os numeros escalaram com o estrangulamento. **Terceira vez nesta auditoria que o detector foi o controle positivo, e a primeira em que o instrumento vacuo era MEU.** Justica a quem escreveu: a memoizacao da calculadora foi removida **de proposito** (`calcular-page.tsx:77-79` — o `useMemo` nunca recomputava porque o RHF muta o array no lugar), e a remocao foi **certa**: preco velho em silencio e pior que preco lento. O que caducou foi a CONCLUSAO de `:220-221` ("nao precisa de memoizacao"), que era aposta razoavel sem medicao e e falsa com ela. **Quinta instancia do padrao dominante da auditoria** (`[F13-004]`): historico e cenarios sao paginados por keyset com indice sob medida; filaments/printers/products/boms **nao tem limite nenhum**, e o cliente nao tem virtualizacao — a cura existe no modulo ao lado e nao foi herdada. Achado de brinde `[F13-006]`: o `pricing-core` **lanca e mata a conta inteira** por causa de uma banda sem comissao, contrariando o SC-107 que ele mesmo documenta em maiusculas ("the engine never throws for one bad channel") — provado com **controle positivo** (comissao 150% isola direitinho), mas **inalcancavel hoje** (guard do 014 em `fee-catalog.ts:112` + `?? 0`), entao **Baixo, sem inflar**. Nao verificado e dito: volume real de dados (a R8 me proibe de semear o banco), latencia de rede/servidor real (so existira no provisionamento), amostra estatistica (um passe por perfil) e vazamento de memoria em sessao longa. **A INVESTIGACAO ACABOU: 16 fases de investigacao executadas (F01..F13, com F02/F03/F04/F11 desdobradas); 0 restantes.** PAREI. |
