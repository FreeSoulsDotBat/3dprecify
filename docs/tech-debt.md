# Technical Debt & Deferred Decisions

## Governing rule (Jonatan, 2026-06-27)
Before deciding anything with a **real chance of changing later**, flag the **change-likelihood %**. Decide now
only if the choice is stable or cheap-to-reverse. Otherwise log it here as **explicit tech debt** with: the %,
the **trigger** that will force the decision, and the **cheap placeholder** used in the meantime. Conventional
choices with low change-risk (≤~20%) are taken inline and noted in the relevant ADR/doc, not here.

`change %` = estimated probability that, if we locked this **now**, we'd later change it (higher = better to defer).

## Register
| ID | Deferred decision | change % | Trigger (when to decide) | Placeholder used now |
|----|-------------------|----------|--------------------------|----------------------|
| TD-001 | i18n architecture (full i18n library) | 40% | Internationalization epic (post-launch) | Typed pt-BR messages module keyed by error `code`; no i18n lib in 001 |
| ~~TD-002~~ | ~~Visual design system / tokens / styling~~ | — | **Being addressed now (2026-06-27)**: co-authoring the Claude Design prompt with Jonatan's vision + UX decided here; final UI rendered by Claude Design | resolved-in-progress |
| ~~TD-003~~ | ~~Deploy target (SPA host + FastAPI host)~~ | — | **RESOLVED 2026-06-28**: backend=Cloud Run (ADC keyless), SPA=Firebase Hosting | resolved |
| ~~TD-004~~ | ~~DB physical schema / money repr / multi-tenancy / migrations~~ | — | **RESOLVED 2026-07-09**: ADR-0013 + `specs/007-e2-catalog-entitlement/data-model.md` | resolved |
| ~~TD-005~~ | ~~Entitlement enforcement design (offline+TTL vs per-request)~~ | — | **RESOLVED 2026-07-09**: ADR-0012 (authoritative Postgres ledger, per-request check) | resolved |
| TD-006 | Payments (Play Billing vs Mercado Pago recurring) | 60% | E6 + ADR (regulatory flux) | None — no payment code before E6 |
| ~~TD-007~~ | ~~Monorepo task runner (Turbo)~~ | — | **RESOLVED 2026-06-28**: pnpm workspaces now → Turborepo at first CI/build pain (F1.3) | resolved |
| TD-008 | pricing-core packaging (ESM/CJS + d.ts) + cross-lang parity | 40% | A 2nd consumer needs the built pkg (server won't reimplement the formula per ADR) | Direct TS import within the monorepo |
| TD-008b | Formal TS **composite project references** (`tsc -b`) wiring apps/web → pricing-core | 30% | Folded into TD-008 (composite needs emit; conflicts with current `noEmit`) | Per-package `tsc --noEmit` (`pnpm -r typecheck`) + tsconfig `paths` + Vite alias |
| TD-009 | pricing-core **version identifier** (to stamp E2 saved-calc snapshots → reproducible quotes) | 60% | E2 persistence (subset of TD-008) | None — 001/002 don't persist calcs |
| TD-010 | Peace Sans **font license** clearance for product/PWA embedding | 50% | Before any public deploy that ships the display font | Use only for logo/static art until cleared; body/UI = Inter (licensed) |
| ~~TD-011~~ | ~~Design-token **semantic role→color** mapping (palette→text/surface roles)~~ | — | **RESOLVED 2026-06-29**: adopted the Truth's Forge semantic token graph verbatim from Claude Design (colors/typography/spacing/radius/elevation/motion + base) into `apps/web/src/styles/`; AA-validated; fixed the dark-accent=cyan bug (accent stays purple, `--accent-text` AA in both themes) | resolved |
| ~~TD-014~~ | ~~Self-host brand webfonts~~ | — | **RESOLVED 2026-07-03**: Inter 400/500/600/700 + Lilita One + Paytone One `.woff2` vendored from Google Fonts (latin subset) into `apps/web/public/brand/fonts/`; `@font-face` active in `base.css`. Peace Sans still gated by TD-010 | resolved |
| ~~TD-015~~ | ~~`--danger`-as-text theme-remapping token~~ | — | **RESOLVED 2026-07-03 (003/T004)**: `--danger-text`/`--success-text`/`--info-text` added for BOTH themes (≥4.5:1 measured); field error consumes `--danger-text`; contrast locked by e2e (T042) + token-parity snapshot | resolved |
| TD-016 | **Maskable PWA icon** (safe-zone padded render) for Android adaptive icons | 30% | Before Play packaging (E7) or an install-quality pass | Manifest ships `any`-purpose 192/512 PNGs (vendored from design); no maskable variant yet |
| ~~TD-017~~ | ~~Adopt the design's full LoginScreen + app-shell~~ | — | **RESOLVED 2026-07-03 (003 slice)**: real shell adopted (app-shell + top-bar + TabBar/sidebar, DS batch-1 primitives incl. Logo/Grafismo/Button), sign-in re-skinned over DS (T040), header cramping gone; all owner-homologated (T027/T070) | resolved |
| TD-012 | correlation-id **FE-originates** mode (correlate even when no response arrives) | 35% | Network-failure debugging proves the server-originates model insufficient | A9 = server-originates; FE consumes + tags Sentry |
| TD-013 | Sentry **Session Replay** (AI/human sees the exact user session) | 50% | After LGPD privacy notice + cost review; likely premium-gated | Sentry **breadcrumbs** only (console/network/clicks) |
| TD-018 | **Globalize server-confirmed identity** into the shell top-bar. The `top-bar` `AccountChrome` shows the **client Firebase session e-mail** (`session-store.user.email`); the server-confirmed `/api/v1/me` identity (A23) is fetched + rendered **only on Conta**. Whether the top-bar should also read `/me` is an owner call — doing so makes the top-bar depend on the authenticated `useIdentity` query and would change the `auth-boundary` e2e expectations (an extra network wait/assert). | 35% | Owner decides during a shell-identity pass (E-level) or when a mismatch between session e-mail and `/me` becomes user-visible | Top-bar = client session e-mail (instant, offline-safe); Conta = server-confirmed `/me` (A23 proven end-to-end) |
| TD-019 | **`use-identity` migrate to the generated `useGetMeApiV1MeGet`** once the OpenAPI `/me` contract is fixed. The Orval mutator is now wired (003/T069-Orval), but `/api/v1/me` still declares a phantom `422 HTTPValidationError` (no body params), so the generated success type is the union `{ data: CurrentUser \| HTTPValidationError }` — migrating would force a `.data as CurrentUser` cast and lose the clean `ApiError` typing, so `use-identity` stays on the ergonomic `apiFetch` (same A20 core). Also: `orval.config.ts` runs with `clean:false` because the hand-written mutator co-lives in the output folder (`src/shared/api`); a bare `clean:true` wipes it. | 40% | A21 backend fix (declare `ErrorEnvelope` on 401/403/422, drop phantom `HTTPValidationError`, regenerate) — then migrate `use-identity` + revisit whether the mutator/generated split can collapse | `use-identity` → `apiFetch<CurrentUser>`; generated client is transport-backed but currently only consumed via `apiFetch` |

| ~~TD-020~~ | ~~**`parseDecimal` silent misparse** — bad/mixed-separator strings coerced to a wrong number or NaN→0, all silent (only weight validated)~~ | — | **RESOLVED 2026-07-06 (E1 / T015-T016)**: `calculator-schema.ts` `numField` (RHF+Zod) rejects any non-empty unparseable string with a per-field pt-BR message and never coerces NaN→0; blank optionals default to 0 explicitly. The lenient `parseDecimal` coercion is gone from `calculator-model.ts`; `computeCalculator` throws `ValidationError{field}` on bad numbers (SC-008). pricing-core bumped 0.0.0→2.0.0 (A25). | resolved |
| TD-021 | Extract `useOnlineStatus` into a `shared/network` store (A33 phase-2 wording) when a 2nd consumer needs connectivity state (e.g. disabling "Salvar" offline at E2) | 30% | Second consumer of online/offline state | Local hook inside `widgets/offline-banner` (single consumer; functionally identical) |
| TD-022 | **First-load focus steal under StrictMode (dev-only)** — `page-header`'s module-scoped `initialHeaderPending` flag is consumed by StrictMode's double-mount, so dev landing focuses the title (NAV-2 says it must not). Production is correct (verified in preview build, P2 PASS). Fix: make the skip-first-mount logic idempotent to re-mounts (root-level marker instead of a mutable module flag) | 25% | Next touch on `page-header`/focus logic, or if it starts flaking focus tests | Dev-only artifact; prod behavior correct; noted in post-close audit 2026-07-03 |
| TD-023 | **T-06 (`AUDITORIA.md`) — backend DB test isolation is by unique-uid-literal convention only, no `TRUNCATE` between tests** (`backend/tests/conftest.py:74-96` sets up the shared session-scoped `migrated_db`; contrast `:108-154`'s billing-specific truncate). Non-billing tests each mint a distinct uid literal (`"u-sc-e504-kit-line"` etc.) so rows never collide — cheap and has held so far, but it is a convention, not an enforced invariant; a duplicated uid literal across two tests, or any assertion counting rows GLOBALLY rather than scoped to its own uid, would silently pass or flake depending on run order. **Conscious non-fix** (013 audit remediation, T054b) — the cost of a per-test truncate/reset fixture (session-scoped Postgres shared across ~800+ backend tests) was judged disproportionate to a risk that has not yet materialized. | 20% | The first duplicated uid literal across two test files, OR the first order-dependent GLOBAL-count assertion (e.g. `SELECT count(*) FROM scenarios` with no `WHERE owner_uid = ...`) | Unique uid-literal-per-test convention; no truncate/reset between non-billing tests |
| TD-024 | **D5 — PWA `registerType: "autoUpdate"` (`apps/web/vite.config.ts:15`) is silent: no `useRegisterSW`, no user-visible "atualização disponível" prompt.** The audit (FC-03) found this was never a recorded decision — just the Vite PWA plugin's default, unexamined. **Decision (013 audit remediation, D5): keep `autoUpdate` as the deliberate choice**, recorded here now — a silent background update is the right default for a PWA whose content (pricing math, catalog) is not mid-session-destructive to refresh, and it avoids nagging a seller mid-quote. | 15% | The first real user report of a broken/stale chunk surfacing mid-session after a deploy (a symptom `autoUpdate` can produce if a long-lived tab holds stale JS calling a changed API shape) | `registerType: "autoUpdate"`, no update-prompt UI |
| TD-025 | **D6 — the offline snapshot outbox (ADR-0018) assumes single-tab usage; two tabs draining the same outbox concurrently is neither implemented nor tested** (audit T-04: `grep BroadcastChannel\|locks` across the outbox code = 0). **Decision (013 audit remediation, D6): declare single-tab as a documented premise now, option (a)** — do not build `navigator.locks`/`BroadcastChannel` coordination speculatively; a seller with the app open in two tabs simultaneously recording offline snapshots is an unmeasured edge case, not a proven one. | 30% | Telemetry (once instrumented) shows real multi-tab concurrent-outbox-drain usage, OR a user-reported lost/duplicated snapshot record traced to a two-tab session | Single-tab assumption, undocumented until now; no locking/coordination code |

Resolved debt moves to the relevant ADR/doc and is struck from this table.

## Not debt — logged to stop re-flags during homologation
- **Masthead wordmark "TRUTH'S FORGE" (not "Precifica3D")** is intentional — Truth's Forge = studio, Precifica3D
  = product ("Precifica3D by Truth's Forge"); confirmed 2026-06-28. The logo's accessible name = product
  ("Precifica3D", via `<Logo alt={messages.appName}>`) vs the visual studio wordmark is also intentional. Full note in **ADR-0007 → "Brand naming
  — masthead wordmark"** and `docs/design/claude-design-brief.md`. Do not "fix" the masthead or the `tf-*` prefix.
