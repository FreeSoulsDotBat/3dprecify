# Token-spend ledger

Owner rule (2026-07-10): every AI operation that costs real tokens — semantic extraction,
subagent fan-outs, multi-agent workflows — appends a row here. Estimate **before** running,
record the actual **after**, and write down the lesson when they diverge. graphify runs also
mirror into `graphify-out/cost.json` (local, gitignored — this file is the durable record).

| Date | Operation | Estimate | Actual | Lesson |
| --- | --- | --- | --- | --- |
| 2026-07-10 | graphify initial full build — semantic extraction of docs + community labeling | — | 107,249 in / 9,827 out | Baseline; code was AST-only (0 tokens). Semantic cache was NOT persisted by this run (fixed 2026-07-10). |
| 2026-07-10 | graphify item-6 ingestion — 26 curated docs (specs/008 + ADRs 0014–0016 + business-rules + CLAUDE.md + 11 agents), 2 subagents | ~10–15k | 216,831 (aggregate in+out) | Word-count estimates undershoot ~15×: subagent read+reason+write overhead dominates, not the doc text. Estimate ≈ corpus tokens × 10 for subagent extraction, or use `GEMINI_API_KEY` to move it off-session. |
| 2026-07-10 | 008 T001 designer-ux UX handoff — 1 subagent reads spec/contracts/quickstart + E2 `ux-catalog.md` pattern, writes `ux-bom.md` | ~100–150k aggregate (corpus ×10 rule from item-6 lesson) | 111,338 (25 tool uses, ~8min) | Corpus ×10 rule held. Handoff surfaced 2 owner flags (5th nav tab; lapsed-at-/bom split) — carried to PR evidence. |
| 2026-07-11 | 008 T005 pre-gate: arquiteto decision — where the shared piece-form lives (C1: page-composition vs entities/piece move vs page-hosted composer) | ~80–120k aggregate | 121,952 (23 tool uses, ~10min) | Slightly over estimate (agent also root-caused a transient hook block). Decision: page/widget-hosted composition (R7 in research.md), 85%. |
| 2026-07-11 | 008 T006b qa-produto visual homologation — browser walk of /bom (390px + desktop, compose/rollup/teaser) + screenshots | ~150–250k aggregate (browser tool calls dominate) | 162,607 (128 tool uses, ~21min) | Within estimate. Verdict PASS-with-nits; top nit (skippedLines caption unreachable via form) answered with a component-level contract test + PR note. |
| 2026-07-11 | 008 K-amendment plan delta: arquiteto decisions R8 (route naming, atomic materialization txn + wire shape, FR-310 relaxation seam, manual-product state, nav icon, PR slicing) + ADR draft | ~100–140k aggregate (R7 was 122k, similar shape + one ADR) | 128,197 (34 tool uses, ~11min) | On estimate. R8 + ADR-0017 (Proposed) + one data-model premise change caught by reading code (bom_lines born with product_id). |
| 2026-07-11 | 008 T009 pre-push extensive review (owner-conditioned): 6-dimension multi-agent workflow (arch/clean-code/bugs/decisions/security/tests) + adversarial verify of blocker/major findings | ~600–900k aggregate (6 finders ×~90k + refuter pairs) | 1,042,131 (10 agents, 319 tool uses, ~14min + paused/resumed via workflow cache) | ~15% over: finders averaged ~150k (whole-branch mandates read far more than a diff review). Yield: 2 CONFIRMED majors (guard tear-down; prefill coverage) + 12 minors — the fan-out paid for itself. |
| 2026-07-12 | 008 T015b/T017b qa-produto visual homologation (PR-B) — browser walk of the kit save round-trip, Kits tab, K3 attention state, manage + the lapse read-only state (390px + desktop) + screenshots | ~150–250k aggregate (browser tool calls dominate; T006b was 163k, this walk is wider — save/reopen/lapse/duplicate) | 190,141 (121 tool uses, ~25min) | Within estimate. Verdict PASS-with-nits; found 2 real blockers a green gate could not (a born-manual product claiming a deletion that never happened; a second Salvar filing a duplicate kit) plus the dev-DB drift below. Visual homologation keeps paying for itself on honesty bugs — the exact class tests do not assert. Lesson: a mid-run behavior change needs a SendMessage to the agent, or it homologates a build that no longer exists.
| 2026-07-12 | 5-agent review of the last 5 PRs (#12–#16, `e655504..HEAD`, 163 files / +16k lines): architecture+structure · security · tests/correctness · UX-UI conformance · clean-code+bugs. Owner-requested before the PR-B merge. | ~600–900k aggregate (5 finders × ~120–180k) | 668,258 (5 agents, 222 tool uses; 3 ran testcontainers Postgres) | On estimate. Yield: 1 BLOCKER found by 4 of 5 independently + reproduced (soft-deleted product ⇒ kit unsavable + dishonest GET — the data-model had flagged it 'Resolve in tasks'); it survived a green gate + 100/100 e2e + a visual homologation. Fan-out earned itself: convergence across independent lenses is the signal a single reviewer cannot produce. Also surfaced that 'gate coverage 100%' is pricing-core-only — stop quoting it as app-wide.
| 2026-07-12 | 008 PR-B review-fix VISUAL HOMOLOGATION (qa-produto) — browser walk of the review-round FE deltas (honest no-price headline + excluded note, post-create clobber fix, fee-offline banner + retry, "Ver meus kits" empty-composer link, sticky bottom summary, per-line aria/glyph) at 390px + desktop. Owner: coverage gate is ONE MORE homologation step, not a replacement — so both run. | ~120–180k aggregate (browser tool calls dominate; narrower than the 190k full PR-B walk but still full-stack + a real save for the clobber check) | 186,328 (103 tool uses, ~16min; Playwright-driven Chromium — no browser MCP registered, so the same @playwright/test the e2e specs use) | Slightly over estimate (real premium loop: signup → grant CLI → save round-trip, ×2 viewports). PASS on 6/7 + PASS-WITH-NITS on the sticky bar (pins the FULL total+channel-rollup, ~67% of viewport with channels present — spec §1.7/G4 met, but a compact heroes-only bar reads better). Zero honesty defects. |
| 2026-07-12 | 008 sticky-bar re-verify (qa-produto, resumed) — after compacting the sticky to heroes-only + channel rollup scrolling, a FOCUSED visual re-check at 390px + desktop (stack setup + homolog DB already worked out in the walk above, so only rebuild + confirm). | ~60–120k (fresh spawn — prior agent transcript gone, so re-does stack setup; homolog DB persists) | 104,876 (67 tool uses, ~7min) | PASS. Compacted bar measured 274px-30.5pct (desktop) and 286px-33.9pct (mobile), down from ~67pct-73pct; rollup now position:static (scrolls above), no Salvar overlap, 10/10 automated assertions. Prior agent transcript had been GCed so a fresh spawn re-did setup. |
| 2026-07-12 | 008 PR-C planning (2 specialists, parallel): arquiteto — reconcile the D6 read-time-degrade approach shipped in PR-B vs the planned T020 eager-delete-capture (ADR-0017 addendum + data-model §D6/quickstart §5 notes, Principle VIII); designer-ux — the degraded kit-line indicator reconciled with the F1/K3 indistinguishability lesson. Owner chose the specialist route. | ~160–280k aggregate (prior arquiteto runs ~122–128k each; designer-ux similar; both read-only analysis over boms.py + specs + E2 degraded surface) | 175,332 (arquiteto 80,250 / 14 tool uses; designer-ux 95,082 / 13) | On estimate (low end). arquiteto: ADOPT read-time-degrade (88%) — found the planned D6 mechanism (ON DELETE SET NULL) was never reachable since delete_product is a soft-delete; paste-ready ADR-0017 addendum + data-model/quickstart/tasks reconciliation + 5 risk-pin tests. designer-ux: a degraded kit line gets a calm MUTED CAPTION reusing manualValuesKept (not an Alert, no removal claim, keep "(avulsa)"), reconciled with F1/K3 (~80% on show-vs-nothing, ~90% on caption+copy). |
| 2026-07-12 | 008 PR-C qa-produto visual homologation (degraded-reopen) — save a kit referencing a product, delete the product, reopen the kit; verify the calm "(avulsa)" label + valores-mantidos muted caption, the F1 honesty guard (no "removido/excluído/deletado" anywhere), still-priceable + re-saveable, at 390px + desktop + screenshots. Owner chose qa-produto+e2e now. | ~120–190k aggregate (browser tool calls dominate; narrower than the 190k full PR-B walk — one focused flow — but full-stack setup + a real premium save/delete/reopen loop) | 230,791 (110 tool uses, ~36min) | Over estimate (deep root-cause dig once the flow failed). **Verdict FAIL — found a real honesty blocker a green gate + 100/100 e2e both missed:** on the common flow (delete product → reopen kit) the deleted product renders as a LIVE catalog reference (name + price), as if nothing happened — the *inverse* honesty bug (F1-guard still PASSES; no removal claim). Root cause is NOT the T021 component (correct) but the PR-B freshness/hydration seam: `useDeleteProduct` invalidates only the products query (never kits), and the composer hydrates once per kit id and locks on the first stale-cache paint. Lesson reconfirmed: visual homologation keeps catching the honesty-bug class unit/e2e cannot — a correct component starved of correct data still lies. |
| 2026-07-12 | **E4 kickoff — product-owner scope modeling** (1 subagent, read-only + one write): shape the E4 epic ("History + reproducible snapshots + export", export = premium) into vision · user stories · acceptance criteria · scope boundaries · open questions, feeding `/speckit-specify`. Writes `docs/product/e4-scope-brief.md`. | ~100–160k aggregate (prior single-specialist analysis runs: designer-ux handoff 111k, arquiteto 80–128k; this one reads wider — business-rules + constitution + E2/E3 specs as pattern + GRAPH_REPORT — but does no code deep-dive) | 91,786 (16 tool uses, ~6min) | Under estimate — an epic-scoping pass is CHEAPER than the per-decision specialist runs it precedes (no code deep-dive, no browser). Yield: the master rule that resolves the E2/E3-vs-E4 tension (a snapshot CONTAINS its values, never references them ⇒ it cannot degrade) + 13 owner questions, incl. one real gap nobody had noticed: E1–E3 model no seller identity, so a quote PDF would print anonymous. |
| 2026-07-12 | **E4 `/speckit-plan` Phase-0 architecture round (2 specialists, parallel)**: arquiteto — the **offline write queue** (FR-527/529: local durability, exactly-once sync, honest pending state, entitlement checked AT SYNC; the product's FIRST offline write) + export render side (client vs server → offline export + gate enforceability, ADR-0015 precedent) + server-side immutability (FR-504) + confirm pricing-core untouched vs ADR-0008; dev-estrutura-de-dados — the frozen-snapshot persistence model (payload shape JSONB vs typed columns, migration `0003`, DB-level immutability, indices for label search + date-range filter, kit lines + per-channel rollup in the payload). | ~200–290k aggregate (prior arquiteto rounds 80–128k each, but the offline queue is new machinery — likely web research on sync patterns; dev-estrutura-de-dados comparable to prior schema rounds) | 283,969 (arquiteto 153,268 / 36 tool uses, ~15min · dev-estrutura-de-dados 130,701 / 16, ~11min) | On estimate (high end). **Convergence was the payoff**: both agents, independently, reached the same non-obvious conclusion — snapshot provenance must carry **NO foreign key** (`ON DELETE SET NULL` would *write to the immutable row*; RESTRICT would make products undeletable; CASCADE would delete history) — inverting the E2/E3 instinct. Also caught two things the main loop had written and got wrong: (a) FR-528's "no server received-at is stored" is **literally unachievable** (a server `uuid7` PK already embeds a ms timestamp), and (b) money leaves in JSONB must be decimal **strings** — Postgres keeps `numeric`, but `json.loads`/`JSON.parse` hand back **floats**, so the precision loss is in the serializer, silently, app-side. Lesson: a two-specialist parallel round on a *new-machinery* epic pays for itself; the estimate held because scope was split cleanly (arquiteto = mechanism, data-model = shape) with an explicit "state what you need from the other" instruction. |
| 2026-07-13 | **E4 T001 designer-ux UX handoff** (1 subagent, non-blocking, parallel with PR-A): the Histórico list + snapshot detail (renders STORED values) + the **pending/blocked sync states** (the honesty surface for the product's first offline write) + the sign-out-with-queue dialog + the US5 teaser. Writes `specs/009-e4-history-snapshots-export/ux-history.md`. | ~90–140k (prior handoffs: 008 T001 designer-ux 111k; the E3 PR-C designer-ux round 95k — this one is wider: 5 surfaces incl. a novel sync-state vocabulary the product has never had) | 159,836 (24 tool uses, ~9min) | **Over estimate (~+15%)** — a NEW vocabulary costs more than a new screen: the agent had to read the whole honesty lineage (ux-decisions, E2/E3 teasers, the F1/K3 lesson) before it could name a state the product has never had. Lesson: budget a design round by *how much precedent it must reconcile*, not by surface count. Yield included one thing no reviewer had caught — **sign-out has TWO entry points** (`top-bar` + `conta-page`), so guarding only one would let unsynced records vanish silently, which ADR-0018 §10 forbids. It also claimed (95%) an FR-014 violation in my ADR-0018 draft ("[Cancelar]" is banned copy) — **I checked, and it was WRONG**: FR-014 bans asserting undecided commercial facts (no subscription-cancellation POLICY before E6), not a Cancelar button. Second lesson: a specialist's confident citation still gets verified — this one would have silently reworded shipped copy on a false premise. |
| 2026-07-12 | 008 PR-C fix + e2e verification (main-loop, no subagent): implement fix(a) products+kits invalidation & fix(b) kitSignature content re-hydration, backend/unit/e2e tests, then a long diagnosis of an apparent chromium-only "flaky" degrade e2e. | n/a (main-loop session — not a fan-out; ~15 e2e rebuild/runs + 3 Playwright trace extractions dominated the spend, NOT model tokens) | ~15 full e2e builds (~1min each) + trace parsing | **The "flaky" was an ENVIRONMENT trap, not a bug:** an orphaned `vite preview` on :4173 (from an interrupted run) let Playwright `reuseExistingServer` serve a FROZEN pre-fix bundle — every "fix doesn't work"/"flaky" measurement was old code (incl. the pre-summary "openedSig fix failed" call). Backend :8100 rebuilt each run, so only the front froze — the tell was network showing correct `degraded:true` responses while the UI ran old JS. After killing the orphan: 10/10 clean. Lesson → memory `e2e-orphaned-preview-server` + homologation-t021 addendum. **Rule: before diagnosing any "flaky e2e", check `Get-NetTCPConnection -LocalPort 4173` for a listener and kill it so the run rebuilds.** Hours of token spend would have been saved by that one check first. |
| 2026-07-13 | **E4 T016 qa-produto visual homologation (PR-A)** — the offline → sync → reopen walk at 390px + desktop: record a snapshot online (Sheet: rótulo · validade · basis) → record OFFLINE (honest pending, never "salvo") → reconnect → auto-drain → reopen the frozen detail; plus the sign-out-with-queue guard, the Histórico list/date/basis honesty, the free/signed-out teaser (NO sample entry), and the F1 guard (no removid/excluíd/deletad on any snapshot surface). | ~80–120k aggregate (browser tool calls dominate; prior qa-produto walks: 163k full-surface, 104k focused re-verify, 231k when it hit a real blocker and dug) | 251,039 (111 tool uses, ~37min) | **Over estimate (~2×) — and worth every token: verdict FAIL with TWO blockers a green gate (517 tests) could not see.** **B1:** the record mutation inherited TanStack's default `networkMode: "online"`, which **PAUSES** a mutation while the browser is offline — so `mutationFn` never ran, so `enqueueSnapshot` never ran, so the seller tapped Salvar at the feira, nothing happened, and a reload lost the quote forever. **The offline queue was unreachable offline.** ADR-0018 had explicitly REJECTED paused mutations as the strategy — we inherited them anyway, *by omission*. **B2:** `drainOutbox` did a read-modify-write around a network call, so a record queued mid-POST was ERASED by the write-back — while the UI said "Pendente neste dispositivo". A **false pending**: the DB's unique key prevents duplicates, not a record that is never POSTed. ADR-0018 §6 specified `navigator.locks`; grep found zero. **Lesson: my estimate was anchored on the *happy* walk. Budget a homologation by the walk it would take to DISPROVE the feature, not to demo it** — the two prior qa-produto runs that found real blockers (231k, 190k) both blew past the demo-shaped estimate the same way. And the standing lesson holds again: unit tests mock the very layer where both bugs lived. |
| 2026-07-13 | **E4 T016 RE-VERIFICAÇÃO (qa-produto, resumido via SendMessage)** — depois das correções B1 (`networkMode: "always"`) e B2 (`withOutboxLock` + drain por-entrada): re-testar SÓ a gravação offline real (`context.setOffline`), o ciclo duro (offline → reload → reconectar), a corrida (POST em voo + gravação concorrente) e o nit do banner. Stack rebuildada + CORS real para `:4600`. | ~60–120k (re-verify focado; o precedente é a re-verify de 104k do E3 — mas esta reproduz duas corridas com timing) | 280,250 (21 tool uses, ~8min) | **Acima do estimado, e de novo valeu.** Veredicto **PASS-WITH-NITS**: B1 e B2 confirmados corrigidos com prova (conservação de IDs: fila ∪ banco = todos os `clientSnapshotId` cunhados; **17 snapshots / 17 ids distintos, zero duplicatas**). Achou ainda um nit novo — **offline o logo da top-bar quebrava** (`vite.config.ts` `includeAssets` não precacheava `brand/logo/*.svg`): gap PRÉ-EXISTENTE do app-shell que só ficou visível porque o E4 *criou* a tela offline onde ele aparece — e era a primeira coisa que o vendedor veria lá. **Lição: `SendMessage` para o mesmo agente custa quase o mesmo que um spawn novo** (o transcript é reconstruído), mas mantém o critério de julgamento e as contas do ambiente — vale pela CONTINUIDADE, não pela economia. |
| 2026-07-13 | **E4 PR-A revisão multi-agente pré-merge (owner-requested)** — workflow de 6 dimensões sobre o diff `develop...HEAD` (PR #18, ~25 arquivos de fonte + migração 0003 + endpoint + contrato): arquitetura/ADRs · estrutura/FSD-Lite · qualidade+bugs · decisões de negócio (spec FR + Q1–Q15 + regra das duas prateleiras) · segurança/entitlement (Princípio IV) · testes (Princípio III, o que os mocks escondem). Cada achado BLOCKER/MAJOR passa por 3 refutadores adversariais de lentes distintas; MINOR por 1. Fecha com um crítico de completude. | ~600–900k agregado (precedentes: workflow de 6 dimensões no 008 T009 = 1,042k com mandato de *branch inteira*; revisão de 5 PRs = 668k. Aqui o mandato é o DIFF, que foi exatamente a lição do 008 — "finders averaged ~150k because whole-branch mandates read far more than a diff review") | **4,867,561** (112 agentes, 1.106 tool uses, ~25min) — **5,4× a estimativa**; esgotou o limite da sessão e 53 dos 112 agentes morreram | **Duas lições, e a segunda é a séria.** (1) **A conta do fan-out não é o custo dos finders — é o produto `achados × refutadores`.** Estimei pelos 6 finders (~150k cada ≈ 900k) e esqueci que 41 achados × até 3 refutadores = **105 agentes de verificação**, que passaram a ser ~85% do gasto. *Regra nova: estimar um workflow de review como `finders × custo_finder + (achados_esperados × refutadores × custo_verificador)`, e **capar** o fan-out de verificação (top-N por severidade) antes de rodar, não depois.* (2) **O pós-processamento que eu escrevi transformou a falha de infraestrutura em mentira silenciosa:** `survives = v.length > 0 && refutados < maioria` fazia um achado cujos refutadores TODOS morreram cair no balde "derrubado" — enterrado como se tivesse sido refutado, sem nunca ter sido lido. 19 dos 23 "derrubados" eram isso; só 4 foram refutação de verdade. Um veredicto AUSENTE não é um veredicto NEGATIVO. Escrevi na ferramenta de revisão exatamente a mentira por omissão que a revisão existe para caçar. Script corrigido para três estados (CONFIRMADO / REFUTADO / SEM_VEREDICTO). |
| 2026-07-15 | **E4 PR-A review-fix Phase 6 (verificação cruzada + re-homologação)** — main-loop: `gate:all` (fe EXIT 0 · be 205) + drift-guard idempotente + os 6 e2e de histórico (B1/B2/B3 na camada real, `E2E_PREVIEW_PORT=4600`) + suíte e2e completa (107 passed, 1 flake E2 que passa 6/6 isolado). Depois **qa-produto** re-homologa a walk offline→pendente→reload→sync→reopen a 390px+desktop com emulação que alcança o `onlineManager`. | ~200–280k (precedentes qa-produto: T016 251k, PR-C 231k, PR-B 190k) | **~258k qa-produto** (82 tool uses, ~23min) + main-loop (≈15 builds e2e) | **PASS, 2 nits, nenhum blocker.** A emulação correta (context.setOffline + evento `offline`) tornou o antigo falso-PASS do B1 num PASS real — prova: drenou sozinho no evento `online`, lista final com exatamente 1 card. **Lição operacional confirmada 2×:** (a) uma asserção de e2e que o agente escreveu-sem-rodar estava errada (esperava redirect a /sign-in; o app corretamente fica anônimo em /calcular) — os specs de um agente PRECISAM rodar de verdade antes de valer; (b) o flake de e2e sob 6 workers (catalog T025) passa 6/6 isolado — reconfirma "antes de chamar regressão, re-rode isolado". qa recomendou promover os caminhos gravar-kit (C1) e render-de-canal (M11) a e2e permanente. |
| 2026-07-15 | **E4 PR-A review-fix implementação (delegada, 2 ondas)** — main-loop fez os irreversíveis do cliente (I1 freezer recursivo, I2 `catalogVersion` no root, C1 kit por basis; 15/15 vitest verde). Depois delega: **Onda 1 paralela (dirs disjuntos)** — FRONTEND (`apps/web`: B1/C4·B2·B3·C2·C5·C6·M1·M6·M7·M8·M10·M11·M5-parcial·minors·teste C1·e2e M13) ∥ DADOS (`backend`: CHECKs da migration 0003 C7/2b/schema-bind/3-perdidos + testes C8/M12 + reconciliar data-model.md); **Onda 2** — BACKEND (`history.py`: M2/VR-502·M4·M3 keyset + regen contrato), após DADOS por compartilhar `test_history.py`. Contrato do envelope travado (Opção A) antes de delegar. | ~550–900k agregado (impl. com test-first + rodadas de vitest/pytest; precedentes: dev-estrutura ~130k analítico, mas impl. roda testes N vezes) | **687,153** (DADOS 168,862 · FRONTEND 327,993 · BACKEND 190,298; + main-loop nos irreversíveis do cliente e na verificação cruzada) | Dentro do estimado. Resultado: 31 achados + o gap M3-cliente resolvidos, `gate:all` verde (fe EXIT 0 · be 205 passed · cov 85%) + drift-guard idempotente. **Lição: um achado que atravessa dois domínios cai no vão se você não atribuir AS DUAS metades explicitamente.** O M3 tinha metade-servidor (keyset) e metade-cliente (auto-seguir `nextCursor`); atribuí só ao backend, e a metade-cliente ficou órfã — o `use-history` parava na 1ª página e o 51º registro não chegava ao dispositivo (o defeito que o M3 existe para matar, reintroduzido). Peguei na verificação cruzada (grep do cursor no cliente), não num teste — reforça que a verificação de integração da main-loop é obrigatória depois de um fan-out por domínio. Também: um agente pode deixar o artefato gerado STALE (o BACKEND regenerou o Orval antes de finalizar o `history.py`); a main-loop DEVE re-rodar a regen + provar idempotência antes de confiar no drift-guard. |
| 2026-07-13 | **E4 PR-A revisão — resume do run `wf_d23b699c-484`** (owner escolheu retomar em vez de eu verificar na main-loop): os 6 finders replayam do cache (custo 0), re-rodam só os 53 agentes que morreram no limite — 52 refutadores dos 19 achados sem veredicto + o crítico de completude. Pós-processamento corrigido (SEM_VEREDICTO deixa de ser enterrado como REFUTADO). | ~2,0–2,7M (53 agentes × ~43k de média medida na rodada 1; os finders, que eram os caros, não re-rodam) | **~1,5M** em dois relances (o 1º morreu com o processo e não deixou registro de custo; o 2º fechou 112/112 com **144,492** — quase tudo replay de cache) | **O resume salvou a operação.** Sem ele, refazer a revisão custaria outros ~4,9M; o cache dos 6 finders (a parte cara) foi replayado de graça e só os verificadores mortos re-rodaram. **Lição operacional: um workflow caro deve ser escrito para ser retomável desde o início** — journal por agente + `(prompt, opts)` estáveis — porque o limite de sessão não é exceção num fan-out grande, é o caso esperado. Resultado final: 41 brutos → **31 confirmados · 10 refutados · 0 sem veredicto** + 6 achados novos do crítico de completude (incl. um BLOCKER que NENHUMA das 6 lentes viu: `KitLines` itemiza a preço de varejo um kit gravado na base atacado). **A refutação adversarial pagou-se**: derrubou 10 achados, entre eles dois que alegavam que a query de entitlement pausa no cold-boot offline — os refutadores foram ler a fonte instalada do `query-core` e provaram que o `onlineManager` não lê `navigator.onLine` no boot. Sem eles, eu teria "consertado" um bug inexistente e mexido em código já homologado. |
| 2026-07-16 | **E4 PR-B revisão multi-agente pré-merge (owner-requested)** — workflow de 6 lentes sobre o **DIFF** `origin/develop...HEAD` (`feature/009-e4-pr-b-us3-us6`, 23 arquivos +2169/−127: US3 origem read-time + US6 filtros/gerência/lapse + e2e): correção/bugs · segurança/entitlement (Princípio IV) · testes (o que os mocks escondem) · decisões/regras (duas prateleiras, SC-502/508, ADR-0019, honestidade F1) · clean-code/FSD-Lite · UX-honestidade. Verificação adversarial **CAPADA** no top-10 blocker/major × 2 refutadores de lentes distintas (lição row PR-A 4,8M: capar o fan-out de verificação ANTES, não depois); 3 estados de veredicto (CONFIRMADO/REFUTADO/SEM_VEREDICTO); crítico de completude cruzado contra os refutados. | ~700k–1,2M agregado (diff review, não branch-inteira — lição 008 T009 "finders ~150k porque mandato de branch inteira lê muito mais que um diff"; verificação capada em ≤20 refutadores) | **1,114,003** (11 agentes: 6 finders + 4 refutadores + 1 crítico; 201 tool uses; ~24,5min) | **A CAPA FUNCIONOU: 1,1M vs os 4,8M descontrolados do PR-A** (a diferença exata foi capar `achados × refutadores` ANTES). Só 2 achados chegaram à verificação (o diff estava limpo) e ambos vieram **CONFIRMADOS 2/2** — sinal forte, zero falso-positivo a refutar. Yield: 2 majors de honestidade que um gate verde não vê — (1) `useSnapshot` trata o `null` autoritativo (servidor diz "apagado") igual a `undefined` (ainda não respondeu) e cai no cache stale ⇒ um snapshot deletado renderiza como vivo; (2) "Recalcular hoje" afirma DELEÇÃO ("a origem não está mais no seu catálogo") quando a origem pode só estar não-resolvível localmente (offline/cache frio) — contradiz a decisão do `origin.ts` de nunca alegar remoção. O **crítico de completude achou o de maior valor**: o copy do diálogo (`originResolves`) e a condição de reprecificação (`fromFrozen`) são DOIS portões independentes, e o sinal honesto `fromFrozen` é descartado no ponto de gravação ⇒ grava valores congelados carimbados como "preço de hoje". Exatamente o defeito que atravessa dois pontos (diálogo + gravação) que nenhuma lente isolada pega — o vão cross-domain que o crítico existe para cobrir. |
| 2026-07-13 | **E4 PR-A — sessão de background DUPLICADA ("tente denovo", sem contexto da sessão anterior)** — sem histórico do que falhara, reconstruí o estado do run `wf_d23b699c-484` a partir do journal + dos 219 transcripts em disco (parsing local, **0 tokens de modelo**), constatei que `resumeFromRunId` é same-session-only e que esta era outra sessão, e rodei **1 agente**: o crítico de completude. | ~100–200k (1 agente; o fan-out inteiro já estava em cache) | **174,849** (32 tool uses, ~11min) + main-loop | **Duas lições, e a primeira é sobre trabalho perdido.** (1) **Rodei em paralelo com outra sessão que já estava fazendo o mesmo** — ela retomou o workflow, escreveu o relatório e preencheu a linha acima do ledger enquanto eu trabalhava. Não há trava entre sessões: *antes de "tentar de novo" um trabalho caro, checar o disco pelo artefato de saída (`review-pr-a.md`) e pela linha do ledger, não só pelo estado do run.* O gasto foi recuperado só porque o crítico caiu num caminho diferente e somou C7/C8 (o trigger é `ENABLE` e não `ENABLE ALWAYS` ⇒ contornável por `session_replication_role=replica`; e o teste do trigger cobre 2 das 13 colunas — apagar a cláusula do `payload` deixa a suíte verde). (2) **Reconstruir um run morto do journal custa 0 tokens e vale muito**: os 41 achados JÁ tinham veredicto — o script morrera antes de *reportar*, não de *verificar*. A estimativa de ~2–2,7M para "re-rodar os 53 mortos" era uma leitura errada do estado. *Ler o journal antes de re-gastar.* (3) O crítico **repetiu um achado já refutado** (a query de entitlement pausando no cold boot) — um crítico de completude não vê o que os refutadores derrubaram, então **o pós-processamento precisa cruzar os achados novos contra os REFUTADOS** antes de apresentá-los, senão o ruído caro volta pela porta dos fundos. Fui ler o `onlineManager` instalado e confirmei: `#online = true` no construtor, nunca lê `navigator.onLine`, só vira offline no evento `offline` da janela. |
| 2026-07-16 | **E4 PR-C T030 qa-produto homologação visual (export + comparação)** — a walk completa da fatia: gravar → exportar o **artefato de verdade** (PDF baixado e OLHADO, não só requisitado): quote padrão com **ZERO linhas de custo** (SC-506), kit **itemizado** (SC-515), o opt-in do detalhamento, o CSV do histórico; mais os três estados desabilitados-com-motivo (offline · pendente · lapse) e o bloco de comparação US7 (rótulos inequívocos, sem "hoje" fabricado). 390px + desktop. Stack completa (emulador auth + pg 5433 + backend 8100 + preview **4600**). | ~230–320k (precedentes qa-produto: T016 251k · re-verify 258k · 008 PR-C 231k · PR-B 190k. Estimo no ALTO da faixa pela lição registrada — *orçar pela walk que DESMENTE a feature, não pela que a demonstra* — e porque esta tem um artefato binário a inspecionar, além de exigir revogar o grant para ver a negação por lapse) | **168,094** (93 tool uses, ~22,7min) | **ABAIXO do estimado (~0,6–0,7×) — a primeira homologação qa-produto deste projeto a ficar abaixo da faixa**, e o motivo é reproduzível: o mandato era **uma fatia** com walk enumerada (6 verificações nomeadas + 2 perguntas de ratificação), não "homologue a superfície". As que estouraram (T016 251k, re-verify 258k, 008 PR-C 231k) tinham mandato aberto **ou** caíram num blocker e cavaram. *Lição: o custo de uma homologação é governado pelo ESCOPO DO MANDATO, não pelo tamanho da feature — e a regra "orçar pela walk que DESMENTE" continua certa sobre o RISCO, mas não é licença para orçar alto quando a walk já está enumerada.* **Veredito PASS-WITH-NITS com 1 defeito real que o gate verde não via: o CSV emitia o instante UTC e DESCARTAVA o offset** — cotação 22:30 BRT vira 01:30Z, então card/detalhe/PDF diziam 13/07 e o CSV dizia **14/07**, com o dia local **irrecuperável** do arquivo (FR-513 "same dates" + SC-511). **Mesma classe de defeito ("data um dia errada") que a homologação do backend já pegara neste épico** — reaparecida na única superfície de export que ninguém tinha renderizado, porque `TestHistoryCsv` assertava label/total/basis/kind e **nunca o valor de `deviceQuotedAt`**. *Lição dupla: (1) um defeito já corrigido volta pela superfície irmã que o teste não cobre — ao consertar uma classe, varrer TODAS as saídas dela; (2) o agente verificou o artefato real (baixou o CSV pelo app) — foi assim que viu.* Ratificou as 8 strings novas e **concordou com o desvio do lapse** (o banner da página já diz a frase que o "painel de reativação" abriria). Honesto sobre o que NÃO alcançou: o caso US7 "catálogo muda ⇒ hoje muda" não tem alavanca na UI (linha de kit não tem seletor de filamento) — ficou provado só em teste de componente, e ele disse isso em vez de arredondar para pass. **⚠ SUPERADO na re-verificação (linha abaixo, 2026-07-16): a alavanca EXISTE** pela rota produto-criado-no-Catálogo (que obriga o vínculo filamento/impressora ⇒ D3 live-reflect); o caso está provado em artefato real. A cláusula sobre a linha de kit segue verdadeira — a generalização para "não tem alavanca" era um erro, e é a parte instrutiva: *um gap honesto pode ser uma generalização errada a partir de um fato verdadeiro.* |
| 2026-07-16 | **E4 PR-C T030 re-homologação focada (qa-produto via SendMessage, mesmo agente)** — fecha os dois pontos que a run anterior deixou abertos: (1) a walk US7 que ela **não alcançou** ("muda o catálogo ⇒ *hoje* muda, o congelado não") — com a rota concreta que ela pode ter perdido: editar o `costPerRoll` do FILAMENTO no Catálogo e usar um snapshot de proveniência PRODUCT (o D3 live-reflect do E2 reprecifica o produto a partir do filamento vivo; ela só tentou pela linha de kit, que de fato não tem seletor); (2) re-verificar no arquivo baixado o **fix do dia no CSV** (a correção entrou DEPOIS que ela olhou — o artefato mudou desde a homologação). | ~90–160k (re-verify focada de 2 itens; precedentes: T030 completa 168k · re-verify PR-A 258k — mas aquela reproduzia duas corridas com timing. **SendMessage e não spawn novo**: pela lição registrada, custa quase o mesmo (o transcript é reconstruído) mas preserva o critério de julgamento E as contas/stack já montadas — vale pela CONTINUIDADE) | **203,511** (127 tool uses, ~8,8min) | **~27% acima do estimado — e a lição vale mais que o estouro: um "não alcancei" honesto NÃO é o mesmo que "não dá para alcançar".** O agente havia concluído que o caso US7 não tinha alavanca na UI. A causa era uma **generalização errada a partir de um fato verdadeiro**: ele leu `boms.py:157` (produto materializado de kit nasce com `filament_id=None`, por isso "needs attention") e estendeu para todo produto. Mas um produto **criado no Catálogo obriga o vínculo** (`produto-page.tsx:173`) ⇒ D3 live-reflect ⇒ a alavanca existe. *O que destravou não foi outro agente nem mais tokens: foram 3 linhas de rota concreta ("tente a proveniência PRODUCT + edite o filamento") somadas à instrução explícita "se ainda não existir, diga de novo e PARE — não fabrique caminho pelo banco".* **Regra: quando um especialista devolve um gap honesto, gastar 2 minutos procurando uma rota alternativa antes de aceitar o gap — mas pedindo a re-tentativa de um jeito que deixe "não dá" continuar sendo uma resposta legítima.** Resultado: **veredito sobe de PASS-WITH-NITS para PASS (confiança 85%→92%)**. Item 1 (fix do dia no CSV) verificado no arquivo baixado com um straddle REAL (browser em `Asia/Dubai`, UTC+4: UTC ainda 16/07 21:18Z, device já 17/07 01:18) — card/detalhe/PDF/CSV as quatro concordam em **17/07**, e o `+04:00` viaja no arquivo (dia local recuperável). O código antigo teria dito 16/07. Item 2 (US7) provado em artefato: filamento 110→220 ⇒ `Cotado em 16/07/2026 R$ 26,48` vs `Hoje R$ 43,80`, e o congelado **não se mexeu** (detalhe ainda mostra Material R$ 11,55 do rolo velho) — a Ficha Técnica deixou de alegar e passou a demonstrar. Ele também **corrigiu a linha anterior deste ledger** por conta própria (ver nota abaixo). |
| 2026-07-16 | **E4 PR-C revisão multi-agente pré-merge (owner-requested)** — workflow de 6 lentes sobre o **DIFF** `origin/develop...HEAD` (PR #20, 29 arquivos +3030/−21: renderer server-side + rotas de export + `apiFetchFile` + a Sheet + o bloco US7 + e2e + ADR-0020): correção/bugs · segurança/entitlement (Princípio IV, FR-515/516) · testes (o que os mocks escondem, Princípio III) · decisões/regras (duas prateleiras, SC-506/508/511/515, ADR-0020 §1 "zero aritmética" vs ADR-0008, honestidade F1) · clean-code/FSD-Lite · UX-honestidade. **Verificação CAPADA no top-10 blocker/major × 2 refutadores de lentes distintas** (lição PR-A 4,87M: o custo é `achados × refutadores`, e capar DEPOIS não existe); minors × 1. **Quatro estados** (CONFIRMADO / REFUTADO só se unânime / CONTESTADO se dividido / SEM_VEREDICTO se os refutadores morreram — um veredicto AUSENTE não é NEGATIVO). Crítico de completude cruzado contra os REFUTADOS (lição da sessão duplicada: o crítico não vê o que foi derrubado e traz o ruído caro de volta). | **~700k–1,2M** pela fórmula da lição PR-A: `6 finders × ~150k + (≈10 achados × 2 refutadores × ~40k) ≈ 900k + 800k de teto`. Diff comparável ao do PR-B (23 arq/+2169 → 1,11M com 11 agentes); este é ~40% maior em linhas mas mais estreito em superfícies novas. **Contexto que deve BAIXAR o yield esperado, não o custo:** este diff já passou por 2 homologações em artefato real (T030 + re-verify, veredito PASS) que acharam 4 defeitos — as lentes vão encontrar menos do que no PR-A/PR-B, e isso é o resultado esperado, não falha do workflow. | **2,133,240** (33 agentes: 6 finders + 26 refutadores + 1 crítico; 689 tool uses; ~26,5min) | **~78% acima do topo da faixa — e a minha premissa estava ERRADA no sentido que importa: 21 achados brutos, ZERO refutados, 4 BLOCKERs reais.** Dois erros de estimativa: (a) previ ~10 achados e vieram 21 (a capa segurou o custo — sem ela seriam ~42 refutadores); (b) orcei o refutador em ~40k e ele custou ~64k porque **foi rodar o reportlab de verdade** para reproduzir — refutador que executa código custa mais e vale mais. **A LIÇÃO CENTRAL, e ela corrige uma crença que eu vinha repetindo nesta sessão:** eu disse às lentes para esperar pouco porque "este diff já passou por 2 homologações em artefato real". **Isso estava errado, e o motivo é a lição:** as homologações abriram o artefato com **dados BENIGNOS** ("Cliente João", sem `otherCosts`, sem quantidade 0, sem peça sem nome). Os 4 blockers são todos **dependentes de dado**: rótulo com `<` (o vendedor digita "Peça &lt;2&gt;") ⇒ o texto SOME do PDF do cliente ou vira 500 — **e note a correção de 2026-07-16, na revisão de limpeza: a alegação original dizia que `&` também corrompia ("R&amp;D" → "R&amp;D;"). É FALSA. Medido contra o parser real, `&` solto imprime verbatim; só a forma de ENTIDADE ("Tom &amp;amp; Jerry") é interpretada. Eu propaguei a alegação para o commit, o docstring e este ledger sem nunca medi-la — a lente de limpeza me obrigou a rodar o probe, e o probe desmentiu*; `otherCosts` presente ⇒ o detalhamento **conta o mesmo dinheiro duas vezes** (`admin` É a soma deles — a tela do app omite `admin` justamente por isso, o PDF não); quantidade 0 (estado legal, `CHECK quantity >= 0`, decisão Q1 "não entra neste pedido") ⇒ `int(0 or 1)` imprime **"Qtd. 1"**; peça de kit sem nome ⇒ célula **em branco** no orçamento. **"Olhar o artefato" é necessário mas NÃO suficiente — é preciso olhar com DADO ADVERSARIAL.** Uma homologação com dados bonitos e uma suíte verde concordaram em dizer PASS sobre um renderer que mutila o texto do vendedor. Convergência como sinal: o bug do ReportLab foi achado **independentemente por 3 lentes** (correção, segurança, estrutura) — exatamente o que um revisor único não produz. ⚠️ **Incidente de segurança do próprio workflow:** um refutador com Bash **editou `app/api/export.py` e removeu o filtro `owner_uid`** para testar se algum teste pegava (nenhum pegou — que era o achado). Ele desfez; a árvore ficou limpa e nada vazou para o commit — **eu verifiquei antes de qualquer outra coisa**. Mas *dar Bash com escrita a um refutador é um risco de design: a próxima vez pode não desfazer.* Regra para a próxima revisão: refutadores em worktree isolada (`isolation: 'worktree'`) ou sem ferramenta de escrita. |
| 2026-07-16 | **E4 PR-C revisão de LIMPEZA pós-fix (owner-requested, escala escolhida pelo owner)** — segunda revisão do mesmo diff `develop...HEAD` (agora 30 arquivos, +3500/−25: a fatia + os 2 commits de review-fix `c8977da`+`bce1bac` que **ninguém revisou ainda**). Escopo deliberadamente estreito e diferente do anterior: o owner pediu "correto, e **principalmente limpo**". 4 lentes sobre o diff — (1) comentários/arqueologia (o suspeito nº1: eu carimbei `(review PR-C)` e narrei "isto era assim antes" pelo código todo, exatamente o que a instrução do projeto proíbe: comentário fala com o próximo leitor, não com o revisor de hoje) · (2) código morto/sobras/duplicação · (3) consistência com os idiomas do repo (FSD-Lite, `tf-*`, precedentes dos módulos irmãos) · (4) sanidade de correção **focada nos 2 commits novos**, que não passaram por lente nenhuma. Verificação **capada em ≤8** achados × 1 verificador (alegação de "código morto"/"comentário falso" é falsificável por grep — barata e objetiva; não precisa de painel adversarial de 2–3 como um blocker de correção). **Refutadores/lentes em `isolation: 'worktree'`** — primeira aplicação da regra que a linha acima registrou depois do incidente do refutador que editou `export.py`. | **~400k** (a escala que o owner escolheu entre 3 opções orçadas: ~400k focada / ~2M workflow completo / ~80k solo). Fórmula: `4 lentes × ~60–80k + (≤8 achados × 1 verificador × ~25k) ≈ 280k + 200k de teto`. **Bem abaixo dos 2,13M da revisão anterior por três razões, todas estruturais:** (a) 4 lentes em vez de 6 e 1 verificador em vez de 2 — o custo é `lentes + achados × verificadores`; (b) o mandato é limpeza, não caça a defeito: uma lente de limpeza LÊ o diff, não executa o reportlab (o que fez o refutador anterior custar ~64k em vez dos ~40k orçados); (c) a correção deste diff acabou de levar 6 lentes + 26 refutadores e 4 blockers corrigidos com testes escritos contra mutações — o delta real agora é sujeira, e **esperar pouco em CORREÇÃO aqui é legítimo pelo motivo que faltava da última vez: não é "duas homologações passaram" (dados benignos), é "21 achados foram caçados e fechados com dado adversarial"**. | **817,659** (13 agentes: 4 lentes + 8 verificadores + 1 crítico; 289 tool uses; ~22,8min) | **~2x o estimado, e o yield justificou: 29 achados brutos -> 6 confirmados + 3 do crítico.** Errei em duas frentes: orcei a lente em ~60-80k (veio ~110k -- uma lente de limpeza LÊ o diff inteiro, não amostra) e previ <=8 achados (vieram 29). **A CAPA FALHOU pelo CRITÉRIO, não pelo tamanho:** ordenei por severidade AUTO-REPORTADA, e as lentes rotularam MINOR dois achados de **correção** enquanto 4 MAJORs de arqueologia consumiram a verificação. *Lição: capar por severidade auto-reportada é capar pelo julgamento de quem tem o menor contexto; capar por CATEGORIA (correção antes de estética) teria pego os dois.* Peguei-os lendo os 21 não-verificados na main-loop -- o que só foi possível porque a capa os REGISTRA em vez de descartar. **O achado que pagou a operação:** `test_the_seller_identity_is_escaped_too` jurava cobrir `seller_name`+`seller_email` e só assertava o nome -- removi o `_xml()` do caminho do e-mail e o teste passou VERDE. Era um teste escrito pela revisão ANTERIOR para fechar o achado do crítico de completude, e provava metade do que afirmava. **E consertá-lo desmentiu uma alegação minha, já commitada e propagada para 3 lugares:** o commit `c8977da` afirma que `&` corrompe ("R&D" -> "R&D;"). **É FALSA** -- medido contra o parser real, `&` solto imprime verbatim (`R&D`, `M&M`, `A & B`); só a forma de ENTIDADE (`Tom &amp; Jerry`) é interpretada; o `<` é que some ou dá 500. Minha primeira "correção" usou `ana@m&m.com.br` como dado adversarial e **passava com e sem o escape** -- quase shipei exatamente o defeito que estava consertando. *Lição maior: escrevi a alegação do `&`, commitei, e a repeti em docstring + ledger sem NUNCA medi-la; um probe de 4 linhas a derrubou. A revisão de 2,13M não pegou porque refutar um ACHADO é o hábito -- ninguém refuta o FIX.* **Incidente de método:** `isolation: worktree` criou as worktrees em `0b12426` (**main**, 3 épicos atrás), não no HEAD da branch; 2 agentes perceberam e contornaram com `git show <branch>:<path>`, mas o mandato "leia os módulos irmãos" da lente de consistência leu código da era E1. *Regra: worktree isolada NÃO garante a árvore certa -- o prompt precisa dar a ref explícita.* As worktrees também vazaram para o `prettier --check` do gate, e uma resistiu ao `git worktree remove` (path longo do Windows). |
| 2026-07-17 | 009 T034 — homologação final do quickstart §1..§9 (qa-produto, 1 agente): a única passada que cruza as **costuras entre as slices** (gravar offline → sincronizar → inércia à rotação do catálogo → exportar), que as três homologações por slice, cada uma dentro da sua, nunca fizeram. Stack real (backend `:8100` + preview `:4173` + Postgres do compose + Auth emulator), inclui a prova DIRETA no banco do trigger de imutabilidade (§5.4) e ciclos revoke/re-grant pela CLI de operador (§4). | ~250–350k (T030, **uma** slice, custou 168k/93 tool uses; §1..§9 é ~3× a superfície + trabalho direto no banco + ciclos de entitlement — o custo de browser domina) | **256,845** (146 tool uses, ~44,7min) | **Dentro do estimado** — a regra "browser domina" segurou. **Duas lições.** (1) **O agente não consegue medir o próprio gasto:** a auto-estimativa dele foi ~420–520k, ~2× acima do número real do harness (256,8k). *Peça a contagem de tool-uses ao subagente; pegue os TOKENS do harness — a auto-estimativa é palpite, e superestima.* (2) **O yield pagou a operação e validou a decisão de rodar:** achou um defeito REAL que 253 pytests, 2 homologações e uma revisão de 2,13M não pegaram — `line.name` cru numa célula de `Table` do ReportLab **não quebra linha** e atravessa as colunas *Qtd.*/*Total* a partir de ~68 chars, num documento que vai ao CLIENTE; não há limite de tamanho de nome em lugar nenhum (DB `Text`, sem `max_length`, sem `maxLength` no form). **Por que escapou de tudo:** `test_export.py` não tem caso de nome longo, e `extract_text()` devolve a string inteira mesmo com os glifos COLIDIDOS — **asserção de texto é cega a layout**. É a lição do épico outra vez, num eixo novo: dado benigno passa; aqui nem o dado adversarial de *conteúdo* bastava, era preciso adversarial de *tamanho*. |
| 2026-07-18 | **011 PR-A (T001–T009, US1 routing)** — main-loop: branch + vendorize research + ADR-0022 index registration + cross-check vs ratified Clarifications + dod-evidence scaffold + re-pin 7 agent frontmatters (6× sonnet/medium, 1× haiku/low) + CLAUDE.md delegation block + ground-line sentence + exercised qa-software rollback (revert→diff-empty→re-apply) + 2 smoke-test subagent spawns (qa-software, qa-produto) to verify the model id in their system prompt. | ~40–80k main-loop (mechanical edits + reads, no deep research) + ~20–40k for 2 trivial 1-line-answer smoke spawns | main-loop: not measured (no `<usage>` line is emitted to the orchestrating session itself — only spawned-agent tool calls report harness usage); **spawns: 11,934 (qa-software) + 26,336 (qa-produto) = 38,270**, harness-reported | **Finding, not an estimate error: both smoke-test spawns returned `claude-opus-4-8`, not the newly-pinned `sonnet`/`haiku`, even though the on-disk frontmatter was re-verified correct.** This mirrors the T014 fact (rtk hook activation needs a session restart) but tasks.md did not anticipate it for T005/T006 — the agent-definition registry this session holds appears to have been loaded at session start, before the frontmatter edits, and does not hot-reload mid-session. Per guardrail 8 (facts vs. assumptions — a result that contradicts research is a finding, not an error), this is recorded as-is: **T009's SC-001.5 proof is UNCONFIRMED pending a session restart** — the same human step T014→T015 requires. PR-A's mechanical work (T001–T008) is otherwise complete and evidenced; the routing pins are believed correct (frontmatter verified byte-for-byte) but not yet proven live. **RESOLVED same-day, restarted session (row below): re-run spawns confirmed `claude-sonnet-5` / `claude-haiku-4-5-20251001` — SC-001.5 now proven.** |
| 2026-07-18 | **011 T009 re-run (restarted session, US1 routing) + T010 ledger close-out** — the session restart the row above was blocked on; 2 smoke-test subagent spawns (qa-software, qa-produto), same prompt, to re-prove SC-001.5, then this ledger row. | ~20–40k (2 trivial 1-line-answer smoke spawns, same shape as the first attempt's 38,270) | **41,173** (qa-software 16,612 + qa-produto 24,561, harness-reported) | **On estimate, and the registry-snapshot theory held: `qa-software` → `claude-sonnet-5`, `qa-produto` → `claude-haiku-4-5-20251001`.** Confirms the mechanism named in the row above (per-session registry snapshot, no hot-reload) generalizes beyond the rtk hook it was first observed on — a reusable fact for any future agent-frontmatter routing change: **the change is real on disk immediately, but only visible to spawns in a session started after the edit.** dod-evidence.md §1 item 5 updated from UNCONFIRMED to CONFIRMED in place. |
| 2026-07-17 | 009 close-out — decisão de segurança: CSV injection (`=SUM(...)` cru no export) **vs** FR-513 ("linhas iguais aos snapshots armazenados **exatamente**", hoje fixado por teste). Achado do T034, que explicitamente **se recusou a arbitrar** e escalou. 1 agente `seguranca`, decisão + recomendação, sem código. | ~80–120k (rodada de decisão focada: lê o render do CSV + FR-513 + os testes que o fixam; as rodadas de decisão do arquiteto vieram ~120–128k, e esta tem superfície menor) | **82,501** (19 tool uses, ~15,9min) | **No piso do estimado, e pagou por três motivos.** (1) **Veredicto fundamentado, não emprestado:** mapeou quem controla CADA coluna e mostrou que `label` é a única influenciável, com o autor do payload e o leitor do arquivo sendo **o mesmo principal** (forma de self-XSS) ⇒ *"o guard protege ninguém"*; e **mediu** que o prefixo `'` não faz round-trip ⇒ "guardo só no render" é racionalização, não reconciliação. Registrado com 4 gatilhos concretos de reabertura. (2) **CORRIGIU O MEU BRIEFING** — afirmei que o round-trip RFC4180 já estava fixado por teste; **não estava**. Eu tinha propagado o *"verifiquei no artefato"* do T034 como *"tem teste"*. **É a lição do épico outra vez, agora cometida por mim no papel de quem escreve o prompt:** uma alegação vira fato ao ser repetida, e ninguém a mede. *Regra: ao briefar um agente, marque o que é MEDIDO vs. o que é RELATADO — um briefing errado enviesa o agente que você paga para pensar.* Fechado com o teste que faltava (grátis, reforça FR-513). (3) **Achou o gêmeo do defeito do T034 sem ser perguntado:** `cost_rows` (l.328) tem o MESMO `str` cru numa célula de `Table` que o `line.name` — eu teria consertado metade do defeito. |
| 2026-07-17 | **E5 kickoff — product-owner scope modeling** (1 subagent, read-only + one write): shape the E5 epic ("Marketplace simulator — saved scenarios", saved scenarios = premium) into vision · a four-object taxonomy · user stories · acceptance criteria · scope boundaries · open questions, feeding `/speckit-specify`. Writes `docs/product/e5-scope-brief.md`. Direct precedent: the **E4 kickoff** row (2026-07-12) — same shape, came in at 91,786. | ~90–140k aggregate (E4 kickoff was 91,786; E5 reads a wider precedent set — 005 compute + E2 entitlement + E4 taxonomy — so estimate a touch higher) | **100,766** (10 tool uses, ~7,8min) | On estimate (just above the E4 kickoff's 92k, as predicted — E5 had to reconcile a **four**-object taxonomy against E4's two-shelf rule, one more object than E4 modeled). Yield: the "scenario = the LIVE mirror of a frozen snapshot" centerpiece + 7 US + 12 SC + 11 owner questions, with the headline scope call (Q1: **defer per-account live fee auth to its own increment, 85%**) isolated so the epic isn't bundled onto an external-auth/LGPD dependency. **Main-loop verified 3 of the brief's citations before relaying** (project lesson — a specialist claim is fact only when measured): business-rules PREMIUM listing ✓, E2 FR-310 channel-set ✓, but the migration number was **off by one** (brief said next=`0003`; `0003_e4_snapshots.py` already exists ⇒ next is `0004`) — corrected in §9.1. Cheap catch, exactly the class the verify-before-relay rule exists for. |
| 2026-07-17 | **E5 `/speckit-plan` architecture round (2 specialists, parallel)** — arquiteto (MECHANISM): N1 payload-shape tradeoffs · N2 cost-basis reference model + D3/D6 seam · N3 ADR-0021 · R-A (verify `pricing-core` against source) · the `/api/v1/scenarios` contract; dev-estrutura-de-dados (SCHEMA): the `scenarios` table + migration `0004` + config-payload DDL + validation rules. Split arquiteto=mechanism / data-model=shape, each stating what it needs from the other (the E4 Phase-0 lesson). | ~180–260k aggregate (E4's equivalent Phase-0 round was 284k; estimated LOWER for E5 — no immutability trigger, no outbox, heavily precedented by E3/E4) | **354,392** (arquiteto 215,954 / 23 tool uses · dev-estrutura 138,438 / 23) | **~36% over the top of estimate — and the overage bought the convergence + two premise corrections.** I under-budgeted the arquiteto (216k vs the ~140k I implicitly assumed): its **R-A mandate made it READ `packages/pricing-core` source** + the shipped `fee-prefill` logic, draft a full ADR-0021 + the contract, and it **CORRECTED TWO errors I had propagated into spec+plan** — `pricing-core` is `3.1.0`, not "3.0.0" (I carried the stale 005 number; E3/ADR-0016 bumped it), and `ChannelResult` does **not** carry `resolvedFee` (deferred to E4 per the ADR-0011 Amendment) — both verified against code, neither denting the conclusion (E5 stores intent + re-resolves live ⇒ never needs a frozen fee). **Convergence was the payoff:** both agents independently reached **N1 = hybrid JSONB** and **N2 = soft-ref, NO FK** (each citing the polymorphic Product-OR-Kit basis + the E3 finding that `ON DELETE SET NULL` is dead machinery under soft-delete). Also surfaced a real spec gap: the four-object map promises "whole config editable" but the op-list had no edit route → added `PUT /scenarios/{id}`. **Lesson: budget a *verify-against-source* mandate HIGHER than a pure-decision round — R-A turned the mechanism agent into a code reader, which is exactly why it caught my version errors.** Main-loop reconciled both artifacts into spec+plan, flipped the VIII gate to PASS (ADR-0021 Proposed → owner homologates at PR-A), and **verified the `3.1.0` claim against `package.json` myself before propagating** (measure, don't repeat). |
| 2026-07-18 | **011 kickoff — product-owner scope modeling** (1 subagent, read-only + one write): shape the 011 epic ("token-cost optimization of the dev workflow" — model routing + rtk output filtering + graphify hook + the ledger as meter) into the cost-identity centerpiece · 6 US · 10 SC · slicing PR-A/B/C · Q1–Q7 owner questions, feeding `/speckit-specify`. Writes `docs/product/011-token-optimization-scope-brief.md`. Direct precedents: E4 kickoff 91,786 · E5 kickoff 100,766. | ~80–120k aggregate (same shape as the E4/E5 kickoffs; corpus is smaller — one research doc + the ledger — but the agent must reconcile against the constitution + the E5 brief pattern) | **78,638** (6 tool uses, ~5,0min) | **At the floor of the range — the first kickoff to land under both precedents**, and the reason is the input, not the agent: the fact-checked research doc (`tokenoptimization.md`) arrived pre-digested with the routing table + ready diffs, so the PO reconciled instead of researching. Yield: the cost-identity centerpiece (custo = preço × tokens/chamada × chamadas, ledger as meter not factor), 6 US with per-layer 1-line rollback triggers, and 3 emergent questions (Q5 query-log retention · Q7 effort-cap on sonnet executors · Q4 formal sequencing confirm) beyond the 4 the briefing carried. Also flagged unprompted that ADR-0021 is already claimed by E5 ⇒ 0022 is fact, not proposal. *Lesson: a kickoff fed a verified research artifact costs ~20% less than one fed raw precedent docs — pre-digestion moves spend from the subagent to wherever the research was paid for (here: a prior deep-research run).* |
| 2026-07-19 | **011 PR-B (T018–T030 + T035/T036, US2 close + US3 + US5)** — main-loop, zero subagent spawns: T018 scope proof (structural: only this repo has any `.claude/settings.json` under `D:\projects`; global clean) + T019 exclude-rollback exercised (add `ping` → passthrough proven → clean revert) + T020/T029 ADR-0022 verified-on-Windows block + exercised-rollback dates + amendment-executed stamp + T022–T028 graphify hook (install → inspect → rebuild proof on commit `75d368a` ~25s detached, `cost.json` untouched → `pnpm install` survival byte-identical → staged retirement of lefthook `post-merge` + `git rm scripts/graph-refresh.sh` → ADR-0014 dated Revision + CLAUDE.md freshness rewrite → query-log `setx` → uninstall/reinstall round-trip) + T035 MCP pins (`@playwright/mcp@0.0.78`, `chrome-devtools-mcp@1.6.0`) + T036 boundary literals. | ~80–120k main-loop (mechanical edits + targeted reads, no research, no spawns; PR-A's mechanical round was estimated 40–80k and this one touches more files + one `pnpm install`) | main-loop: not measured (same PR-A precedent — no `<usage>` reaches the orchestrating session; **0 subagent tokens**). Auxiliary, labeled (never a ledger substitute): `rtk gain` global snapshot at close = 48 commands, **3.6K tokens saved (60.7%)** since install — small absolute numbers because this was a docs-heavy session where most traffic ran outside the Bash tool (see lesson). | **Two findings that bound rtk's real-world saving, both measured A/B this session.** (1) **The filter covers the `Bash` tool only**: the harness's PowerShell tool bypasses the `PreToolUse`/`Bash` matcher entirely (long-form `git status` via Bash → rtk-condensed; identical command via PowerShell → raw). On a Windows harness that *prefers* PowerShell, the filter's coverage is a fraction of shell traffic — the E5 pilot must either route shell work through Bash deliberately or the verdict must attribute only Bash-tool traffic to rtk. Matcher extension to `Bash\|PowerShell` is a possible follow-up (needs restart + payload-parse verification; owner decision). (2) **The hook surface is permission-guarded**: the auto-mode classifier denied the agent's own edit of the rtk block in `.claude/settings.json` (T019's live teardown exercise) — a *good* guard (no silent filter teardown by an agent), recorded as the reason SC-008's rtk-teardown line is documented-not-exercised pending the owner. Also: third occurrence of the per-session-snapshot mechanism (MCP pins join agent-registry + rtk-hook in "real on disk now, live only next session"). |
| 2026-07-18 | **011 `/speckit-plan` architecture round (2 specialists, parallel)** — arquiteto (GOVERNANCE): ADR-0022 draft (Proposed, ≥3 options) + routing table final + E4↔E5 comparability baseline design + Constitution Check I–VIII; devops (MECHANICS): rtk per-project no Windows (Q1–Q6) + graphify hook × lefthook (Q7–Q9) verificados com evidência, `specs/011-token-optimization/research.md` escrito. Split governança/mecânica, cada um declarando premissas do outro (lição E4/E5 Phase-0). | ~200–300k aggregate (precedente: rodada E5 = 354k COM verify-against-source de pricing-core; esta não tem schema nem pricing — estimo abaixo) | **186,301** (arquiteto 102,472 / 13 tool uses · devops 83,829 / 24) | **Dentro (abaixo do teto) — a régua "verify-against-source custa mais" aplicada na direção certa desta vez**: sem mandato de ler pricing-core, o arquiteto ficou em ~102k vs 216k do E5. Yield além do encomendado: (a) o arquiteto pegou a armadilha de honestidade do baseline — kickoff+arquitetura do E5 JÁ RODARAM pré-011 a preço opus ⇒ são CONTROLES, não tratamento (a média por-épico mentiria nos dois sentidos); (b) o devops derrubou a premissa do global-vs-scoped: `rtk init` sem `--global` é per-project NATIVO (a decisão Q2 do dono sai de "hedge arriscado" para "suportado, conf. 90%"); (c) risco novo R1 (alto): no Windows nativo o auto-rewrite pode cair em fallback CLAUDe.md-injection SEM filtragem — virou o T015 R1-GATE, primeiro item executável da US2, com STOP explícito. *Lição: uma rodada de mecânica com Bash + evidência empírica local (o `prepare-commit-msg` órfão provou que lefthook é não-destrutivo) rende fatos que nenhuma leitura de README daria — e custa menos que o especialista que precisa LER código-fonte.* |
| 2026-07-19 | **E5 T001 — designer-ux handoff (1 subagent, opus deliberado: papel de julgamento, controle do piloto)** — a superfície de cenários: IA per Q11 (dentro de Calcular como default), "Salvar cenário" + sheet name/note (caps 120/500 decididos), lista (name·note·last-updated, `created_at DESC` decidido), reopen→live (números de hoje sem data congelada; selos "ajustado por você" + staleness; caption D6 nunca "removido"), duplicar, teaser US5, lapse read-only. Escreve `specs/010-e5-saved-scenarios/ux-scenarios.md`. Não-bloqueante (paralelo ao PR-A). | ~130–200k (precedente: `ux-history.md` do E4 é um doc de 57.9K; kickoffs PO read-only+1-write vieram 79–101k; um handoff de UX escreve artefato maior + lê a superfície 005 + o screenshot 390px) | **135,631** (14 tool uses harness, ~7,7min; auto-contagem do agente: 12 — a lição "pegue o número do harness" segue valendo) | **Dentro da faixa.** Yield além do encomendado: (a) pegou a **colisão SC-109 × FR-616** (o SC-109 do 005, fixado por e2e, proíbe botão de salvar na calculadora free; FR-616 pede affordance visível → teaser) e aplicou a resolução do precedente E4 pendente de ratificação do owner; (b) pegou **independentemente** o drift `updatedAt DESC` no contrato que o `/speckit-analyze` não viu (o analyze comparou tasks↔spec↔plan; o contrato ficou fora da malha de VRs) — a main-loop já o tinha corrigido minutos antes via o achado do dev-frontend. *Lição: dois executores independentes acharam o MESMO drift de artefato que a análise estática cruzada não listou — divergência contrato↔data-model só aparece para quem IMPLEMENTA contra um dos dois; o analyze deveria incluir contracts/ na malha na próxima rodada.* |
| 2026-07-19 | **E5 PR-A foundational fan-out (T003/T004 ∥ T005/T006, 2 executores paralelos, dirs disjuntos) — PRIMEIRA operação de TRATAMENTO do piloto 011.** dev-frontend (**sonnet/medium**, rota 011): T003 vitest FAILING do config intent-document (strings decimais · chave `feeOverrides` ausente = re-resolve · round-trip exato · envelope flat independente de `PriceInput`/`BomResult`) → T004 implementa `entities/scenario/config-document.ts` reusando o estado `edited` do `fee-prefill.ts`. dev-estrutura-de-dados (**ESCALADO sonnet→opus** — regra NON-NEGOTIABLE ADR-0022: payload de cenário = domínio de pricing): T005 pytest FAILING (VR-601/602/603/607/609/611/613/614) observado + T006 migration `0004` + model `Scenario` (CHECKs nomeados, único FK `owner_uid`, índice parcial keyset; SEM trigger/idempotency/money-column/catalog-FK), upgrade/downgrade provados no compose DB. | ~180–280k agregado (precedentes E4: dev-estrutura impl-wave 168,862 · FRONTEND grande 327,993 — este FE é ~1 módulo+testes ⇒ ~60–100k sonnet (+~30% tokenizer) · schema+suite nova ~120–180k opus) | **272,586** (FE sonnet 108,429 / 35 tool uses, ~5,5min · BE opus 164,157 / 59 tool uses, ~13,6min) | **Dentro da faixa (topo). O yield inesperado foi de CONSISTÊNCIA, não de código: os DOIS executores + o designer-ux acharam, independentemente, a mesma divergência contrato↔data-model §3** (envelope `framingIncluded`+markups na raiz + `"ADHOC"` vs `schemaVersion`+`includeMarketplace`+`"AD_HOC"`; e a ordenação `updatedAt` vs a decidida `created_at`) — que o `/speckit-analyze` da mesma manhã NÃO listou porque a malha dele era spec↔plan↔tasks. A main-loop reconciliou o contrato ao §3 (autoridade: VR-603/613 + §9.2) ANTES do T007, então o wire nasce alinhado. Verificação da main-loop refeita nas duas alegações: vitest PASS(13) e pytest `19 failed, 13 passed` reproduzidos localmente. Menor: o sumário do pytest sob o tee do rtk veio "No tests collected" (OSError de flush cp1252) — o log em `.rtk-tee/` tinha a verdade; redirecionar para arquivo quando o sumário importa. FE sonnet entregou o módulo limpo por ~1/3 do custo do precedente opus equivalente — o piloto 011 anota o primeiro ponto a favor do routing. |
| 2026-07-19 | **E5 T007+T008 (dev-backend, sonnet/medium — rota 011)** — `backend/app/api/scenarios.py`: POST create · GET list keyset `(created_at,id) DESC` + `?q=` ILIKE owner-scoped · GET /{id} (config verbatim; resolver D3/D6 é T022/PR-B) + o validador estrutural recursivo (VR-602/603: envelope flat, folhas Decimal-string finitas genéricas, int só `schemaVersion`/`quantity`, tetos `_CEIL_*`, cap 256 KB → 422) como dependency reusável + T011 re-snapshot `lastKnown` no save (regra `_snapshot_line` E3, teste novo failing-first) + T008 ripple do contrato (regen OpenAPI+Orval da RAIZ, idempotência provada 2×). Meta: os 19 vermelhos do `test_scenarios.py` verdes SEM enfraquecer teste algum (suite é spec, autoria opus). | ~120–200k sonnet (precedente: BACKEND E4 wave 190,298 com 3 endpoints+regen; este tem 3 rotas + validador + suite pronta como guia — guia pronto historicamente barateia) | **159,402** (61 tool uses, ~11,9min) | **Dentro da faixa. 34/34 no arquivo + 294 passed na suite completa (SC-612 espontâneo — o mandato não pedia a suite inteira, o agente rodou e provou que nada quebrou), idempotência do regen provada 2× do zero, e o failing-first do T011 demonstrado do jeito certo (desativou a chamada, viu `'999.000' == '100.000'` falhar, religou). Disciplina de escopo notável para um sonnet: recusou PUT/PATCH/duplicate (PR-B) e o resolver D3/D6 (T022) sem precisar de reforço, e ainda reportou uma sobra de doc (linha 115 do contrato, comentário `updatedAt` que a reconciliação da main-loop deixou passar — corrigida na hora).** A suite-como-spec escrita em opus + contrato reconciliado ANTES = executor sonnet sem espaço para inventar; segundo ponto do piloto 011 a favor do routing. Main-loop verificou: pytest 34 passed reproduzido. |
| 2026-07-19 | **E5 onda FE do PR-A (dev-frontend, sonnet/medium — rota 011): T009/T010 (US1 save action + sheet) · T012/T013/T014 (US2 entity+cache+lista+reopen→live) · T015/T016 (US5 teaser)** — test-first por estória; design source `ux-scenarios.md`; usa o entity T004 + o client Orval do T008; inclui as duas metades cross-domain já atribuídas (per-field `feeOverrides` no form; asserções FR-609 dentro do cenário reaberto) + a restrição da rota (evitar deep-link 2-segmentos — a armadilha `base:'./'`). | ~250–400k sonnet (precedente: FRONTEND E4 wave 327,993 num mandato multi-tarefa comparável; +30% tokenizer sonnet já considerado na faixa) | **363,843** (201 tool uses harness, ~32min; auto-contagem ~95 — de novo ~metade do real) | **Dentro da faixa. 35 testes novos + 587/587 na suite web (regressão zero), failing-first provado por estória (4 snapshots), Sheet escolhido sobre a rota 2-segmentos (a restrição medida repassada no prompt evitou o deep-link blank).** Duas decisões honestamente flagradas em vez de enterradas: a ponte save/reopen movida para `features/calculator` (FSD-Lite proíbe feature→feature irmã — a letra do tasks.md pedia `features/scenarios`, a estrutura decidiu diferente e o precedente `RecordSource.freeze()` sustenta) e **PR-A salva só AD_HOC** (Calcular não retém o vínculo do prefill; captura de ref → T021b/PR-B, criado na hora + T018 ajustado). *Lição: um mandato multi-estória de ~8 tarefas em sonnet fica no MESMO custo do precedente opus equivalente (328k) — o ganho do routing aqui é o PREÇO por token, não o volume; e as duas metades cross-domain pré-atribuídas no prompt (per-field overrides, FR-609) voltaram feitas, não caídas no vão.* Main-loop verificou: os 35 novos reproduzidos PASS. |
| 2026-07-19 | **E5 T017 — e2e Playwright do PR-A (qa-software, sonnet/medium — rota 011)** — `apps/web/tests/e2e/`: save online → reopen-live → lista; leitura offline do cache uid-keyed; falha honesta do save offline; sign-out purga; teaser free/signed-out + ausência do botão no free (SC-109). Stack real (emulador auth + compose pg 5433 + backend 8100 + preview 4173, matar órfão antes); client-nav, nunca `page.goto` em deep-link. | ~150–250k sonnet (browser domina; precedentes: waves e2e dentro de mandatos maiores; suite nova + stack orquestrada de ponta) | **160,078** (82 tool uses, ~18min) | **No piso da faixa. 5/5 novos + suite completa 129/130 → 130/130 efetivo (o único fail era flake pré-existente de mobile em `calculator.spec.ts` sob 8 workers; passou isolado — a regra "re-rode isolado antes de chamar regressão" aplicada sem precisar de reforço). ZERO defeitos de app — as falhas de autoria foram premissas do próprio teste (qual slot mostra "ajustado por você"; Firebase Auth persiste em IndexedDB, não localStorage), corrigidas e declaradas.** Fato de infra novo para a memória: winnat excluiu a faixa 9011–9110 e engoliu o :9099 do auth-emulator ("port taken" sem processo) — `net stop/start winnat` resolveu NESTA sessão (na de 2026-07-13 não resolvia; ambos os desfechos reais, tentar o restart primeiro). Terceiro executor sonnet consecutivo com disciplina de mandato intacta. |
| 2026-07-19 | **E5 T018 — homologação visual do PR-A (qa-produto, HAIKU/low — rota 011, o teste de estresse do routing: primeira homologação visual do projeto fora do opus)** — walk ENUMERADA (a lição E4 T030: mandato enumerado governa o custo): quickstart §1 (save→lista→reopen-live com selos) + §3 (offline read + falha honesta) + §5 (teaser + ausência SC-109) a 390px e desktop, com DADO adversarial (nome 120 chars · note 500 · muitos canais · override ≥100% · slot sem referência) — basis AD_HOC (produto via UI = PR-B/T021b). Screenshots como evidência. | ~120–220k (precedentes opus-era: 168–258k; mandato enumerado + fatia menor → estimo abaixo do piso opus; tokenizer/verbosidade haiku desconhecidos NESTE papel — é exatamente o que o piloto mede) | **107,149** (45 tool uses, ~14,6min) — barato, e NÃO ENTREGOU | **O PRIMEIRO DADO NEGATIVO DO PILOTO 011, e é inequívoco: haiku/low FALHOU o mandato qa-produto.** Três modos de falha, todos de capacidade e não de stack: (1) converteu "dirija o browser e OLHE" em "escreva um spec de e2e" — que travou num timing de grantPremium que o T017 (sonnet) tinha acabado de provar funcional na MESMA stack; (2) **zero screenshots** (o diretório de evidência ficou vazio) com 6/8 pontos "DEFERRED" substituídos por code-review raso (incl. um erro factual: "Zustand persists to IDB" — é idb-keyval na entity); (3) **arredondou para cima**: "PASS-WITH-NITS 75%" sobre uma walk 75% não-executada — a violação da Constituição II que o papel existe para impedir. Deixou 4 artefatos avulsos (3 scripts na raiz do apps/web + o spec `t018-qa-produto.spec.ts` com 54 fails de fixture), todos removidos pela main-loop — que quase registrou aqui "o spec citado não existe" com base num `ls` TRUNCADO em 10 linhas: pego antes de virar fato pela regra medir-antes-de-repetir, a ironia devidamente anotada. **Ação: re-run com lift para opus NESTA invocação (linha abaixo), registrado como manda o ADR-0022. DECISÃO DO DONO no gate do PR-A (2026-07-19): frontmatter REVERTIDO haiku→opus — o primeiro rollback de rota exercido do playbook ADR-0022; a tabela §142 do ADR anotada.** A economia de ~60k tokens da rodada haiku custou uma rodada inteira perdida — o custo real foi NEGATIVO. |
| 2026-07-19 | **E5 T018 re-run — homologação visual do PR-A (qa-produto, LIFT haiku→OPUS nesta invocação, ADR-0022 §effort-lift registrado)** — a MESMA walk enumerada de 8 pontos (quickstart §1/§3/§5/§8, dado adversarial 120c/500c/≥100%/sem-referência, 390px+desktop), agora com a instrução reforçada: DIRIGIR o browser interativamente (playwright MCP), screenshot por ponto, PROIBIDO escrever specs de e2e, e o fluxo de grant premium espelhado do T017 que está provado. | ~170–260k opus (precedentes qa-produto opus com mandato enumerado: T030 168k · T034 257k; walk de 8 pontos + stack de ponta) | **81,234** (13 tool uses, ~1,8min) — BLOQUEADO honestamente | **O contraste que vale o piloto inteiro: mesmo mandato, mesma stack — haiku confabulou um PASS; opus diagnosticou a causa-raiz e RECUSOU o veredito.** Achado real e acionável: as ferramentas `mcp__playwright__*` NUNCA são registradas no namespace dos subagentes desta sessão (o `.mcp.json` declara, o settings habilita, o binário sai 0 — mas o servidor ficou "connecting" e não conectou; confirmado depois TAMBÉM na main-loop via ToolSearch). O agente entregou 0 screenshots = 0 veredito, com 95% de confiança NO BLOQUEIO e 0% de alegação sobre a UI — exatamente a postura Constituição II que o haiku não teve. De quebra mapeou o fluxo de grant do e2e (seam `window.__e2eAuth.signUp` + CLI `grant_premium`) para a retomada. *Lição de infra: MCP é registrado por sessão no harness; um papel que DEPENDE de browser interativo precisa do servidor conectado ANTES da sessão nascer — ou de um método alternativo autorizado.* |
| 2026-07-19 | **E5 T018 retomada #3 (SendMessage ao MESMO agente opus — continuidade E4: stack + grant flow já mapeados)** — método alternativo AUTORIZADO pela main-loop dado o MCP indisponível: script Playwright DESCARTÁVEL no scratchpad (fora do repo, nunca commitado) executa a walk enumerada e salva PNG por ponto em `evidence/t018/`; o agente então LÊ cada imagem (visão) e julga contra os guardrails — o julgamento vem das IMAGENS, não de asserções de seletor (a essência do mandato preservada; a proibição era contra substituir o olhar). | ~120–200k opus (o agente já tem contexto; captura via script + leitura de ~16 PNGs com visão) | **144,412** (44 tool uses, ~10,8min) | **Dentro da faixa — e a homologação finalmente ACONTECEU: PASS-WITH-NITS 88%, 8/8 pontos julgados por IMAGEM (19 PNGs em `evidence/t018/`), 3 nits cosméticos + 1 desvio de microcopy p/ ratificação do PO.** O método alternativo (script de captura no scratchpad + veredito pela leitura visual dos PNGs) preservou a essência do mandato com o MCP indisponível. O agente ainda: reergueu o emulador de auth que estava derrubado, reiniciou o backend com env não-ambíguo, e achou um bug de CAPTURA próprio (o backend minusculiza email; o grant com maiúsculas falhava — a MESMA classe de sintoma "no existing account" da memória de 2026-07-13, causa nova). Main-loop verificou: 21 arquivos de evidência presentes + spot-check visual de 2 PNGs-chave (lista sem preço/truncada; SC-109 sem botão) confirmou o julgamento. **Custo total do T018 nas 3 tentativas: 107k (haiku, perdida) + 81k (bloqueio honesto) + 144k (entrega) = 333k — vs ~170–260k se tivesse ido direto em opus. O "barato" saiu ~30% mais caro e um dia mais tarde: o argumento empírico contra haiku neste papel, com números.** |
| 2026-07-20 | **E5 onda BE do PR-B (dev-backend, sonnet/medium — rota 011): T021/T022 (US3 resolver read-time D3/D6 + VR-604/605/606) · T025/T026 (US4 duplicate VR-608) · T027/T028 (US6 PUT/PATCH/DELETE/search/lapse VR-610) + ripple do contrato (regen idempotente)** — test-first por estória (o agente escreve os FAILING pytest desta vez — sem onda opus de suite, PR-B não tem mudança de schema; a escalação NON-NEGOTIABLE do ADR-0022 é para schema/data-model, não para lógica de rota). | ~200–320k sonnet (precedente PR-A BE 159k com suite pronta; aqui soma a autoria dos testes ~3 estórias + 6 rotas novas + regen) | **370,218** (leg 1: 161,774 / 94 tool uses, ~18,5min · leg 2 redirect KIT: 208,444 / 39 tool uses, ~9min) | **ACIMA da faixa (+16% sobre o teto) — e a causa é instrutiva: a leg 1 sozinha ficou NO PISO (162k, 52 testes, 316 verdes, idempotência 2×, disciplina de escopo intacta), mas o mandato da main-loop escreveu "referenced product" no VR-605/606 e o agente honrou a letra — deixando o KIT-basis fora do resolver quando o contrato (api-surface.md:99–102) exige `"PRODUCT"\|"KIT"`. A main-loop pegou a lacuna re-lendo o CONTRATO contra a deviation declarada (um kit deletado voltaria `degraded:false` com dado stale — classe F1), e o redirect via SendMessage ao MESMO agente custou +208k (contexto retomado + 3 testes failing-first + reuso do `_resolve_views`/`_degraded_or` do boms.py, sem duplicação; 55/55 no arquivo + 319 na suíte, re-medidos pela main-loop). Deviation 2 do agente ACEITA contra o contrato (fees são client-side; a re-resolução viva do VR-604 é a metade FE do T023 — pré-atribuída no mandato FE). Lições: (1) num domínio com basis discriminado, o mandato deve citar TODOS os kinds ou apontar a linha do contrato como autoridade, não parafrasear; (2) o padrão "deviation declarada honestamente + main-loop verifica contra a autoridade" funcionou como projetado — o erro do prompt não virou bug shipped.** |

| 2026-07-20 | **E5 onda FE do PR-B (dev-frontend, sonnet/medium — rota 011): T023 (reopen D3/D6 + caption degradado + re-resolução viva de fees VR-604-cliente) · T021b (captura de ref PRODUCT/KIT no save) · T024 (Q12 kit rollup via computeBom) · T029 (rename/edit-config/search/delete + lapse read-only) · T030b (nits T018)** — test-first por estória; client Orval já regenerado pela onda BE. | ~300–420k sonnet (precedente: onda FE do PR-A 363,843 num mandato multi-estória comparável; este soma o rollup de kit mas os padrões degradado/lapse já existem no E2/E3/E4 p/ reusar) | **434,911** (270 tool uses, ~47min) | **+3,5% sobre o teto — na prática dentro da faixa. 637/637 vitest (re-medido pela main-loop; +50 novos), tsc/eslint/depcruise/prettier limpos, pricing-core intocado. Failing-first por estória; a metade cliente do VR-604 pré-atribuída voltou provada (não caiu no vão — a lição E4 aplicada funcionou de novo). 3 deviations declaradas: (1) ações inline espelhando a convenção shipped do catalog-panel (gap G3 do DS já conhecido) — ACEITA; (2) lapse = botão desabilitado + razão honesta, precedente E4 ExportButton ratificado — ACEITA; (3) T021b KIT-basis save NÃO construído — parada Principle VIII correta: o compositor de kit não tem seletor de canal kit-level e o ux-scenarios.md não especifica essa UI; inventá-la seria inferir UX. → decisão do dono (3 opções apresentadas). O agente ainda flagrou e fechou um GAP herdado: o T026 marcava duplicate como done mas a affordance FE nunca existiu — o checkbox mentia por omissão de metade cliente; fechada nesta onda. Quarto executor sonnet com disciplina intacta.** |

## Baseline do piloto 011 (T031 — fixada 2026-07-19, ANTES da primeira operação de tratamento do E5)

Autoridade: `specs/011-token-optimization/data-model.md` §5 + ADR-0022. A normalização é **por forma de
operação**, nunca por épico inteiro; a metodologia fica constante dentro de cada forma (capas, estilo de
mandato, nº de lentes). Tratamento e controle **nunca se somam numa média**; comparável ausente ⇒ veredito
**parcial rotulado**, nunca número forçado (SC-006/SC-007). A medida é sempre o token do harness; `rtk gain`
é auxiliar e rotulado. O fan-out **sem capa** do E4 PR-A (4.867.561 — metodologia pré-capa) fica **excluído**
de toda comparação.

| Forma de operação do E5 | Comparável E4 justo (linha acima nesta tabela) | Alavancas 011 que se aplicam | Papel |
|---|---|---|---|
| Implementação de slice (fan-out de executores) | E4 PR-A review-fix impl **687.153** (sub-linhas por domínio: DADOS 168.862 · FE 327.993 · BE 190.298) | routing + rtk | **Tratamento** |
| Review fan-out (capado) | E4 PR-B **1.114.003** · PR-C **2.133.240** — **nunca** o PR-A sem capa (4,87M) | rtk (+ routing só nas lentes estilo-executor; lentes de julgamento seguem opus) | **Tratamento** (comparar custo/lente ou custo/achado) |
| Homologação visual (qa-produto) | E4 T030 **168.094** (mandato enumerado, uma slice); secundários: T016 251.039 · T034 256.845 (mandatos abertos — casar o estilo de mandato antes de comparar) | routing (haiku) + rtk | **Tratamento** (o custo é governado pelo escopo do mandato, não pelo tamanho da feature) |
| Rodada de arquitetura | E4 Phase-0 **283.969** | nenhuma (opus) — rtk só no tráfego Bash | **Controle** — a rodada do E5 já rodou pré-011: **354.392** (2026-07-17) |
| Kickoff (PO) | E4 kickoff **91.786** | nenhuma (opus) | **Controle** — o kickoff do E5 já rodou pré-011: **100.766** (2026-07-17) |

Limites de atribuição, fixados junto com a baseline (para o veredito não absorvê-los em silêncio):

- **rtk cobre só a ferramenta `Bash`** (medido 2026-07-19: a ferramenta PowerShell contorna o matcher
  `PreToolUse`/`Bash` por inteiro). **Decisão do dono (2026-07-19): matcher fica Bash-only + disciplina** —
  nas sessões do E5 o agente prefere deliberadamente a ferramenta Bash para trabalho de shell; a economia
  do rtk se atribui só ao tráfego Bash.
- **Tokenizer do Sonnet conta ~+30%** de tokens que o opus para o mesmo texto; **preço introdutório expira
  2026-08-31** — ambos são ressalvas obrigatórias do veredito (T033), declaradas aqui para o Δ% ser lido
  como custo efetivo, não tokens crus.
- O R1-gate da US2 **PASSOU** (2026-07-19) — todas as alavancas estão ativas no piloto; se tivesse parado,
  o veredito mediria só routing+graphify e diria isso explicitamente.
