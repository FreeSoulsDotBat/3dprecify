# Token-spend ledger

Owner rule (2026-07-10): every AI operation that costs real tokens — semantic extraction,
subagent fan-outs, multi-agent workflows — appends a row here. Estimate **before** running,
record the actual **after**, and write down the lesson when they diverge. graphify runs also
mirror into `graphify-out/cost.json` (local, gitignored — this file is the durable record).

| Date | Operation | Estimate | Actual | Lesson |
| --- | --- | --- | --- | --- |
| 2026-07-10 | graphify initial full build — semantic extraction of docs + community labeling | — | 107,249 in / 9,827 out | Baseline; code was AST-only (0 tokens). Semantic cache was NOT persisted by this run (fixed 2026-07-10). |
| 2026-07-10 | graphify item-6 ingestion — 26 curated docs (specs/008 + ADRs 0014–0016 + business-rules + CLAUDE.md + 11 agents), 2 subagents | ~10–15k | 216,831 (aggregate in+out) | Word-count estimates undershoot ~15×: subagent read+reason+write overhead dominates, not the doc text. Estimate ≈ corpus tokens × 10 for subagent extraction, or use `GEMINI_API_KEY` to move it off-session. |
| 2026-07-10 | 008 T001 designer-ux UX handoff — 1 subagent reads spec/contracts/quickstart + E2 `ux-catalog.md` pattern, writes `ux-bom.md` | ~100–150k aggregate (corpus ×10 rule from item-6 lesson) | 111,338 (25 tool uses, ~8min) | Corpus ×10 rule held. Handoff surfaced 2 owner flags (5th nav tab; lapsed-at-/bom split) — carried to PR evidence. |
| 2026-07-11 | 008 T005 pre-gate: arquiteto decision — where the shared piece-form lives (C1: page-composition vs entities/piece move vs page-hosted composer) | ~80–120k aggregate | 121,952 (23 tool uses, ~10min) | Slightly over estimate (agent also root-caused a transient hook block). Decision: page/widget-hosted composition (R7 in research.md), 85%. |
| 2026-07-11 | 008 T006b qa-produto visual homologation — browser walk of /bom (390px + desktop, compose/rollup/teaser) + screenshots | ~150–250k aggregate (browser tool calls dominate) | 162,607 (128 tool uses, ~21min) | Within estimate. Verdict PASS-with-nits; top nit (skippedLines caption unreachable via form) answered with a component-level contract test + PR note. |
| 2026-07-11 | 008 K-amendment plan delta: arquiteto decisions R8 (route naming, atomic materialization txn + wire shape, FR-310 relaxation seam, manual-product state, nav icon, PR slicing) + ADR draft | ~100–140k aggregate (R7 was 122k, similar shape + one ADR) | 128,197 (34 tool uses, ~11min) | On estimate. R8 + ADR-0017 (Proposed) + one data-model premise change caught by reading code (bom_lines born with product_id). |
| 2026-07-11 | 008 T009 pre-push extensive review (owner-conditioned): 6-dimension multi-agent workflow (arch/clean-code/bugs/decisions/security/tests) + adversarial verify of blocker/major findings | ~600–900k aggregate (6 finders ×~90k + refuter pairs) | 1,042,131 (10 agents, 319 tool uses, ~14min + paused/resumed via workflow cache) | ~15% over: finders averaged ~150k (whole-branch mandates read far more than a diff review). Yield: 2 CONFIRMED majors (guard tear-down; prefill coverage) + 12 minors — the fan-out paid for itself. |
| 2026-07-12 | 008 T015b/T017b qa-produto visual homologation (PR-B) — browser walk of the kit save round-trip, Kits tab, K3 attention state, manage + the lapse read-only state (390px + desktop) + screenshots | ~150–250k aggregate (browser tool calls dominate; T006b was 163k, this walk is wider — save/reopen/lapse/duplicate) | 190,141 (121 tool uses, ~25min) | Within estimate. Verdict PASS-with-nits; found 2 real blockers a green gate could not (a born-manual product claiming a deletion that never happened; a second Salvar filing a duplicate kit) plus the dev-DB drift below. Visual homologation keeps paying for itself on honesty bugs — the exact class tests do not assert. Lesson: a mid-run behavior change needs a SendMessage to the agent, or it homologates a build that no longer exists.
| 2026-07-12 | 5-agent review of the last 5 PRs (#12–#16, `e655504..HEAD`, 163 files / +16k lines): architecture+structure · security · tests/correctness · UX-UI conformance · clean-code+bugs. Owner-requested before the PR-B merge. | ~600–900k aggregate (5 finders × ~120–180k) | 668,258 (5 agents, 222 tool uses; 3 ran testcontainers Postgres) | On estimate. Yield: 1 BLOCKER found by 4 of 5 independently + reproduced (soft-deleted product ⇒ kit unsavable + dishonest GET — the data-model had flagged it 'Resolve in tasks'); it survived a green gate + 100/100 e2e + a visual homologation. Fan-out earned itself: convergence across independent lenses is the signal a single reviewer cannot produce. Also surfaced that 'gate coverage 100%' is pricing-core-only — stop quoting it as app-wide.
| 2026-07-12 | 008 PR-B review-fix VISUAL HOMOLOGATION (qa-produto) — browser walk of the review-round FE deltas (honest no-price headline + excluded note, post-create clobber fix, fee-offline banner + retry, "Ver meus kits" empty-composer link, sticky bottom summary, per-line aria/glyph) at 390px + desktop. Owner: coverage gate is ONE MORE homologation step, not a replacement — so both run. | ~120–180k aggregate (browser tool calls dominate; narrower than the 190k full PR-B walk but still full-stack + a real save for the clobber check) | 186,328 (103 tool uses, ~16min; Playwright-driven Chromium — no browser MCP registered, so the same @playwright/test the e2e specs use) | Slightly over estimate (real premium loop: signup → grant CLI → save round-trip, ×2 viewports). PASS on 6/7 + PASS-WITH-NITS on the sticky bar (pins the FULL total+channel-rollup, ~67% of viewport with channels present — spec §1.7/G4 met, but a compact heroes-only bar reads better). Zero honesty defects. |
| 2026-07-12 | 008 sticky-bar re-verify (qa-produto, resumed) — after compacting the sticky to heroes-only + channel rollup scrolling, a FOCUSED visual re-check at 390px + desktop (stack setup + homolog DB already worked out in the walk above, so only rebuild + confirm). | ~60–120k (fresh spawn — prior agent transcript gone, so re-does stack setup; homolog DB persists) | 104,876 (67 tool uses, ~7min) | PASS. Compacted bar measured 274px-30.5pct (desktop) and 286px-33.9pct (mobile), down from ~67pct-73pct; rollup now position:static (scrolls above), no Salvar overlap, 10/10 automated assertions. Prior agent transcript had been GCed so a fresh spawn re-did setup. |
| 2026-07-12 | 008 PR-C planning (2 specialists, parallel): arquiteto — reconcile the D6 read-time-degrade approach shipped in PR-B vs the planned T020 eager-delete-capture (ADR-0017 addendum + data-model §D6/quickstart §5 notes, Principle VIII); designer-ux — the degraded kit-line indicator reconciled with the F1/K3 indistinguishability lesson. Owner chose the specialist route. | ~160–280k aggregate (prior arquiteto runs ~122–128k each; designer-ux similar; both read-only analysis over boms.py + specs + E2 degraded surface) | 175,332 (arquiteto 80,250 / 14 tool uses; designer-ux 95,082 / 13) | On estimate (low end). arquiteto: ADOPT read-time-degrade (88%) — found the planned D6 mechanism (ON DELETE SET NULL) was never reachable since delete_product is a soft-delete; paste-ready ADR-0017 addendum + data-model/quickstart/tasks reconciliation + 5 risk-pin tests. designer-ux: a degraded kit line gets a calm MUTED CAPTION reusing manualValuesKept (not an Alert, no removal claim, keep "(avulsa)"), reconciled with F1/K3 (~80% on show-vs-nothing, ~90% on caption+copy). |
| 2026-07-12 | 008 PR-C qa-produto visual homologation (degraded-reopen) — save a kit referencing a product, delete the product, reopen the kit; verify the calm "(avulsa)" label + valores-mantidos muted caption, the F1 honesty guard (no "removido/excluído/deletado" anywhere), still-priceable + re-saveable, at 390px + desktop + screenshots. Owner chose qa-produto+e2e now. | ~120–190k aggregate (browser tool calls dominate; narrower than the 190k full PR-B walk — one focused flow — but full-stack setup + a real premium save/delete/reopen loop) | 230,791 (110 tool uses, ~36min) | Over estimate (deep root-cause dig once the flow failed). **Verdict FAIL — found a real honesty blocker a green gate + 100/100 e2e both missed:** on the common flow (delete product → reopen kit) the deleted product renders as a LIVE catalog reference (name + price), as if nothing happened — the *inverse* honesty bug (F1-guard still PASSES; no removal claim). Root cause is NOT the T021 component (correct) but the PR-B freshness/hydration seam: `useDeleteProduct` invalidates only the products query (never kits), and the composer hydrates once per kit id and locks on the first stale-cache paint. Lesson reconfirmed: visual homologation keeps catching the honesty-bug class unit/e2e cannot — a correct component starved of correct data still lies. |
| 2026-07-12 | **E4 kickoff — product-owner scope modeling** (1 subagent, read-only + one write): shape the E4 epic ("History + reproducible snapshots + export", export = premium) into vision · user stories · acceptance criteria · scope boundaries · open questions, feeding `/speckit-specify`. Writes `docs/product/e4-scope-brief.md`. | ~100–160k aggregate (prior single-specialist analysis runs: designer-ux handoff 111k, arquiteto 80–128k; this one reads wider — business-rules + constitution + E2/E3 specs as pattern + GRAPH_REPORT — but does no code deep-dive) | 91,786 (16 tool uses, ~6min) | Under estimate — an epic-scoping pass is CHEAPER than the per-decision specialist runs it precedes (no code deep-dive, no browser). Yield: the master rule that resolves the E2/E3-vs-E4 tension (a snapshot CONTAINS its values, never references them ⇒ it cannot degrade) + 13 owner questions, incl. one real gap nobody had noticed: E1–E3 model no seller identity, so a quote PDF would print anonymous. |
| 2026-07-12 | **E4 `/speckit-plan` Phase-0 architecture round (2 specialists, parallel)**: arquiteto — the **offline write queue** (FR-527/529: local durability, exactly-once sync, honest pending state, entitlement checked AT SYNC; the product's FIRST offline write) + export render side (client vs server → offline export + gate enforceability, ADR-0015 precedent) + server-side immutability (FR-504) + confirm pricing-core untouched vs ADR-0008; dev-estrutura-de-dados — the frozen-snapshot persistence model (payload shape JSONB vs typed columns, migration `0003`, DB-level immutability, indices for label search + date-range filter, kit lines + per-channel rollup in the payload). | ~200–290k aggregate (prior arquiteto rounds 80–128k each, but the offline queue is new machinery — likely web research on sync patterns; dev-estrutura-de-dados comparable to prior schema rounds) | 283,969 (arquiteto 153,268 / 36 tool uses, ~15min · dev-estrutura-de-dados 130,701 / 16, ~11min) | On estimate (high end). **Convergence was the payoff**: both agents, independently, reached the same non-obvious conclusion — snapshot provenance must carry **NO foreign key** (`ON DELETE SET NULL` would *write to the immutable row*; RESTRICT would make products undeletable; CASCADE would delete history) — inverting the E2/E3 instinct. Also caught two things the main loop had written and got wrong: (a) FR-528's "no server received-at is stored" is **literally unachievable** (a server `uuid7` PK already embeds a ms timestamp), and (b) money leaves in JSONB must be decimal **strings** — Postgres keeps `numeric`, but `json.loads`/`JSON.parse` hand back **floats**, so the precision loss is in the serializer, silently, app-side. Lesson: a two-specialist parallel round on a *new-machinery* epic pays for itself; the estimate held because scope was split cleanly (arquiteto = mechanism, data-model = shape) with an explicit "state what you need from the other" instruction. |
| 2026-07-13 | **E4 T001 designer-ux UX handoff** (1 subagent, non-blocking, parallel with PR-A): the Histórico list + snapshot detail (renders STORED values) + the **pending/blocked sync states** (the honesty surface for the product's first offline write) + the sign-out-with-queue dialog + the US5 teaser. Writes `specs/009-e4-history-snapshots-export/ux-history.md`. | ~90–140k (prior handoffs: 008 T001 designer-ux 111k; the E3 PR-C designer-ux round 95k — this one is wider: 5 surfaces incl. a novel sync-state vocabulary the product has never had) | 159,836 (24 tool uses, ~9min) | **Over estimate (~+15%)** — a NEW vocabulary costs more than a new screen: the agent had to read the whole honesty lineage (ux-decisions, E2/E3 teasers, the F1/K3 lesson) before it could name a state the product has never had. Lesson: budget a design round by *how much precedent it must reconcile*, not by surface count. Yield included one thing no reviewer had caught — **sign-out has TWO entry points** (`top-bar` + `conta-page`), so guarding only one would let unsynced records vanish silently, which ADR-0018 §10 forbids. It also claimed (95%) an FR-014 violation in my ADR-0018 draft ("[Cancelar]" is banned copy) — **I checked, and it was WRONG**: FR-014 bans asserting undecided commercial facts (no subscription-cancellation POLICY before E6), not a Cancelar button. Second lesson: a specialist's confident citation still gets verified — this one would have silently reworded shipped copy on a false premise. |
| 2026-07-12 | 008 PR-C fix + e2e verification (main-loop, no subagent): implement fix(a) products+kits invalidation & fix(b) kitSignature content re-hydration, backend/unit/e2e tests, then a long diagnosis of an apparent chromium-only "flaky" degrade e2e. | n/a (main-loop session — not a fan-out; ~15 e2e rebuild/runs + 3 Playwright trace extractions dominated the spend, NOT model tokens) | ~15 full e2e builds (~1min each) + trace parsing | **The "flaky" was an ENVIRONMENT trap, not a bug:** an orphaned `vite preview` on :4173 (from an interrupted run) let Playwright `reuseExistingServer` serve a FROZEN pre-fix bundle — every "fix doesn't work"/"flaky" measurement was old code (incl. the pre-summary "openedSig fix failed" call). Backend :8100 rebuilt each run, so only the front froze — the tell was network showing correct `degraded:true` responses while the UI ran old JS. After killing the orphan: 10/10 clean. Lesson → memory `e2e-orphaned-preview-server` + homologation-t021 addendum. **Rule: before diagnosing any "flaky e2e", check `Get-NetTCPConnection -LocalPort 4173` for a listener and kill it so the run rebuilds.** Hours of token spend would have been saved by that one check first. |
| 2026-07-13 | **E4 T016 qa-produto visual homologation (PR-A)** — the offline → sync → reopen walk at 390px + desktop: record a snapshot online (Sheet: rótulo · validade · basis) → record OFFLINE (honest pending, never "salvo") → reconnect → auto-drain → reopen the frozen detail; plus the sign-out-with-queue guard, the Histórico list/date/basis honesty, the free/signed-out teaser (NO sample entry), and the F1 guard (no removid/excluíd/deletad on any snapshot surface). | ~80–120k aggregate (browser tool calls dominate; prior qa-produto walks: 163k full-surface, 104k focused re-verify, 231k when it hit a real blocker and dug) | 251,039 (111 tool uses, ~37min) | **Over estimate (~2×) — and worth every token: verdict FAIL with TWO blockers a green gate (517 tests) could not see.** **B1:** the record mutation inherited TanStack's default `networkMode: "online"`, which **PAUSES** a mutation while the browser is offline — so `mutationFn` never ran, so `enqueueSnapshot` never ran, so the seller tapped Salvar at the feira, nothing happened, and a reload lost the quote forever. **The offline queue was unreachable offline.** ADR-0018 had explicitly REJECTED paused mutations as the strategy — we inherited them anyway, *by omission*. **B2:** `drainOutbox` did a read-modify-write around a network call, so a record queued mid-POST was ERASED by the write-back — while the UI said "Pendente neste dispositivo". A **false pending**: the DB's unique key prevents duplicates, not a record that is never POSTed. ADR-0018 §6 specified `navigator.locks`; grep found zero. **Lesson: my estimate was anchored on the *happy* walk. Budget a homologation by the walk it would take to DISPROVE the feature, not to demo it** — the two prior qa-produto runs that found real blockers (231k, 190k) both blew past the demo-shaped estimate the same way. And the standing lesson holds again: unit tests mock the very layer where both bugs lived. |
| 2026-07-13 | **E4 T016 RE-VERIFICAÇÃO (qa-produto, resumido via SendMessage)** — depois das correções B1 (`networkMode: "always"`) e B2 (`withOutboxLock` + drain por-entrada): re-testar SÓ a gravação offline real (`context.setOffline`), o ciclo duro (offline → reload → reconectar), a corrida (POST em voo + gravação concorrente) e o nit do banner. Stack rebuildada + CORS real para `:4600`. | ~60–120k (re-verify focado; o precedente é a re-verify de 104k do E3 — mas esta reproduz duas corridas com timing) | 280,250 (21 tool uses, ~8min) | **Acima do estimado, e de novo valeu.** Veredicto **PASS-WITH-NITS**: B1 e B2 confirmados corrigidos com prova (conservação de IDs: fila ∪ banco = todos os `clientSnapshotId` cunhados; **17 snapshots / 17 ids distintos, zero duplicatas**). Achou ainda um nit novo — **offline o logo da top-bar quebrava** (`vite.config.ts` `includeAssets` não precacheava `brand/logo/*.svg`): gap PRÉ-EXISTENTE do app-shell que só ficou visível porque o E4 *criou* a tela offline onde ele aparece — e era a primeira coisa que o vendedor veria lá. **Lição: `SendMessage` para o mesmo agente custa quase o mesmo que um spawn novo** (o transcript é reconstruído), mas mantém o critério de julgamento e as contas do ambiente — vale pela CONTINUIDADE, não pela economia. |
| 2026-07-13 | **E4 PR-A revisão multi-agente pré-merge (owner-requested)** — workflow de 6 dimensões sobre o diff `develop...HEAD` (PR #18, ~25 arquivos de fonte + migração 0003 + endpoint + contrato): arquitetura/ADRs · estrutura/FSD-Lite · qualidade+bugs · decisões de negócio (spec FR + Q1–Q15 + regra das duas prateleiras) · segurança/entitlement (Princípio IV) · testes (Princípio III, o que os mocks escondem). Cada achado BLOCKER/MAJOR passa por 3 refutadores adversariais de lentes distintas; MINOR por 1. Fecha com um crítico de completude. | ~600–900k agregado (precedentes: workflow de 6 dimensões no 008 T009 = 1,042k com mandato de *branch inteira*; revisão de 5 PRs = 668k. Aqui o mandato é o DIFF, que foi exatamente a lição do 008 — "finders averaged ~150k because whole-branch mandates read far more than a diff review") | **4,867,561** (112 agentes, 1.106 tool uses, ~25min) — **5,4× a estimativa**; esgotou o limite da sessão e 53 dos 112 agentes morreram | **Duas lições, e a segunda é a séria.** (1) **A conta do fan-out não é o custo dos finders — é o produto `achados × refutadores`.** Estimei pelos 6 finders (~150k cada ≈ 900k) e esqueci que 41 achados × até 3 refutadores = **105 agentes de verificação**, que passaram a ser ~85% do gasto. *Regra nova: estimar um workflow de review como `finders × custo_finder + (achados_esperados × refutadores × custo_verificador)`, e **capar** o fan-out de verificação (top-N por severidade) antes de rodar, não depois.* (2) **O pós-processamento que eu escrevi transformou a falha de infraestrutura em mentira silenciosa:** `survives = v.length > 0 && refutados < maioria` fazia um achado cujos refutadores TODOS morreram cair no balde "derrubado" — enterrado como se tivesse sido refutado, sem nunca ter sido lido. 19 dos 23 "derrubados" eram isso; só 4 foram refutação de verdade. Um veredicto AUSENTE não é um veredicto NEGATIVO. Escrevi na ferramenta de revisão exatamente a mentira por omissão que a revisão existe para caçar. Script corrigido para três estados (CONFIRMADO / REFUTADO / SEM_VEREDICTO). |
| 2026-07-15 | **E4 PR-A review-fix Phase 6 (verificação cruzada + re-homologação)** — main-loop: `gate:all` (fe EXIT 0 · be 205) + drift-guard idempotente + os 6 e2e de histórico (B1/B2/B3 na camada real, `E2E_PREVIEW_PORT=4600`) + suíte e2e completa (107 passed, 1 flake E2 que passa 6/6 isolado). Depois **qa-produto** re-homologa a walk offline→pendente→reload→sync→reopen a 390px+desktop com emulação que alcança o `onlineManager`. | ~200–280k (precedentes qa-produto: T016 251k, PR-C 231k, PR-B 190k) | **~258k qa-produto** (82 tool uses, ~23min) + main-loop (≈15 builds e2e) | **PASS, 2 nits, nenhum blocker.** A emulação correta (context.setOffline + evento `offline`) tornou o antigo falso-PASS do B1 num PASS real — prova: drenou sozinho no evento `online`, lista final com exatamente 1 card. **Lição operacional confirmada 2×:** (a) uma asserção de e2e que o agente escreveu-sem-rodar estava errada (esperava redirect a /sign-in; o app corretamente fica anônimo em /calcular) — os specs de um agente PRECISAM rodar de verdade antes de valer; (b) o flake de e2e sob 6 workers (catalog T025) passa 6/6 isolado — reconfirma "antes de chamar regressão, re-rode isolado". qa recomendou promover os caminhos gravar-kit (C1) e render-de-canal (M11) a e2e permanente. |
| 2026-07-15 | **E4 PR-A review-fix implementação (delegada, 2 ondas)** — main-loop fez os irreversíveis do cliente (I1 freezer recursivo, I2 `catalogVersion` no root, C1 kit por basis; 15/15 vitest verde). Depois delega: **Onda 1 paralela (dirs disjuntos)** — FRONTEND (`apps/web`: B1/C4·B2·B3·C2·C5·C6·M1·M6·M7·M8·M10·M11·M5-parcial·minors·teste C1·e2e M13) ∥ DADOS (`backend`: CHECKs da migration 0003 C7/2b/schema-bind/3-perdidos + testes C8/M12 + reconciliar data-model.md); **Onda 2** — BACKEND (`history.py`: M2/VR-502·M4·M3 keyset + regen contrato), após DADOS por compartilhar `test_history.py`. Contrato do envelope travado (Opção A) antes de delegar. | ~550–900k agregado (impl. com test-first + rodadas de vitest/pytest; precedentes: dev-estrutura ~130k analítico, mas impl. roda testes N vezes) | **687,153** (DADOS 168,862 · FRONTEND 327,993 · BACKEND 190,298; + main-loop nos irreversíveis do cliente e na verificação cruzada) | Dentro do estimado. Resultado: 31 achados + o gap M3-cliente resolvidos, `gate:all` verde (fe EXIT 0 · be 205 passed · cov 85%) + drift-guard idempotente. **Lição: um achado que atravessa dois domínios cai no vão se você não atribuir AS DUAS metades explicitamente.** O M3 tinha metade-servidor (keyset) e metade-cliente (auto-seguir `nextCursor`); atribuí só ao backend, e a metade-cliente ficou órfã — o `use-history` parava na 1ª página e o 51º registro não chegava ao dispositivo (o defeito que o M3 existe para matar, reintroduzido). Peguei na verificação cruzada (grep do cursor no cliente), não num teste — reforça que a verificação de integração da main-loop é obrigatória depois de um fan-out por domínio. Também: um agente pode deixar o artefato gerado STALE (o BACKEND regenerou o Orval antes de finalizar o `history.py`); a main-loop DEVE re-rodar a regen + provar idempotência antes de confiar no drift-guard. |
| 2026-07-13 | **E4 PR-A revisão — resume do run `wf_d23b699c-484`** (owner escolheu retomar em vez de eu verificar na main-loop): os 6 finders replayam do cache (custo 0), re-rodam só os 53 agentes que morreram no limite — 52 refutadores dos 19 achados sem veredicto + o crítico de completude. Pós-processamento corrigido (SEM_VEREDICTO deixa de ser enterrado como REFUTADO). | ~2,0–2,7M (53 agentes × ~43k de média medida na rodada 1; os finders, que eram os caros, não re-rodam) | **~1,5M** em dois relances (o 1º morreu com o processo e não deixou registro de custo; o 2º fechou 112/112 com **144,492** — quase tudo replay de cache) | **O resume salvou a operação.** Sem ele, refazer a revisão custaria outros ~4,9M; o cache dos 6 finders (a parte cara) foi replayado de graça e só os verificadores mortos re-rodaram. **Lição operacional: um workflow caro deve ser escrito para ser retomável desde o início** — journal por agente + `(prompt, opts)` estáveis — porque o limite de sessão não é exceção num fan-out grande, é o caso esperado. Resultado final: 41 brutos → **31 confirmados · 10 refutados · 0 sem veredicto** + 6 achados novos do crítico de completude (incl. um BLOCKER que NENHUMA das 6 lentes viu: `KitLines` itemiza a preço de varejo um kit gravado na base atacado). **A refutação adversarial pagou-se**: derrubou 10 achados, entre eles dois que alegavam que a query de entitlement pausa no cold-boot offline — os refutadores foram ler a fonte instalada do `query-core` e provaram que o `onlineManager` não lê `navigator.onLine` no boot. Sem eles, eu teria "consertado" um bug inexistente e mexido em código já homologado. |
| 2026-07-16 | **E4 PR-B revisão multi-agente pré-merge (owner-requested)** — workflow de 6 lentes sobre o **DIFF** `origin/develop...HEAD` (`feature/009-e4-pr-b-us3-us6`, 23 arquivos +2169/−127: US3 origem read-time + US6 filtros/gerência/lapse + e2e): correção/bugs · segurança/entitlement (Princípio IV) · testes (o que os mocks escondem) · decisões/regras (duas prateleiras, SC-502/508, ADR-0019, honestidade F1) · clean-code/FSD-Lite · UX-honestidade. Verificação adversarial **CAPADA** no top-10 blocker/major × 2 refutadores de lentes distintas (lição row PR-A 4,8M: capar o fan-out de verificação ANTES, não depois); 3 estados de veredicto (CONFIRMADO/REFUTADO/SEM_VEREDICTO); crítico de completude cruzado contra os refutados. | ~700k–1,2M agregado (diff review, não branch-inteira — lição 008 T009 "finders ~150k porque mandato de branch inteira lê muito mais que um diff"; verificação capada em ≤20 refutadores) | **1,114,003** (11 agentes: 6 finders + 4 refutadores + 1 crítico; 201 tool uses; ~24,5min) | **A CAPA FUNCIONOU: 1,1M vs os 4,8M descontrolados do PR-A** (a diferença exata foi capar `achados × refutadores` ANTES). Só 2 achados chegaram à verificação (o diff estava limpo) e ambos vieram **CONFIRMADOS 2/2** — sinal forte, zero falso-positivo a refutar. Yield: 2 majors de honestidade que um gate verde não vê — (1) `useSnapshot` trata o `null` autoritativo (servidor diz "apagado") igual a `undefined` (ainda não respondeu) e cai no cache stale ⇒ um snapshot deletado renderiza como vivo; (2) "Recalcular hoje" afirma DELEÇÃO ("a origem não está mais no seu catálogo") quando a origem pode só estar não-resolvível localmente (offline/cache frio) — contradiz a decisão do `origin.ts` de nunca alegar remoção. O **crítico de completude achou o de maior valor**: o copy do diálogo (`originResolves`) e a condição de reprecificação (`fromFrozen`) são DOIS portões independentes, e o sinal honesto `fromFrozen` é descartado no ponto de gravação ⇒ grava valores congelados carimbados como "preço de hoje". Exatamente o defeito que atravessa dois pontos (diálogo + gravação) que nenhuma lente isolada pega — o vão cross-domain que o crítico existe para cobrir. |
| 2026-07-13 | **E4 PR-A — sessão de background DUPLICADA ("tente denovo", sem contexto da sessão anterior)** — sem histórico do que falhara, reconstruí o estado do run `wf_d23b699c-484` a partir do journal + dos 219 transcripts em disco (parsing local, **0 tokens de modelo**), constatei que `resumeFromRunId` é same-session-only e que esta era outra sessão, e rodei **1 agente**: o crítico de completude. | ~100–200k (1 agente; o fan-out inteiro já estava em cache) | **174,849** (32 tool uses, ~11min) + main-loop | **Duas lições, e a primeira é sobre trabalho perdido.** (1) **Rodei em paralelo com outra sessão que já estava fazendo o mesmo** — ela retomou o workflow, escreveu o relatório e preencheu a linha acima do ledger enquanto eu trabalhava. Não há trava entre sessões: *antes de "tentar de novo" um trabalho caro, checar o disco pelo artefato de saída (`review-pr-a.md`) e pela linha do ledger, não só pelo estado do run.* O gasto foi recuperado só porque o crítico caiu num caminho diferente e somou C7/C8 (o trigger é `ENABLE` e não `ENABLE ALWAYS` ⇒ contornável por `session_replication_role=replica`; e o teste do trigger cobre 2 das 13 colunas — apagar a cláusula do `payload` deixa a suíte verde). (2) **Reconstruir um run morto do journal custa 0 tokens e vale muito**: os 41 achados JÁ tinham veredicto — o script morrera antes de *reportar*, não de *verificar*. A estimativa de ~2–2,7M para "re-rodar os 53 mortos" era uma leitura errada do estado. *Ler o journal antes de re-gastar.* (3) O crítico **repetiu um achado já refutado** (a query de entitlement pausando no cold boot) — um crítico de completude não vê o que os refutadores derrubaram, então **o pós-processamento precisa cruzar os achados novos contra os REFUTADOS** antes de apresentá-los, senão o ruído caro volta pela porta dos fundos. Fui ler o `onlineManager` instalado e confirmei: `#online = true` no construtor, nunca lê `navigator.onLine`, só vira offline no evento `offline` da janela. |
| 2026-07-16 | **E4 PR-C T030 qa-produto homologação visual (export + comparação)** — a walk completa da fatia: gravar → exportar o **artefato de verdade** (PDF baixado e OLHADO, não só requisitado): quote padrão com **ZERO linhas de custo** (SC-506), kit **itemizado** (SC-515), o opt-in do detalhamento, o CSV do histórico; mais os três estados desabilitados-com-motivo (offline · pendente · lapse) e o bloco de comparação US7 (rótulos inequívocos, sem "hoje" fabricado). 390px + desktop. Stack completa (emulador auth + pg 5433 + backend 8100 + preview **4600**). | ~230–320k (precedentes qa-produto: T016 251k · re-verify 258k · 008 PR-C 231k · PR-B 190k. Estimo no ALTO da faixa pela lição registrada — *orçar pela walk que DESMENTE a feature, não pela que a demonstra* — e porque esta tem um artefato binário a inspecionar, além de exigir revogar o grant para ver a negação por lapse) | **168,094** (93 tool uses, ~22,7min) | **ABAIXO do estimado (~0,6–0,7×) — a primeira homologação qa-produto deste projeto a ficar abaixo da faixa**, e o motivo é reproduzível: o mandato era **uma fatia** com walk enumerada (6 verificações nomeadas + 2 perguntas de ratificação), não "homologue a superfície". As que estouraram (T016 251k, re-verify 258k, 008 PR-C 231k) tinham mandato aberto **ou** caíram num blocker e cavaram. *Lição: o custo de uma homologação é governado pelo ESCOPO DO MANDATO, não pelo tamanho da feature — e a regra "orçar pela walk que DESMENTE" continua certa sobre o RISCO, mas não é licença para orçar alto quando a walk já está enumerada.* **Veredito PASS-WITH-NITS com 1 defeito real que o gate verde não via: o CSV emitia o instante UTC e DESCARTAVA o offset** — cotação 22:30 BRT vira 01:30Z, então card/detalhe/PDF diziam 13/07 e o CSV dizia **14/07**, com o dia local **irrecuperável** do arquivo (FR-513 "same dates" + SC-511). **Mesma classe de defeito ("data um dia errada") que a homologação do backend já pegara neste épico** — reaparecida na única superfície de export que ninguém tinha renderizado, porque `TestHistoryCsv` assertava label/total/basis/kind e **nunca o valor de `deviceQuotedAt`**. *Lição dupla: (1) um defeito já corrigido volta pela superfície irmã que o teste não cobre — ao consertar uma classe, varrer TODAS as saídas dela; (2) o agente verificou o artefato real (baixou o CSV pelo app) — foi assim que viu.* Ratificou as 8 strings novas e **concordou com o desvio do lapse** (o banner da página já diz a frase que o "painel de reativação" abriria). Honesto sobre o que NÃO alcançou: o caso US7 "catálogo muda ⇒ hoje muda" não tem alavanca na UI (linha de kit não tem seletor de filamento) — ficou provado só em teste de componente, e ele disse isso em vez de arredondar para pass. **⚠ SUPERADO na re-verificação (linha abaixo, 2026-07-16): a alavanca EXISTE** pela rota produto-criado-no-Catálogo (que obriga o vínculo filamento/impressora ⇒ D3 live-reflect); o caso está provado em artefato real. A cláusula sobre a linha de kit segue verdadeira — a generalização para "não tem alavanca" era um erro, e é a parte instrutiva: *um gap honesto pode ser uma generalização errada a partir de um fato verdadeiro.* |
| 2026-07-16 | **E4 PR-C T030 re-homologação focada (qa-produto via SendMessage, mesmo agente)** — fecha os dois pontos que a run anterior deixou abertos: (1) a walk US7 que ela **não alcançou** ("muda o catálogo ⇒ *hoje* muda, o congelado não") — com a rota concreta que ela pode ter perdido: editar o `costPerRoll` do FILAMENTO no Catálogo e usar um snapshot de proveniência PRODUCT (o D3 live-reflect do E2 reprecifica o produto a partir do filamento vivo; ela só tentou pela linha de kit, que de fato não tem seletor); (2) re-verificar no arquivo baixado o **fix do dia no CSV** (a correção entrou DEPOIS que ela olhou — o artefato mudou desde a homologação). | ~90–160k (re-verify focada de 2 itens; precedentes: T030 completa 168k · re-verify PR-A 258k — mas aquela reproduzia duas corridas com timing. **SendMessage e não spawn novo**: pela lição registrada, custa quase o mesmo (o transcript é reconstruído) mas preserva o critério de julgamento E as contas/stack já montadas — vale pela CONTINUIDADE) | **203,511** (127 tool uses, ~8,8min) | **~27% acima do estimado — e a lição vale mais que o estouro: um "não alcancei" honesto NÃO é o mesmo que "não dá para alcançar".** O agente havia concluído que o caso US7 não tinha alavanca na UI. A causa era uma **generalização errada a partir de um fato verdadeiro**: ele leu `boms.py:157` (produto materializado de kit nasce com `filament_id=None`, por isso "needs attention") e estendeu para todo produto. Mas um produto **criado no Catálogo obriga o vínculo** (`produto-page.tsx:173`) ⇒ D3 live-reflect ⇒ a alavanca existe. *O que destravou não foi outro agente nem mais tokens: foram 3 linhas de rota concreta ("tente a proveniência PRODUCT + edite o filamento") somadas à instrução explícita "se ainda não existir, diga de novo e PARE — não fabrique caminho pelo banco".* **Regra: quando um especialista devolve um gap honesto, gastar 2 minutos procurando uma rota alternativa antes de aceitar o gap — mas pedindo a re-tentativa de um jeito que deixe "não dá" continuar sendo uma resposta legítima.** Resultado: **veredito sobe de PASS-WITH-NITS para PASS (confiança 85%→92%)**. Item 1 (fix do dia no CSV) verificado no arquivo baixado com um straddle REAL (browser em `Asia/Dubai`, UTC+4: UTC ainda 16/07 21:18Z, device já 17/07 01:18) — card/detalhe/PDF/CSV as quatro concordam em **17/07**, e o `+04:00` viaja no arquivo (dia local recuperável). O código antigo teria dito 16/07. Item 2 (US7) provado em artefato: filamento 110→220 ⇒ `Cotado em 16/07/2026 R$ 26,48` vs `Hoje R$ 43,80`, e o congelado **não se mexeu** (detalhe ainda mostra Material R$ 11,55 do rolo velho) — a Ficha Técnica deixou de alegar e passou a demonstrar. Ele também **corrigiu a linha anterior deste ledger** por conta própria (ver nota abaixo). |
| 2026-07-16 | **E4 PR-C revisão multi-agente pré-merge (owner-requested)** — workflow de 6 lentes sobre o **DIFF** `origin/develop...HEAD` (PR #20, 29 arquivos +3030/−21: renderer server-side + rotas de export + `apiFetchFile` + a Sheet + o bloco US7 + e2e + ADR-0020): correção/bugs · segurança/entitlement (Princípio IV, FR-515/516) · testes (o que os mocks escondem, Princípio III) · decisões/regras (duas prateleiras, SC-506/508/511/515, ADR-0020 §1 "zero aritmética" vs ADR-0008, honestidade F1) · clean-code/FSD-Lite · UX-honestidade. **Verificação CAPADA no top-10 blocker/major × 2 refutadores de lentes distintas** (lição PR-A 4,87M: o custo é `achados × refutadores`, e capar DEPOIS não existe); minors × 1. **Quatro estados** (CONFIRMADO / REFUTADO só se unânime / CONTESTADO se dividido / SEM_VEREDICTO se os refutadores morreram — um veredicto AUSENTE não é NEGATIVO). Crítico de completude cruzado contra os REFUTADOS (lição da sessão duplicada: o crítico não vê o que foi derrubado e traz o ruído caro de volta). | **~700k–1,2M** pela fórmula da lição PR-A: `6 finders × ~150k + (≈10 achados × 2 refutadores × ~40k) ≈ 900k + 800k de teto`. Diff comparável ao do PR-B (23 arq/+2169 → 1,11M com 11 agentes); este é ~40% maior em linhas mas mais estreito em superfícies novas. **Contexto que deve BAIXAR o yield esperado, não o custo:** este diff já passou por 2 homologações em artefato real (T030 + re-verify, veredito PASS) que acharam 4 defeitos — as lentes vão encontrar menos do que no PR-A/PR-B, e isso é o resultado esperado, não falha do workflow. | **2,133,240** (33 agentes: 6 finders + 26 refutadores + 1 crítico; 689 tool uses; ~26,5min) | **~78% acima do topo da faixa — e a minha premissa estava ERRADA no sentido que importa: 21 achados brutos, ZERO refutados, 4 BLOCKERs reais.** Dois erros de estimativa: (a) previ ~10 achados e vieram 21 (a capa segurou o custo — sem ela seriam ~42 refutadores); (b) orcei o refutador em ~40k e ele custou ~64k porque **foi rodar o reportlab de verdade** para reproduzir — refutador que executa código custa mais e vale mais. **A LIÇÃO CENTRAL, e ela corrige uma crença que eu vinha repetindo nesta sessão:** eu disse às lentes para esperar pouco porque "este diff já passou por 2 homologações em artefato real". **Isso estava errado, e o motivo é a lição:** as homologações abriram o artefato com **dados BENIGNOS** ("Cliente João", sem `otherCosts`, sem quantidade 0, sem peça sem nome). Os 4 blockers são todos **dependentes de dado**: rótulo com `<` ou `&` (o vendedor digita "Peça &lt;2&gt;", "R&amp;D") ⇒ o texto SOME do PDF do cliente ou vira 500; `otherCosts` presente ⇒ o detalhamento **conta o mesmo dinheiro duas vezes** (`admin` É a soma deles — a tela do app omite `admin` justamente por isso, o PDF não); quantidade 0 (estado legal, `CHECK quantity >= 0`, decisão Q1 "não entra neste pedido") ⇒ `int(0 or 1)` imprime **"Qtd. 1"**; peça de kit sem nome ⇒ célula **em branco** no orçamento. **"Olhar o artefato" é necessário mas NÃO suficiente — é preciso olhar com DADO ADVERSARIAL.** Uma homologação com dados bonitos e uma suíte verde concordaram em dizer PASS sobre um renderer que mutila o texto do vendedor. Convergência como sinal: o bug do ReportLab foi achado **independentemente por 3 lentes** (correção, segurança, estrutura) — exatamente o que um revisor único não produz. ⚠️ **Incidente de segurança do próprio workflow:** um refutador com Bash **editou `app/api/export.py` e removeu o filtro `owner_uid`** para testar se algum teste pegava (nenhum pegou — que era o achado). Ele desfez; a árvore ficou limpa e nada vazou para o commit — **eu verifiquei antes de qualquer outra coisa**. Mas *dar Bash com escrita a um refutador é um risco de design: a próxima vez pode não desfazer.* Regra para a próxima revisão: refutadores em worktree isolada (`isolation: 'worktree'`) ou sem ferramenta de escrita. |
