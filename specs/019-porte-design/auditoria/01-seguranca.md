# Auditoria de implementabilidade 019 — parecer 01: segurança (PR-B / PR-D / PR-E)

> Agente `seguranca` (opus), 2026-08-27, só leitura, base `develop`@`a0917c6`. Entrada bruta da síntese em
> `../analise-implementabilidade.md`. Onde falta decisão do dono, apresenta opções (Princípio VIII).

## Condições por task

| task | condição (com a linha do servidor que a garante ou que FALTA) | sev. | texto a acrescentar à task |
| --- | --- | --- | --- |
| **T047** (PR-B) | A prova proposta não prova o que a fatia promete. O gate mora em `backend/app/entitlement/__init__.py:92` (`require_entitlement`) e `:105` (`require_catalog_read`), mas quem o APLICA é o `Depends(...)` de cada rota: `filaments.py:156,188,208` · `printers.py:143,176,197` · `products.py:496,536,554` · `boms.py:589,636,660` · `history.py:311,449,478` · `scenarios.py:532,573,633,656,694` · `export.py:45,91`. Um diff vazio em `app/entitlement` é compatível com uma rota que perdeu o `Depends`. | **BLOQUEIA** | SC-1903 = `git diff develop -- backend/app/entitlement backend/app/api backend/app/models backend/app/services` **vazio** (os 4 caminhos, saída colada na dod-evidence) **+** `pytest backend/tests/test_entitlement_gate.py` verde (incl. a N1 nova). |
| **T036** | `premiumGate` puro é interface, não fronteira. A resposta pode vir do **cache do aparelho** (`entities/user/use-entitlement.ts:98,107` + `entitlement-cache.ts:41-48`): um `active` LEMBRADO produz `active` no gate; o 403 do servidor corrige (`entitlement-cache.ts:19-21`). Decisão vigente, não defeito — tem de estar escrito. | PRECISÃO | Caso extra: `premiumGate` com resposta STALE (de `loadCachedEntitlement`) devolve `active` — por decisão (ADR-0018 §9); a barreira real é o 403 do servidor. `stale` NUNCA promove `none`→`active`. |
| **T037 / T045** | **Defeito real, alcançável**: `features/catalog/catalog-panel.tsx:157-167` faz `await create?.(body)` / `await update?.(...)` e **depois** dispara `toast(copy.savedToast, {tone:"success"})` e `setSheet(null)`. Com `create` AUSENTE (o que a T037 manda), `await undefined` resolve e o app diz **"Salvo!"** para uma escrita que não existiu, fecha o formulário e descarta o digitado. Idem `handleInlineSubmit` (`:182-190`). Classe E6/PR-B (toast que afirma reconhecimento que não houve). | **BLOQUEIA** | A ausência do handler tem de ser ESTRUTURAL: `handleSubmit`/`handleInlineSubmit` **retornam sem toast e sem fechar a gaveta** quando `create`/`update` são `undefined`. Teste: `create` ausente + submit programático ⇒ **zero** `savedToast`, gaveta ABERTA, valores preservados. |
| **T040** | "outbox 0 itens" é grande demais. `useRecordSnapshot` é durable-first e a Calculadora é pública: um grátis que registra por `/calcular` cria entrada legítima que vira `blocked` no 403 (`entities/history/outbox.ts:270-271`). | PRECISÃO | O `outbox` é medido **no fluxo do Catálogo/Kits** (0 entradas); a entrada da Calculadora pública fica fora e, quando existir, é `blocked` (403), nunca `synced`. |
| **T048 / E-5** | Deslogado: `app/router.tsx:115-117` exige auth com `?produto=` (`requireAuth` → `/sign-in` com `redirect`). O inerte de filamento/impressora é in-panel; o de produto passa por aí. Se a fatia mexer no `beforeLoad`, o painel roda deslogado — leituras não disparam (`entities/catalog/use-catalog.ts:98` `enabled: status==="authenticated"`), mas uma chamada solta devolve 401 e `shared/api/transport.ts:180` marca `markSessionExpired()` ⇒ faixa "Entrar de novo" para quem nunca teve sessão (falso alarme A3). | RETRABALHO | O `beforeLoad` de `catalogoRoute` **não muda**. Se mudar, provar por `page.route` que nenhuma requisição sai deslogado E que `session-expiry` continua limpo. |
| **T062 / T071** | Dois bloqueadores na 0008: (a) o índice único parcial nasce sobre `name` **sem limite** (`models/__init__.py:145,181,294,372` = `Text`; `FilamentIn`/`ProductIn` sem `max_length`; `quote_render.py:319-324`: "nothing anywhere bounds a name's length") — btree recusa tupla >2704 bytes ⇒ `DataError` ⇒ **500** em rota de escrita (contra `history.py:135-138`); (b) `CREATE UNIQUE INDEX` **ABORTA** se alguma conta tiver dois vivos com o mesmo nome normalizado — possível hoje (`Filament.__table_args__` `models/__init__.py:133-139` só tem CHECKs). | **BLOQUEIA** | (a) A 0008 **desduplica no backfill** aplicando `(2)`/`(3)` às linhas vivas em conflito ANTES do índice; teste com 3 duplicatas por tabela. (b) Limite do nome = decisão do dono: **(i)** teto 120 como `scenarios.py:84` (`_NAME_MAX_CHARS`) no pydantic + CHECK, **(ii)** indexar `left(name_norm, 200)`, **(iii)** indexar `md5(name_norm)`. |
| **T072** (lote) | Sem teto de lote em nenhuma autoridade (`contracts/api-019.md:20-27`, ADR-0033:110-120) e **não existe rate limiting** (`main.py:101-111` só CORS). Idioma da casa: teto por requisição (`history.py:66` 512 KB, `:81` profundidade 64, `scenarios.py:130`). Sem FK em `subject_id` (data-model.md:21), `items` de 1e6 UUIDs = escrita ilimitada por conta paga. | **BLOQUEIA** | `PriceObservationsIn.items` tem `max_length` explícito (valor com o dono; a tela mostra dezenas) ⇒ **422** acima, com teste. Rate limiting continua inexistente por decisão. |
| **T072** (escopo) | `owner_uid` **sempre** de `claims["uid"]` (`history.py:323`, `export.py:53`); `CamelModel` **não** tem `extra="forbid"` (`errors.py:44-46`) — é por modelo (`filaments.py:49`, `products.py:122`, `scenarios.py:596`, `history.py:196`). | **BLOQUEIA** | Os dois schemas novos declaram `model_config = ConfigDict(extra='forbid')`. `owner_uid` só de `claims['uid']`; `ownerUid`/`accountUid` no corpo/query ⇒ 422. |
| **T072** (corrida) | Retry sob `IntegrityError` como descrito quebra: em SQLAlchemy async a exceção **aborta a transação**; sem `session.begin_nested()` a próxima instrução estoura `InFailedSQLTransaction` → 500. O laço de sufixo precisa de teto. Nenhum `ErrorCode` novo — `errors.py:35-40` já registra o precedente (409 reusa `VALIDATION_ERROR`). | **BLOQUEIA** | Retry dentro de `session.begin_nested()` (SAVEPOINT), **limitado**; esgotado ⇒ 409/422 `ErrorCode.VALIDATION_ERROR`. Teste: 2 criações concorrentes + 1 caso com `(2)` já ocupado. |
| **T072** (kits) | Colisão com a materialização atômica (ADR-0017): `boms.py:428-433` insere `Product` direto e `_dedup_match` (`boms.py:294-309`) casa por nome **EXATO, case-sensitive**. Após o índice, "gancho" com "Gancho" salvo não casa, tenta INSERT e viola `(owner_uid, name_norm)` ⇒ `IntegrityError` **dentro da transação do kit** ⇒ 500. Se `_apply_product` não preencher `name_norm` (NOT NULL), **todo** kit ad-hoc quebra. | **BLOQUEIA** | `_materialize`/`_apply_product` passam pelo MESMO escritor de `name_norm`+sufixo; a semântica de `_dedup_match` é decisão do dono: **(i)** casar por `name_norm` (muda o ADR-0017), **(ii)** exato + sufixo no conflito, **(iii)** exato + 422 honesto. Teste: kit ad-hoc "gancho" com "Gancho" salvo ⇒ 2xx. |
| **T073** | **`PATCH /api/v1/products/{id}` NÃO EXISTE** — `products.py:464` GET, `:489` POST, `:514` GET/{id}, `:529` PUT, `:547` DELETE. Rota nova de escrita sem porta declarada = buraco do Princípio IV. | **BLOQUEIA** | A rota nova declara `claims: Annotated[dict, Depends(require_entitlement)]` (ACTIVE-only; `lapsed` não fixa), resolve por `_owned` (`products.py:320` ⇒ 404) e corpo `extra='forbid'` com **apenas** `sellerFixedPrice` (idioma `RenameIn`, `scenarios.py:592-596`). |
| **T064** | Porta por rota: `PUT` → `require_entitlement` (`:92`, `status != "active"` ⇒ 403); `GET` → `require_catalog_read` (`:105`, recusa só `none`). | RETRABALHO | Nomear a porta por rota no teste e asserir `ENTITLEMENT_REQUIRED` no envelope. |
| **T075** | Se o cliente persistir observações em IDB, entram na varredura de privacidade (`app/providers.tsx:49-53`) e na tabela de `app/providers.test.tsx:117-177`, senão a conta A deixa preço para a B. | RETRABALHO | Se houver cache local: chave uid-scoped + `purge…Cache` + entrada na tabela. Se NÃO houver, escrever "sem persistência local". |
| **T070** | Drift-guard dispara por docstring; prettier de `apps/web` reformata o cliente gerado. | PRECISÃO | Regen da RAIZ (`export_openapi` + `pnpm gen:api`), duas vezes, diff vazio na segunda; formatar só da raiz. |
| **T081 / T086** | `CASE` vacuoso já na task (`models/__init__.py:618-626`). Falta: (a) não-vácuo por MUTAÇÃO; (b) extensão dos dois CHECKs de enum (`ck_snapshots_kind_enum` `:584`, `ck_snapshots_headline_basis_enum` `:585-587`) e do `CASE` na MESMA migração; (c) o gatilho de imutabilidade é por COLUNA e agnóstico a `kind` (`0003_e4_snapshots.py:45-62`) ⇒ cobre QUOTE por construção. | **BLOQUEIA** (a+b) | Mutação: remover `WHEN 'PRECO_ORCAMENTO'` ⇒ vermelho. Enums + `CASE` no mesmo `upgrade()`. Registrar que o gatilho de 0003 já cobre `QUOTE`. |
| **T082** | **Espelho incompleto — dinheiro errado no documento do cliente.** A guarda cobre só `history._BASIS_TOTAL_KEY` (`history.py:71-74`). Há **mais duas cópias**: `services/quote_render.py:31` (`_BASIS_TOTAL`) e `:247` (`_BASIS_CAPTION`). E `_basis_key` (`quote_render.py:96-97`) tem **fallback silencioso para `"precoVarejo"`** — o PDF do orçamento imprimiria `precoVarejo` sob a legenda crua "PRECO_ORCAMENTO". | **BLOQUEIA** | Igualdade de conjuntos sobre as TRÊS cópias == `Literal` de `SnapshotIn`; o fallback de `_basis_key` vira erro explícito. |
| **T086** (moeda) | **E4-01 reintroduzido.** `validation.py:52` `_MONEY_POSITION_KEYS = {"totals","breakdown"}`; o payload QUOTE (data-model.md:80-87) põe dinheiro em `discount.{value,amount,grossTotal}`, `costFloor`, `lines[].{unitPrice,subtotal}` — **fora**. Inteiro JSON ali passa (`validation.py:111-117`), congela, e o PDF imprime string sem coagir (`quote_render.py:100-102`) ⇒ **célula VAZIA**. | **BLOQUEIA** | `_MONEY_POSITION_KEYS` ganha as posições de dinheiro do QUOTE; teste com `discount.amount` e `lines[0].unitPrice` inteiros ⇒ **422**, vermelho antes. |
| **T086** (desconto) | O servidor não valida coerência do desconto: `SnapshotIn._validate_frozen_document` (`history.py:131-189`) só amarra `headline_total` ao total da base. Nada impede `grossTotal 100 / amount 90 / precoOrcamento 50`, `mode` inválido, `value 300` em PCT, string negativa (`validation.py:123` usa `abs()`). Tem de ser **422 no pydantic** — 500 é re-POSTado para sempre pelo outbox (`history.py:136-138`; `outbox.ts:272-274`). | RETRABALHO | Espelhar no validador: `mode ∈ {PCT,AMOUNT}` · `PCT: 0 ≤ value ≤ 100` · `AMOUNT: 0 ≤ value ≤ grossTotal` · `amount ≥ 0` · `grossTotal − amount == totals.precoOrcamento` · nenhuma string de dinheiro negativa. |
| **T085 / T088** | `computeQuote` roda no cliente e o servidor não recalcula (ADR-0008; `export.py:5-9`). `netTotal`/`belowCost`/`costFloor` são DECLARAÇÕES do vendedor. Q10 = servidor aceita abaixo do custo. | PRECISÃO | Registrar: o servidor **aceita** `belowCost` (Q10) e não verifica o motor, guardadas as amarras de coerência. |
| **T084 / T089** | Escape já existe (`quote_render.py:112-127` `_xml`, usos `:305,307,310,327,363`). "Válido até" nasce de `quote_validity_days` (CHECK 1..3650 — `models/__init__.py:589-593`). Risco novo: **nome de item** e **rótulo da linha de desconto**. | RETRABALHO | Toda linha nova do PDF com texto do vendedor passa por `_xml` + `Paragraph`; caso adversarial com `<b>` e `&amp;` no nome. |
| **T089** (CSV) | `_CSV_FIELDS` (`quote_render.py:57`) não muda ⇒ QUOTE só como VALOR de `kind`; a aceitação da injeção de fórmula do E4 (`:392`) fica intacta. | PRECISÃO | Coluna nova no CSV **reabre** a decisão do E4 — nesta fatia o CSV não ganha coluna. |
| transversal | `observability.py:71-89` emite só correlationId/service/route/status/latency/errorCode/releaseSha — sem corpo, sem query, sem uid. | PRECISÃO | Nem PR-D nem PR-E acrescentam log por item; nada de dinheiro em path ou query. |

## Tasks NOVAS de segurança (testes vermelhos primeiro)

- N1 [P] [US3] `backend/tests/test_entitlement_gate.py` — **substituir** `test_every_catalog_route_carries_the_gate` (`:198-215`): hoje só varre 4 prefixos (`CATALOG_PREFIXES` `:25-30` — fora: `/history`, `/scenarios`, `/price-observations`) e aceita qualquer das duas portas em qualquer método. Novo: todo `POST/PUT/PATCH/DELETE` sob os prefixos gated carrega `require_entitlement`; todo `GET` carrega uma das duas. Não-vácuo por mutação.
- N2 [P] [US5] `backend/tests/test_price_observations.py` — teto de lote: acima ⇒ **422**; `items: []` ⇒ 200 `{upserted: 0}` sem escrita.
- N3 [P] [US5] `backend/tests/test_price_observations.py` — isolamento: conta B faz `PUT` com `subjectId` de A; A faz `GET` e não vê; `ownerUid` no corpo ⇒ 422.
- N4 [P] [US5] `backend/tests/test_migration_0008.py` — DB semeado com duplicatas vivas nas 4 tabelas + nome de 5.000 chars: `upgrade()` completa e o índice existe.
- N5 [P] [US5] `backend/tests/test_boms.py` — kit com peça ad-hoc "gancho" existindo "Gancho": 2xx, zero 500, atomicidade do ADR-0017 preservada.
- N6 [P] [US6] `backend/tests/test_history.py` — QUOTE com inteiro JSON em `discount.amount`, `costFloor`, `lines[0].unitPrice` ⇒ 422 cada; `grossTotal − amount != totals.precoOrcamento` ⇒ 422.
- N7 [US6] `backend/tests/test_export.py` — QUOTE exportado imprime `payload.totals.precoOrcamento` (não `precoVarejo`) e legenda não-crua; mutação em `_BASIS_TOTAL` ⇒ vermelho.
- N8 [US3] `apps/web/src/features/catalog/catalog-panel.test.tsx` — `create`/`update` ausentes + submit ⇒ sem `savedToast`, gaveta aberta, valores preservados. Vermelho hoje contra `catalog-panel.tsx:157-167`.

## O que já está coberto (arquivo:linha)

- Toda escrita premium tem porta ACTIVE-only (`Depends(require_entitlement)`, `entitlement/__init__.py:92-102`, 403 `ENTITLEMENT_REQUIRED`); leituras `require_catalog_read` (`:105-115`). **PR-B não remove nenhuma proteção de servidor.**
- Sem oráculo de existência: 404 igual para inexistente/apagado/de-outro-dono (`history.py:226-229,249-268` · `products.py:307-337` · `filaments.py:110-132` · `printers.py:98-119` · `boms.py:231-258` · `scenarios.py:197-221`).
- Troca de conta no mesmo aparelho: varredura por uid (`app/providers.tsx:49-53`, provada em `providers.test.tsx:117-177`); caches uid-keyed (`use-catalog.ts:74-94`, `entitlement-cache.ts:24-26,41-48`).
- 401 ≠ 403 no cliente (`transport.ts:26,180`; `outbox.ts:267-274`).
- Imutabilidade cobre QUOTE por construção (`0003_e4_snapshots.py:45-62,192-207`; `models/__init__.py:865-889`); `UNIQUE(owner_uid, client_snapshot_id)` (`:583`).
- Documento já limitado: profundidade 64 (`history.py:81-101`), 512 KB (`:150-151`), andarilho de moeda (`validation.py:68-127`), 422 terminal (`history.py:136-138`).
- PDF escapa texto livre (`quote_render.py:112-127`). Logs sem dinheiro (`observability.py:71-89`). Rate limiting inexistente — o idioma é teto por requisição.

**Veredito**: PR-B libera zero superfície no servidor; seu bloqueador é o toast falso de `catalog-panel.tsx:157-167` e a prova insuficiente da T047. PR-D e PR-E, como escritas, **não passam** (migração 0008 pode abortar e derrubar o kit atômico; `PATCH` sem porta; lote sem teto; QUOTE com dois caminhos de dinheiro errado/em branco no documento do cliente, invisíveis a asserção de texto).
