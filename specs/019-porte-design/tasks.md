# Tasks: 019 — O porte do design (157 superfícies) + as features que o dono incluiu

**Feature**: `019-porte-design` | **Spec**: [spec.md](./spec.md) | **Plan**: [plan.md](./plan.md)
**Autoridade técnica**: [research.md](./research.md) (A–K) · **ADRs**: 0032 (PR-A) · 0031 §Emenda (PR-F) ·
0033 (PR-D) · 0034 (PR-E) — Proposed; o dono flipa cada um no gate da fatia.
**Autoridade de design**: `docs/design/handoff-019/` + pranchetas remotas (Claude Design `a90ed7d4`) —
**copy sempre transcrita verbatim; divergência de um caractere é defeito.**

**Tests são obrigatórios** (Constituição III) — e neste incremento cada teste novo é provado
**não-vacuoso** (vermelho antes do verde; mutação onde a guarda for estrutural), porque o projeto já
pagou três vezes por guarda que passava em tudo.

**Ordem das fatias** (cada uma é um PR autorizado pelo dono, squash em `develop`; homologação do dono
ESPERA a Rodada 1 — cada fatia entrega em **CORREÇÃO DECLARADA**):

| Fatia | Fases | Conteúdo | Depende de | Escalação |
| --- | --- | --- | --- | --- |
| **V0** | 1 | medição — não vira PR | — | — |
| **PR-A** | 2 + 3 + 4 | fundação DS (US1) + marca/foco/texto/vocabulário (US2) | V0 | — |
| **PR-B** | 5 | Premium sem parede (US3) | PR-A | — |
| **PR-C** | 6 | Calculadora (US4) | PR-A | — |
| **PR-D** | 7 | Recálculo do Catálogo (US5) — migração 0008 | PR-A | **opus** (ADR-0022) |
| **PR-E** | 8 | Montar e Enviar (US6) — pricing-core 4.2.0, migração 0009 | PR-A, PR-D | **opus** |
| **PR-F** | 9 | Simulações desktop + D1/D2 (US7) | PR-A | — |
| Polish | 10 | evidência, ledger, ADRs | todas | — |

B, C, D, F são independentes entre si depois de A. **Ordem decidida pelo dono (27/08): B → C → D → F → E.** E é a única tudo-ou-nada e sai por último.

---

## Phase 1: Setup — V0, a medição que dimensiona a PR-A (não vira PR)

- [x] T001 Classificar cada item do handoff §1 (8 primitivos) e §3 (8 adaptações) + itens de marca em **(a) já correto · (b) existe local e sobe · (c) não existe**, com arquivo:linha ou screenshot por item, em `specs/019-porte-design/dod-evidence.md` §V0. Partir das 5 correções já medidas pelo arquiteto (research §"Correções de escopo") — NÃO reconferir o que já tem arquivo:linha; conferir o resto. **Feito 27/08** → dod-evidence §V0 (22 linhas: 10 c · 1 b · 8 a · 1 misto · 2 n/a; o README erra `--tf-warning-deep` — o token real é `--tf-amber-deep`).
- [x] T002 [P] Medir o contraste de `--tf-amber-deep` (`#bd6c0e`) como TEXTO sobre `#ffffff` e sobre `--tf-warning-soft` nos dois temas (WCAG sRGB, com o mesmo método do `a11y-targets-contrast.spec.ts`) e registrar em dod-evidence §V0. **Se < 4,5:1: decisão do dono JÁ TOMADA (27/08) — escurecer só o TEXTO até ≥4,5:1** (ícone/badge/botões seguem no laranja da marca; ADR-0032 §4). **Feito 27/08** → §T002: `#bd6c0e` = 3,95 / 3,46 — REPROVA; `--warning-text` nasce claro `#9a570a` (5,61 / 4,92) · escuro `var(--tf-orange)` (7,94 / 6,08).
- [x] T003 [P] Contar as ocorrências de `canal|canais|Canal|Canais` em `apps/web/src/**` (produto) e `apps/web/tests/**` (teste) separadamente e registrar os dois números em dod-evidence §V0 — substituem os "374" da prancheta e são o tamanho real da US2/T033. **Feito 27/08** → §T003: produto 63 (31 em `messages.pt-br.ts`) · testes 95 (53 no cf-010) · backend 0.
- [x] T004 [P] Registrar a estimativa de tokens do incremento em `docs/token-ledger.md` (uma linha por fatia; real fecha no Polish). **Feito 27/08** — linha "019 V0 + estimativas por fatia" (~4,4M total).
- [x] T005 Subir a stack pelo `quickstart.md` e registrar em `specs/019-porte-design/evidencias/ambiente.md`: /health 200 · /api/v1/entitlement 401 · alembic em `0007 (head)` · porta do emulador usada (a armadilha 9099 está documentada). **Feito 27/08** → [evidencias/ambiente.md](./evidencias/ambiente.md): 200 · 401 · 0007 · emulador em 9500.

**Checkpoint**: a tabela (a)/(b)/(c) existe e é ela — não a contagem de `NOVO` — que abre a Phase 2.

---

## Phase 2: Foundational — as guardas de folha e a transcrição (BLOQUEIA tudo)

- [x] T006 Escrever o teste VERMELHO `apps/web/src/styles/tf-class-uniqueness.test.ts`: varre `apps/web/src/**/*.css`, extrai toda definição `.tf-[a-z0-9_-]+` (seletor simples e composto), falha em nome definido em **dois arquivos**. Previa vermelho por `tf-alert--compact`; **ficou vermelho 27/08 por `tf-grafismo`** (`styles/base.css` × `shared/ui/grafismo.css`, achado novo) — `tf-alert--compact` só tem UMA definição em `src` hoje; a guarda protege a transição da T021. Registrado em dod-evidence §T006.
- [x] T007 [P] Escrever o teste `apps/web/src/styles/prancheta-devices.test.ts`: zero ocorrência de `tf-phone-scroll` e `tf-price--rola` em `apps/web/src/**` E no bundle (`apps/web/dist/**/*.css` após `pnpm build`). Provar não-vácuo inserindo a classe num CSS temporário e vendo vermelho; reverter. **Feito 27/08** — vermelho provado e revertido (§T007).
- [x] T008 [P] Criar `specs/019-porte-design/design/README.md` com a regra de transcrição: cada fatia busca as pranchetas de que precisa via DesignSync (projeto `a90ed7d4`), salva a cópia `.dc.html` em `design/` (prettierignored via `specs/*/design/**`) e transcreve a copy para `messages.pt-br.ts` **byte a byte** — nunca de memória, nunca "melhorada". Listar por fatia quais pranchetas: A → `Primitivos - A Camada de Baixo`, `Shell - O Cromo e a Navegacao`, `Entrada e Bordas` · B → `Premium - O Caminho Sem Parede`, `Catalogo - Os Estados da Lista` · C → `Calculadora - Aviso de Plausibilidade`, `Bloco da Maquina`, `Selo de Procedencia`, `A Conta e os Precos` · D → `Catalogo - Lista e o Recalculo`, `O Item Aberto` · E → `Orcamentos - Montar e Enviar` · F → `Simulacoes - A Estrategia Viva`, `As Escritas Congeladas`.
- [x] T009 Baixar via DesignSync as pranchetas da fatia A (as 3 da T008, tema escuro E claro) para `specs/019-porte-design/design/` e registrar o hash de cada uma em `design/README.md` (cópia congelada = contrato). **Feito 27/08** — 6 arquivos (3 pranchetas × 2 temas), hashes em [design/README.md](./design/README.md).
- [x] T010 Ampliar o vetor compartilhado de normalização de nome: criar `contracts/fixtures/name-norm.json` (casos: acento, maiúscula, `ß`, espaços duplos, trim, NFD/NFC equivalentes) — lido por pytest E vitest nas fases 7. Sem implementação ainda; só o vetor. **Feito 27/08** — 18 casos (inclui `ß`, NFD/NFC, tab, numeral romano).

**Checkpoint**: T006 vermelho (duplicata real), T007 verde, pranchetas da fatia A congeladas.

---

## Phase 3: US1 — A camada de baixo: os oito primitivos + o tom de ATENÇÃO (P1 · PR-A)

**Meta**: os primitivos do handoff §1 no DS, cada um com a medida que o justificou; `--warning-text`;
as adaptações desfeitas; os consertos do produto preservados.
**Teste independente**: cada primitivo renderizado nos dois temas com as medidas (frozen ≥5,67:1 /
≥18,23:1; plist ≥9 itens a 390px; close 44×44 sem alterar altura); T006 verde após a promoção.

### Tests (vermelhos primeiro)

- [x] T011 [P] [US1] `apps/web/src/shared/ui/frozen.test.tsx`: `<Frozen>` renderiza `<fieldset disabled class="tf-frozen">`; não existe prop que desligue o `disabled`; um botão FORA dele continua clicável; a dica (`tf-field__hint`) dentro dele mantém `color` sem `opacity` no wrapper (asserção de estilo computado). **Feito 27/08** (dev-frontend) — 5/5; ver dod-evidence §T011.
- [x] T012 [P] [US1] `apps/web/src/shared/ui/plist.test.tsx` + `table.test.tsx` + `aviso.test.tsx`: estrutura/roles (`list`/`listitem`; `table` com `columnheader`; `aviso` com `role="status"` e botão de dispensa); `Aviso` NÃO é `alert`.
- [x] T013 [P] [US1] `apps/web/src/shared/ui/alert.test.tsx` (casos novos): `compact` aplica `tf-alert--compact`; `action` renderiza `tf-alert__action`; `onDismiss` renderiza `tf-alert__close` com nome acessível; `tone="warning"` aplica `tf-alert--warning`; o alvo do `__close` é 44×44 por pseudo-elemento e a altura do alerta com uma linha NÃO cresce (comparar com o alerta sem `onDismiss`).
- [x] T014 [P] [US1] `apps/web/src/shared/ui/button.test.tsx` + `segmented.test.tsx` + `badge.test.tsx` (casos novos): `width="full"|"half"` → classes; `split` → `tf-segmented--split`; `tone="warning"` no Badge. `tf-badge--success/--danger` NÃO são criados (já existem — asserção de ausência de duplicata via T006).
- [x] T015 [P] [US1] Atualizar `apps/web/src/styles/token-parity.test.ts`: baseline 87 → **88** (`--warning-text` nos dois temas), como mudança revisada; o teste fica vermelho até a T020.
- [x] T016 [US1] `apps/web/tests/e2e/a11y-targets-contrast.spec.ts` (casos novos): contraste MEDIDO de dica e rótulo dentro de um `<Frozen>` nos dois temas (≥5,67:1 e ≥18,23:1 — contra o fundo REAL `--bg-muted`); `tf-plist` a 390px com 12 itens → ≥9 visíveis sem rolar (caixas dentro do viewport). **Feito 27/08** — `tests/e2e/porte-medidas.spec.ts` (marcação da prancheta injetada contra o bundle real; nenhuma tela usa Frozen/Plist antes da PR-B/PR-D): 8/8 chromium+mobile. Limiar asserido = AA 4,5:1 (normativo); os 5,67/18,23 da folha são a medida de um tema — registrada, não asserida.

### Implementation

- [x] T017 [US1] Criar `apps/web/src/shared/ui/frozen.tsx` + `frozen.css` (regra do handoff: esmaecimento nos CONTROLES + `background: var(--bg-muted)` obrigatório; adaptações desfeitas) e exportar em `shared/ui/index.ts`.
- [x] T018 [P] [US1] Criar `apps/web/src/shared/ui/plist.tsx`+`.css`, `table.tsx`+`.css`, `aviso.tsx`+`.css` a partir de `docs/design/handoff-019/tf-components.css` (`grep NOVO`), desfazendo as 8 adaptações do §3 (nome do token `--border-default`, `fixed`, ícone via `TF_ICON_BASE`, componente, sem regra de `text-decoration`, `12vw`, `min-width:0`), e exportar em `index.ts`.
- [x] T019 [P] [US1] Estender `alert.tsx`/`alert.css` (`compact`, `action`, `onDismiss`, `tone="warning"` + `.tf-alert__action` com cor por tom + `.tf-alert__close` com alvo por `::after`), `button.tsx`/`.css` (`width`), `segmented.tsx`/`.css` (`split`), `badge.tsx`/`.css` (`tone="warning"`).
- [x] T020 [US1] Adicionar `--warning-text` em `apps/web/src/styles/tokens/colors.css` nos dois temas (valor = resultado do gate T002: `--tf-amber-deep` se ≥4,5:1, senão o tom que o dono escolher) + `--warning-soft` se faltar; T015 fica verde.
- [x] T021 [US1] **Promover** `tf-alert--compact`: apagar as regras locais de `apps/web/src/features/calculator/shopee-warnings.css` e trocar `shopee-warnings.tsx` para `<Alert compact>`; **e juntar `tf-grafismo` numa casa só** (os 4 declarativos de `styles/base.css:173` vão para `shared/ui/grafismo.css` — o achado da T006); T006 fica verde. Re-medir a seção Shopee a **360px** contra a medida do 016/PR-F (a geometria muda ~8px — research §A) e registrar em dod-evidence. **Feito 27/08** (dod-evidence §T021) — promoção + `tf-grafismo` juntado, guarda 3/3 com dívida vazia; re-medida a 360px: selo 60→**75px** (+15, não ~8; registrado).
- [x] T022 [US1] Rodar `pnpm gate:all` + `a11y-targets-contrast` + `overflow-geometria` (10 larguras) e registrar; os consertos 015/A6 e 016/T018-A1 em `price-hero.css` continuam intocados (diff vazio no arquivo). **Feito 27/08** — gate verde (1808 unit · 474 backend), a11y-targets-contrast + overflow-geometria verdes na rodada completa, `price-hero.css` diff = 0.

**Checkpoint**: 8 primitivos no DS com medida registrada; T006/T007/T015 verdes; nenhuma tela mudou ainda além da Shopee (re-medida).

---

## Phase 4: US2 — Marca, foco, texto e vocabulário (P1 · PR-A)

**Meta**: guardas de marca; **remoção do indicador de foco** (decisão do dono 25/08, reafirmada 27/08 —
exceção explícita ao WCAG 2.4.7); TabBar 10px; grafismos fora de 404/erro; acentos; "1 ano";
canal→marketplace no texto visível.
**Teste independente**: zero "canal" em superfície legível (UI + PDF/CSV); símbolos `channel*` intactos;
zero anel em `:focus-visible` (guarda do inverso); orçamento antigo abre idêntico.

### Tests (vermelhos primeiro)

- [x] T023 [P] [US2] `apps/web/src/widgets/top-bar/top-bar.test.tsx` (caso novo): nenhum `<img>` com `tf-lockup` em rota alguma; o wordmark é `logo-inteira-{white,black}.png` e o símbolo só na top-bar — guarda anti-regressão (já correto: V0(a)). **Feito 27/08** — 3 casos em `top-bar.test.tsx` (wordmark nos 2 temas · zero `tf-lockup` em src · `<Logo>` só em top-bar e sign-in-screen); 6/6.
- [x] T024 [P] [US2] `apps/web/tests/e2e/focus-none.spec.ts` (NOVO — a guarda do INVERSO): para cada tipo de controle (botão, link do menu, aba segmentada, switch, campo, dispensa de alerta), focar por Tab e asserir `outline-style: none` e `box-shadow: none` computados; campo mantém `border-color` de acento. Provar não-vácuo restaurando um anel temporariamente → vermelho → reverter. Substitui o medidor de foco do 018 (`_diag-foco`, que fica obsoleto). **Feita 27/08** — verde nos 2 projetos × 2 larguras na rodada final (350/0). A 1ª rodada achou 2 bugs do spec (switch disabled; `has` não-relativo) e a transição de `border-color` (`expect.poll`). Não-vácuo PROVADO: anel global temporário em `base.css` → 4 vermelhas → revertido (a 1ª prova, em `.tf-btn`, mirava a classe errada — registrado).
- [x] T025 [P] [US2] `apps/web/src/widgets/app-nav/app-nav.test.tsx` (caso novo): rótulo da TabBar a 10px com ≥7px de respiro em cada célula a 390px (medir "Orçamentos", a mais longa) — geometria, não texto. **Feito 27/08** em e2e (`porte-medidas.spec.ts` — jsdom não mede texto): 5 rótulos a 10px, sem transbordo, ≥7px por lado.
- [x] T026 [P] [US2] `apps/web/src/features/calculator/calculator-model.test.ts` (caso novo): `paybackYearsLabel` com `n=1` → "1 ano"; `n=3` → "3 anos". **Escrita 27/08 em `calculator-model.test.ts` contra `PAYBACK_YEAR_OPTIONS` (onde o rótulo nasce), VERMELHA** (`"1 anos"`); verde na T031.
- [x] T027 [P] [US2] `apps/web/src/shared/i18n/messages.test.ts` (NOVO): (a) nenhuma string visível de `messages.pt-br.ts` contém `canal`/`canais` (case-insensitive) exceto uma lista explícita de exceções justificadas (vazia ao fim); (b) `avisoAtacadoAcimaDoVarejo` contém "preço", "só", "é". **Escrita 27/08, VERMELHA por desenho**: 22 folhas visíveis com "canal" (não 31 — a V0 contou substrings, incl. comentários e chaves) + o acento; fica verde na T031/T032.
- [x] T028 [US2] `apps/web/tests/e2e/history-export.spec.ts` (caso novo): um snapshot gerado APÓS a fatia exporta PDF/CSV dizendo "marketplace"; um snapshot criado com payload contendo "canal" (inserido via API, ADR-0019) abre e exporta **idêntico** ao byte. **Resolvido 27/08 por ESTRUTURA** (dod-evidence §T028): o servidor tem 0 strings de vocabulário e o CSV são as linhas armazenadas; a guarda que sobra vive onde o texto do PDF é legível — `backend/tests/test_export.py::test_the_quote_never_says_canal` (3 payloads, artefato completo, não-vácuo por tamanho). O e2e não grepa PDF deflatado (o próprio spec explica por quê).

### Implementation

- [x] T029 [US2] Zerar `:focus-visible` em `button.css`, `card.css` (`--interactive`), `app-nav.css`, `segmented.css`, `switch.css`, `plist.css`, `alert.css` (`__action`, `__close`), `aviso.css`; campos: `field.css` mantém só a borda de acento. Remover a asserção geométrica de anel do 018 onde existir (`apps/web/tests/e2e/a11y-*`), com nota apontando para o T024. **Feito 27/08** (dev-frontend) — 11 arquivos (incl. o reset global `base.css`, `category-picker.css`, `dialog.css`, `toast.css`, achados por grep); `_diag-foco` apagado; nenhuma asserção de anel existia; `--ring`/`--focus-ring` órfãos (mantidos, baseline 88).
- [x] T030 [P] [US2] `apps/web/src/widgets/app-nav/app-nav.css`: rótulo 12→10px + respiro; `apps/web/src/pages/not-found/not-found-page.{tsx,css}` e `pages/error/error-page.{tsx,css}`: remover `Grafismo`, centrar nos dois eixos. **Feito 27/08** (dod-evidence §T030) — a prancheta 24c resolve o item 13 da V0: é TIRAR; 17/17 verdes.
- [x] T031 [P] [US2] `apps/web/src/shared/i18n/messages.pt-br.ts`: acentos em `avisoAtacadoAcimaDoVarejo` (`:179`); `paybackYearsLabel` com singular (`:486`) — e o `machine-cost.ts` que o consome. **Feito 27/08** — o consumidor real é `PAYBACK_YEAR_OPTIONS` em `calculator-schema.ts` (chave nova `paybackYearLabel: "{n} ano"`); acentos aplicados; T026 verde.
- [x] T032 [US2] Vocabulário: trocar "canal/canais" → "marketplace(s)" nos VALORES de `messages.pt-br.ts` (31 medidas) e no texto embutido de `calculator-form.tsx`/`channel-rollup.tsx`/`export` (extraindo para chave quando for hard-code); **símbolos, chaves, rotas, nomes de arquivo intactos**; PDF/CSV (`backend/app/api/history_export.py` ou equivalente — as strings do exportador) incluídos. **Feito 27/08** — 17 folhas em `messages.pt-br.ts`; zero hard-code fora de `messages`; backend 0 (§T028); T027 verde com `EXCECOES` vazia.
- [x] T033 [US2] Atualizar os testes que assertam a string antiga (`cf-010-canais.spec.ts` mantém o NOME; 48 asserções medidas + as demais da T003) — **commit separado** do commit de produto, revisado como mudança de asserção (R3/research §J). **Feito 27/08** — 1 asserção (`bom-page.test.tsx:509`); os 94 restantes já assertavam via `messages` ou são identificadores/metadados; commit separado.
- [x] T034 [US2] Rodar `pnpm gate:all` + e2e completo; screenshots 1:1 nos dois temas de: TabBar 390px, 404, erro, um selo com `tf-alert--compact`, um `<Frozen>`; registrar em `specs/019-porte-design/evidencias/pr-a/`. **Feito 27/08** — e2e 350/0 (a dívida do 018 paga + 2 fix(018)); 10 screenshots 1:1 (a tela de erro não tem rota); gate verde.
- [x] T035 [US2] Abrir o PR-A com o corpo assertando AUSÊNCIAS (zero `tf-lockup`, zero anel, zero "canal" visível, zero classe duplicada) e pedir ao dono o flip do **ADR-0032** no gate. **Feito 27/08** — PR aberto (ver dod-evidence §T035).

**Checkpoint PR-A**: fundação completa; nenhuma outra fatia começa antes do merge.

---

## Phase 5: US3 — Premium sem parede (P1 · PR-B) — MUDANÇA DE COMPORTAMENTO

> Reescrita 2026-08-27 pela auditoria de implementabilidade (`analise-implementabilidade.md`; pareceres em
> `auditoria/`). IDs originais preservados; tasks novas T105+ (IDs vagos T113, T116, T117, T119–T123, T126, T128, T132, T134, T136–T138 = numeração da auditoria absorvida em tasks vizinhas — nenhuma task se perdeu); as 5 decisões da §4 da análise foram tomadas pelo dono em 27/08 (spec §Clarifications) e estão aplicadas; a verificação pós-auditoria (`auditoria/07–09`) foi aplicada na mesma data.

**Meta**: bloquear SÓ no salvar. Vazio didático + formulário inerte; lapsed-com-itens preenchido; o deslogado vê o
MESMO caminho (E-5/FR-1906). As PAREDES são retornos antecipados inteiros, não um `disabled`: `catalogo-page.tsx:105-113`,
`historico-page.tsx:75/:80`, `scenarios-list-sheet.tsx:462`, `bom-page.tsx:116/:136`.
**Teste independente**: conta grátis real e deslogado nas 4 telas: vazio didático → "Adicionar" → formulário em
`<Frozen>` com dica legível → "Salvar" disabled VISÍVEL → zero requisição de escrita, zero entrada de outbox
no fluxo; `git diff develop -- backend/app/{entitlement,api,models,services}` vazio.

### Transcrição (PRIMEIRA — as asserções verbatim dependem dela)

- [x] T042 [US3] Baixar via DesignSync as pranchetas da fatia B (`Premium - O Caminho Sem Parede`, `Catalogo - Os Estados da Lista`, dois temas) para `design/` (hash no `design/README.md`) e transcrever byte a byte para `messages.pt-br.ts`: as 6 frases de vazio, NOMINALMENTE — Filamentos, Impressoras, Produtos, Kits (as 4 abas de `catalogo-page.tsx:29-34`), Orçamentos, Simulações — + "Salvar faz parte do Premium." + "Reative o Premium…" (já existe como `catalogForm.reactivateTitle/reactivateBody`, `:651-652` — conferir byte a byte, não duplicar) + "Fazer um cálculo". Chaves novas em § `catalogo`, § `historico`, § `scenarios`, § `premiumTeaser`. Se a 32h existir no remoto, transcrever também a copy da entrada-com-intenção (o comportamento JÁ existe: `TeaserUpgrade` → `/sign-in?redirect=/conta?assinar=1`). **Feito 28/08** — 4 cópias congeladas (2 pranchetas × 2 temas, hashes em design/README.md; claras derivadas pela transformação da T009); copy transcrita: `catalogo.emptyFilamentsTitle` (perdeu o "ainda"), `catalogo.didatico{Filaments,Printers,Products,Kits}Body`, `historico.didaticoTitle/Body`, `scenarios.didaticoBody`, `premiumTeaser.salvarFazParteDoPremium/fazerUmCalculo`, `billing.reactivateAction`; `reactivateBody` conferido byte a byte (idêntico, não duplicado). **A 32h NÃO existe no remoto** — copy da entrada-com-intenção não transcrita, follow-up declarado. Títulos de impressora/produto/kit/simulação NÃO desenhados no lote 32 — ficam os de hoje (dod-evidence §T042).

### Tests (vermelhos primeiro)

- [x] T036 [P] [US3] `apps/web/src/shared/billing/premium-gate.test.ts` (NOVO): `premiumGate(entitlement, session)` é uma união de CINCO estados — `"active" | "lapsed" | "free-nunca-teve" | "signed-out" | "unknown"` (o "-com-itens" é composição da TELA a partir de `list.items.length`; a função pura não sabe de itens). `{status:"none"}`+autenticado → `free-nunca-teve`; `lapsed` → `lapsed`; `active` → `active`; sessão não autenticada (qualquer entitlement) → `signed-out`; entitlement ausente/erro com sessão → `unknown` (nunca presume). Resposta STALE do cache (`entities/user/entitlement-cache.ts:41-48`) devolve o status lembrado por decisão (ADR-0018 §9) — `stale` NUNCA promove `none`→`active`. Guarda de grafo: o módulo não importa `@/entities` nem `@/features` (asserção sobre o import, molde `tf-class-uniqueness`). **Feito 28/08** — `premium-gate.test.ts` 8 casos (5 estados + stale + guarda de grafo), vermelho por módulo ausente → verde.
- [x] T037 [P] [US3] `apps/web/src/features/catalog/catalog-panel.test.tsx` (casos novos): nos DOIS ramos que o grátis alcança — `catalog-panel.tsx:228-231` (`ENTITLEMENT_REQUIRED`, logado sem premium) e `:240-248` (lista vazia SEM consulta, deslogado — `use-catalog.ts:98` `enabled: status === "authenticated"`) — o painel renderiza o vazio didático (título verbatim de T042) e NÃO o `EmptyState icon="crown"/apiError.entitlementRequired` de hoje (asserção de ausência do antigo; `PremiumTeaser` nunca esteve ali); "Adicionar filamento" (`addButton :192-196`, hoje só nos ramos de lista) existe nos dois e abre o formulário; o formulário está dentro de `<Frozen>`; spy de mutação/fetch com ZERO chamadas ao clicar em tudo; "Salvar" existe, `disabled`, VISÍVEL (hoje é REMOVIDO — `filament-form.tsx:98-102`); "Assinar Premium" é `secondary`, FORA do fieldset (`closest("fieldset[disabled]") === null`) e clicável; "Salvar faz parte do Premium." vem ANTES da linha de botões no DOM. **Feito 28/08** (dev-frontend) — 2 casos (ENTITLEMENT_REQUIRED/free · deslogado) em `catalog-panel.test.tsx`; dod-evidence §T037.
- [x] T038 [US3] (mesmo arquivo da T037 — sequencial) `catalog-panel.test.tsx` (caso lapsed): com `lapsed` e itens no cache, os itens aparecem; a faixa `Alert(catalogo.lapsedTitle/lapsedBody)` de `catalog-panel.tsx:444-448` NÃO renderiza mais; o formulário (Sheet mobile E ficha do mestre-detalhe) abre PREENCHIDO dentro de `<Frozen>` com `catalogForm.reactivateTitle/reactivateBody` (`messages:651-652`, hoje só no ramo `readOnly` do mestre-detalhe). A faixa tem um 2º consumidor: `pages/catalogo/produto-page.tsx:289-291` — a T038 remove as DUAS (`catalog-panel.tsx:444-448` e `produto-page.tsx:289-291`; a ficha passa a dizer `reactivateTitle/reactivateBody` dentro do `<Frozen>`) e APAGA `catalogo.lapsedTitle/lapsedBody` (`:645-647`) por ficarem sem consumidor; a T090 vigia as SEIS restantes. **Feito 28/08** — caso lapsed-com-itens; as DUAS faixas removidas; `catalogo.lapsedTitle/lapsedBody` apagadas (6 asserções de ausência retargetadas para o texto literal antigo).
- [x] T106 [US3] (mesmo arquivo da T037 — sequencial) `catalog-panel.test.tsx` (o toast falso — achado 01): com `create`/`update` AUSENTES, um submit programático ⇒ ZERO `copy.savedToast`, gaveta ABERTA, valores preservados. Vermelho hoje contra `catalog-panel.tsx:157-167` (`await create?.(body)` seguido de `toast(...)` + `setSheet(null)`) e `:182-190`. **Feito 28/08** — 2 camadas (form sem handler + `handleSubmit` retorna sem toast/fechar); o toast falso morreu.
- [x] T039 [US3] `apps/web/src/pages/historico/historico-teaser.test.tsx` (existe — `historico-page.test.tsx` NÃO existe) + `features/scenarios/scenarios-list-sheet.test.tsx` (casos novos): derrubadas as paredes de `historico-page.tsx:75/:80` (`<TeaserShell>`) e `scenarios-list-sheet.tsx:462/:475-476` (`showTeaser`), no grátis (nunca-teve E deslogado — 32f) o vazio mostra a frase verbatim e "Fazer um cálculo" navega para `/calcular`; exatamente UM `TeaserUpgrade` por tela (contado pelo link `messages.billing.subscribeAction`). Depende de T111 (o 403 precisa ser distinguível de erro de rede). Não confundir com o vazio de lista zerada do `active` (`historico-page.tsx:117`). **Feito 28/08** (dev-frontend) — `historico-teaser.test.tsx` reescrito (+2 casos) e `scenarios-list-sheet.test.tsx` (+7, porta sem parede + fallback T112).
- [x] T105 [P] [US3] `apps/web/src/pages/catalogo/catalogo-teaser.test.tsx` — reescrever: hoje asserta EXATAMENTE o que a PR-B reverte (`:74` sem `tablist`, `:76` "nenhum + Adicionar", `:70`/`:111` título do teaser para grátis e deslogado). Commit separado do de produto (research §J). **Feito 28/08** — reescrito por completo (tablist sempre; vazio didático; 1 link; href sign-in no deslogado; só os 3 preços). Commit `fc0e975`, separado do de produto.
- [x] T107 [US3] Guarda de AUSÊNCIA no cliente (molde SC-709/E6, vitest): varre `features/catalog/**` e `pages/{catalogo,bom,historico}/**` e falha se qualquer mutação de escrita (`useCreate*`, `useUpdate*`, `useDelete*`, `useRecordSnapshot`) for CHAMADA sob gate ≠ `active` (spy nos hooks; render de cada painel nos 4 estados não-active). **Feito 28/08** (qa-software) — `pages/premium-write-absence.test.tsx`: 7 superfícies × 4 estados não-active (36 casos), clica em todo botão habilitado + submete todo form; 18 hooks espiados, completude PROVADA por leitura de `entities/**` (regex `export function use(Create|Update|Delete|Record)`); não-vácuo: `active` chama `mutateAsync` 1×. Os 2 achados do agente (`remove` incondicional nos painéis; `onClick` do Salvar de Kits presente) fechados pelo mesmo princípio — handler AUSENTE fora de `active`.
- [x] T040 [US3] `apps/web/tests/e2e/premium-sem-parede.spec.ts` (NOVO, stack real): conta grátis real → Catálogo → vazio didático → "Adicionar filamento" → formulário inerte visível com dica legível → "Salvar" desabilitado; `page.route` contando `POST/PUT/PATCH/DELETE` = 0; o store IndexedDB do outbox (a MESMA chave de `use-history.ts:171 outboxQueryKey`) com 0 entradas NO FLUXO do Catálogo/Kits (a Calculadora pública pode criar entrada legítima que vira `blocked` no 403 — fora do escopo desta asserção). Conta com grant vencido (`grantPremium(email, { expiresAt: passado })` — T114) → itens listados + formulário preenchido inerte + "Reative". Deslogado (E-5): o MESMO vazio + inerte em Filamentos/Impressoras/Kits/Orçamentos/folha de Simulações; na aba Produtos o `beforeLoad` (`app/router.tsx:115-117`, `?produto=`) continua exigindo conta — o clique leva a `/sign-in?redirect=…` com a intenção preservada, asserido nominalmente; "Assinar Premium" visível; clique → `/sign-in?redirect=/conta?assinar=1` → após login cai na oferta; zero escrita, zero outbox. **Feito 28/08** — `premium-sem-parede.spec.ts`, 3 cenários × 2 projetos **18/18** na stack real (0 escritas por `page.route`; 0 entradas no IDB `history:outbox:*`; lapsed via SQL `vencerGrants` — ver §T114 por quê); dod-evidence §T040.
- [x] T041 [US3] `apps/web/tests/e2e/teaser-sweep.spec.ts` (atualizar): SC-006 continua — exatamente um convite por tela grátis, 1920 e 390. A ÂNCORA "a tela renderizou" deixa de ser `premiumTeaser.<F>.title` (`:20-24`) e passa a ser o título do vazio didático; a contagem de links `messages.billing.subscribeAction` continua == 1 **por estado renderizado**: com o formulário inerte ABERTO, o `TeaserUpgrade` do vazio didático NÃO é renderizado e o rodapé do formulário é o único — asserido nos DOIS estados (lista e formulário aberto), a 390 e a 1920 (`teaser-sweep.spec.ts:40`). **Feito 28/08** — âncora = título do vazio didático; contagem nos DOIS estados (lista e formulário aberto) a 390 e 1920; verde nas rodadas 1–2.
- [x] T108 [US3] `apps/web/tests/e2e/catalog.spec.ts:143` (grátis) e `:157-182` (deslogado) ancoram no `premiumTeaser.CATALOG.title` e no slot desabilitado — passam a asseverar o vazio didático + formulário inerte; a asserção "só os três preços praticados" (`:170-172`) continua. **Feito 28/08** — `:61`, `:143` e o bloco do deslogado passam a asseverar vazio + inerte; `/calcular` (teaser do picker) intocado.
- [x] T109 [US3] `apps/web/tests/homologacao/cf-011-048-shell-teasers-rotas.spec.ts:24-26` varre `/catalogo`, `/kits`, `/historico` pelos títulos de teaser — atualizar (a suíte de homologação roda fora do e2e e ficaria vermelha em silêncio). **Feito 28/08** — `TEASERS` ancora nos títulos do vazio didático; CF-045 só acusa quando o CONVITE aparece com o plano em 500.
- [x] T110 [US3] Adotar os testes EXISTENTES que a PR-B vira vermelhos (irmã da T105; commit separado, research §J): `pages/bom/bom-teaser.test.tsx:83-87,106-110,117` (asserta `premiumTeaser.KITS.title` para `none`/deslogado e que "Salvar" NÃO está no DOM no `lapsed` — a T046 inverte os dois), `pages/bom/bom-page.test.tsx:630-644`, `pages/bom/bom-save.test.tsx`, `pages/catalogo/catalogo.test.tsx:185-197`, `features/catalog/products-panel.test.tsx:165-175` (faixa lapsed), `pages/historico/historico.test.tsx`, `features/scenarios/save-scenario-sheet.test.tsx` (conferir) — cada um passa a asseverar o vazio didático / formulário inerte / Salvar visível+disabled. **Feito 28/08** — 12 arquivos de teste adotados (lista em dod-evidence e no commit `fc0e975`), incl. 3 âncoras e2e que nenhuma task citava (`bom.spec`, `kits-save.spec`, `pages-desktop-width.spec`) e `calcular-scenarios.test.tsx`.
- [x] T115 [P] [US3] `backend/tests/test_entitlement_gate.py` — SUBSTITUIR `test_every_catalog_route_carries_the_gate` (`:198-215`): hoje só varre 4 prefixos (`CATALOG_PREFIXES :25-30`; fora: `/history`, `/scenarios`, e a futura `/price-observations`) e aceita qualquer das duas portas em qualquer método. Novo: todo `POST/PUT/PATCH/DELETE` sob prefixo gated carrega `require_entitlement`; todo `GET` carrega uma das duas. Não-vácuo por mutação (trocar a porta de `POST /filaments` ⇒ vermelho). **Feito 28/08** (dev-backend) — **a guarda antiga era VÁCUA** (`app.routes` não achata `include_router` no FastAPI 0.138; varria 0 rotas); `_flatten_routes` cobre 43 rotas por método; mutação em `POST /filaments` ⇒ vermelho, revertida; 10/10 com banco.
- [x] T114 [P] [US3] `apps/web/tests/e2e/history-helpers.ts:41` — `grantPremium(email, { expiresAt? })` repassa `--expires <ISO>` ao `grant_premium.py` (o flag existe: `backend/app/scripts/grant_premium.py:150-156,167`). Não confundir com `forcarExpiracao` (`billing-lifecycle.spec.ts:32-54`, transição ativo→vencido). **Feito 28/08** — `grantPremium(email, { expiresAt? })` → `--expires`; chamada antiga intacta (15 specs).

### Implementation

- [x] T043 [US3] Criar `apps/web/src/shared/billing/premium-gate.ts` (união de 5, pura; assinatura `premiumGate(entitlement: {status:"none"|"lapsed"|"active"} | undefined, session: {status:"loading"|"authenticated"|"unauthenticated"})`) + `shared/billing/vazio-didatico.tsx`, que COMPÕE `<EmptyState>` (`shared/ui/empty-state.tsx:26` já é o `tf-empty`, com `action`) escolhendo a frase pela feature — sem coroa, sem preço, SEM CSS próprio (guarda T006), e carregando o ÚNICO `TeaserUpgrade` da tela. `shared/billing` não tem barrel: import direto do módulo. As 4 telas MANTÊM os próprios `GateChecking`/`GateError`; `premiumGate` devolve `unknown` e a tela decide — a unificação é follow-up declarado (o lote 32 não desenha o estado de verificação). **Feito 28/08** — `premium-gate.ts` (puro, sem imports; `loading` → `unknown`, decisão registrada) + `vazio-didatico.tsx` (compõe `EmptyState`, sem CSS, carrega o único `TeaserUpgrade`; `teaser={false}` com o formulário aberto) + `TeaserUpgrade` ganha `variant/label/price` (o rodapé secundário sem preço da 32b é o MESMO elemento). Divergência 32a × FR-1906 registrada (dod-evidence §T043).
- [x] T044 [US3] `apps/web/src/pages/catalogo/catalogo-page.tsx:105-113`: remover a parede `if (signedOut || status === "none") return <PremiumTeaser…>` INTEIRA (logado E deslogado); `catalogo-page.tsx:100` passa `readOnly={premiumGate(...) !== "active"}` ao `ProdutoPage` (hoje `=== "lapsed"` — um logado `none` em `/catalogo?produto=novo` chega com formulário VIVO; o `?produto=` retorna ANTES da parede, `:96-103`; o `beforeLoad` de `app/router.tsx:115-117` NÃO muda). `features/catalog/catalog-panel.tsx`: o vazio didático ocupa `:228-231` E `:240-248`, com `action={addButton(true)}`; a prop `lapsed?: boolean` (`:88`) vira o estado do `premiumGate` nos 4 painéis (`filaments-panel.tsx:46`, `printers-panel`, `products-panel.tsx:74`, `kits-panel`). **Feito 28/08** (dev-frontend) — parede inteira removida; `gate` nos 4 painéis + `ProdutoPage`; mestre-detalhe 32g (vazio + ficha inerte); 2 correções da revisão do main loop (duplo convite no desktop; lapsed com erro de rede) — dod-evidence §T044.
- [x] T045 [US3] A barreira é ESTRUTURAL na assinatura: `catalog-panel.tsx:69-78` — `renderForm.onSubmit` passa a OPCIONAL e o painel NÃO o passa fora de `active` (`create={gate === "active" ? (b) => create.mutateAsync(b) : undefined}`, idem `update`, em `filaments-panel.tsx`/`printers-panel.tsx`); `handleSubmit`/`handleInlineSubmit` (`:157-167`, `:182-190`) RETORNAM sem toast e sem fechar quando `create`/`update` são `undefined`. `filament-form.tsx` (`:31` prop opcional; `:53` `<form>` sem `onSubmit` quando ausente — nunca handler vazio) / `printer-form.tsx` / `produto-page.tsx` (`:298,:366,:386` os 3 fieldsets viram `<Frozen>`; o rodapé do produto é `:315-322` — Salvar removido por `{!lapsed && …}` — e o aviso de reativação `:324-325`): rodapé NOVO — `<p>{salvarFazParteDoPremium}</p>` ANTES da `div.flex.justify-end` (só não-active) + "Salvar" SEMPRE renderizado `type="button" disabled` fora de `active` (substitui `{!readOnly && …}` de `filament-form.tsx:98-102`) + `Button variant="secondary"` "Assinar/Reativar Premium" FORA do `<Frozen>` (o `TeaserUpgrade` — o mesmo elemento do vazio, nunca um 2º link). No `free-nunca-teve`/`signed-out`: campos VAZIOS; no `lapsed`: valores + "Reative". **Feito 28/08** — `onSubmit` opcional/ausente fora de `active`; `<Frozen>` nos 2 forms + 3 fieldsets do produto; rodapé 32b/32e via `PremiumFooterNote`/`PremiumInviteCta` (`catalog-controls.tsx`); testids `catalog-form-frozen`/`premium-footer-note`/`teaser-upgrade-cta`.
- [x] T111 [US3] `apps/web/src/entities/history/use-history.ts:150-166` — `HistoryListState` ganha `error: ApiError | null` (molde `CatalogListState`, `use-catalog.ts:54`) para a tela distinguir 403 `ENTITLEMENT_REQUIRED` de falha de rede. **Feito 28/08** — `HistoryListState.error` (+ o corolário `ScenarioListState.error`).
- [x] T112 [US3] `features/scenarios/scenarios-list-sheet.tsx` — `ScenarioListBody` (`:478`) ganha o ramo `ENTITLEMENT_REQUIRED` → vazio didático de Simulações (hoje `:462` intercepta antes). **Feito 28/08** — ramo `ENTITLEMENT_REQUIRED` em `ScenarioListBody` → vazio didático.
- [x] T046 [US3] `pages/bom/bom-page.tsx` (DECISÃO 3, 27/08: a parede de `/kits` cai junto e MONTAR SEM SALVAR é permitido): as PAREDES do composer (`:116` deslogado, `:136` `status === "none"` → `<BomGatePanel>`) saem; o composer monta e COMPÕE para todos (adicionar/remover linhas é estado local, não rede); só "Salvar" (`:632`) `disabled={gate !== "active" || saving}` + o convite único (`TeaserUpgrade`); vazio didático de Kits quando não há nada — UMA frase (T042) para `/catalogo?tab=kits` e `/kits`; em `/kits` aparece com o composer sem linhas e sem kit carregado; teste de que compor não dispara nenhuma mutação. `historico-page.tsx:75/:80` e `scenarios-list-sheet.tsx:462`: vazio com "Fazer um cálculo" → `/calcular` (32f). **Feito 28/08** (dev-frontend Kits + Histórico/Simulações) — `BomGatePanel` e a parede de criação do lapsed saem; composer para todos; Salvar visível+disabled com convite secundário; vazio didático de Kits; teste "compor nunca é rede" nos 4 estados; Orçamentos/folha com "Fazer um cálculo". 50/50.
- [x] T047 [US3] Provar SC-1903: `git fetch origin develop` e `git diff origin/develop -- backend/app/entitlement backend/app/api backend/app/models backend/app/services` = VAZIO (os 4 caminhos; um diff vazio só em `entitlement` é compatível com uma rota que perdeu o `Depends` — as portas são aplicadas em `filaments.py:156,188,208` · `printers.py:143,176,197` · `products.py:496,536,554` · `boms.py:589,636,660` · `history.py:311,449,478` · `scenarios.py:532,573,633,656,694` · `export.py:45,91`) + `pytest backend/tests/test_entitlement_gate.py` verde (T115). Gate:all + e2e + `tests/homologacao`; screenshots 1:1 dos 4 estados × 2 temas em `evidencias/pr-b/`. **Feito 28/08** — diff vazio nos 4 caminhos + `test_entitlement_gate.py` 10/10; e2e completo 351/0 (+16/16 na re-rodada dos 2 specs adotados), `tests/homologacao` cf-011-048 atualizado (T109); 32 screenshots 1:1 × 2 temas + `medidas-pr-b.json` em `evidencias/pr-b/`; `pnpm gate:all` verde (ver dod-evidence §T047).
- [x] T048 [US3] E-5: o deslogado passa pelo MESMO caminho (T044/T045 via `premiumGate` `free-nunca-teve` e `signed-out`). A entrada-com-intenção JÁ EXISTE (`TeaserUpgrade` → `/sign-in?redirect=/conta?assinar=1`, provado em `catalogo-teaser.test.tsx:113` e `teaser-upgrade.test.tsx:78-85`); com a 32h desenhada só a COPY entra por cima; sem ela nada muda e o follow-up fica declarado. Abrir o PR-B (CORREÇÃO DECLARADA). **Feito 28/08** — E-5 implementado (deslogado no MESMO caminho, provado no e2e incl. sign-in com intenção → oferta); 32h ausente no remoto (follow-up declarado). Push autorizado pelo dono (ls-remote = HEAD `c5a4ec1`, gate do pre-push verde) e **PR #60 aberto** contra `develop` — CORREÇÃO DECLARADA.

**Checkpoint**: as 4 paredes caíram; zero escrita no grátis provada por route-count, outbox e guarda de ausência; servidor byte-idêntico nos 4 caminhos.

---

## Phase 6: US4 — Comportamentos da calculadora (P2 · PR-C)

**Meta**: plausibilidade (blur/anúncio/Entendi/erro-não-come/dinheiro) sobre o módulo QUE JÁ EXISTE
(`shared/lib/plausibilidade.ts`); máquina (readout nos dois modos/confirmação só quando perde dado/rótulos);
selo compact com dispensa até a fonte mudar; T212 sticky; tarifa 4 casas; SEM bump.

### Transcrição (PRIMEIRA)

- [x] T055 [US4] Baixar as pranchetas da fatia C (`Calculadora - Aviso de Plausibilidade`, `Bloco da Maquina`, `Selo de Procedencia`, `A Conta e os Precos`; dois temas) para `design/` e transcrever: 3 frases da confirmação de modo, "Estimar"/"Ajustar" (SUBSTITUEM `messages.pt-br.ts:490-491` `adjustButton`/`backToEstimateButton`), "falta o valor da máquina", "de {valor} ÷ {horas} h", "Entendi", "Ver fonte", a copy do selo compact — e os VALORES-EXEMPLO de plausibilidade, confrontados com `LIMIARES` (`plausibilidade.ts:48-79`): 850 g NÃO dispara hoje (`printGramsMax = 50_000`, `:67`) — DECISÃO 5 (27/08): o limiar FICA; a copy é transcrita e o número-exemplo da prancheta não é regra (a T049 usa um valor acima de 50.000 g). **Feito 28/08** — 4 pranchetas × 2 temas congeladas (hashes no README); copy transcrita (Entendi, fecho com recusa, Estimar/Ajustar, readout, ressalva, 4 chaves da confirmação, rótulos/ações do selo); `adjustButton`/`backToEstimateButton`/`derivedCaption` apagadas; limiares confrontados (todos os exemplos da prancheta ultrapassam os do produto; sem exemplo de gramas). 4 leituras que divergem das tasks em dod-evidence §T055 (15e = inline, não diálogo).

### Tests (vermelhos primeiro)

- [x] T049 [P] [US4] `apps/web/src/features/calculator/calculator-form.test.tsx` (casos novos sobre `ControlledField`, `calculator-form.tsx:179-197` — NÃO existe `plausibility.tsx`): digitar até o valor sem blur ⇒ zero `Aviso` (hoje dispara no `change` — vermelho por desenho); blur ⇒ `<Aviso>` (`role="status"` já é fixo em `aviso.tsx:23`) com "Entendi"; "Entendi" ⇒ some; outro valor acima do limiar + blur ⇒ volta; erro de validação ⇒ o aviso PERMANECE (REVERSÃO explícita de `calculator-form.tsx:175-178`, comentário reescrito com data e FR-1909); dinheiro "R$ 6.000.061,60" (`fmt` de `plausibilidade.ts:81-83` ganha `style:"currency"` nos campos de dinheiro). Usa os limiares do produto (DECISÃO 5: `printGramsMax` fica em 50.000; o exemplo do teste é um valor acima dele). **Feito 28/08** (dev-frontend) — 5 casos novos (sem-blur · blur+Entendi · outro valor · erro+aviso com fecho trocado · dinheiro 2 casas); vermelho capturado; dod-evidence §T049.
- [x] T050 [P] [US4] `apps/web/src/shared/lib/plausibility-dismiss-store.test.ts` (NOVO — em `shared/lib`, ao lado de `plausibilidade.ts`, porque `widgets/bom-line-editor` renderiza os MESMOS `CalcFieldMeta` e `features/bom` não importa `features/calculator`, `plausibilidade.ts:26-29`, `eslint.config.mjs:83`): chave `${campo}:${valorNormalizado}`; sobrevive a unmount/remount; NÃO usa `localStorage` (só `create`, sem `persist` — não há precedente de store efêmero; `nav-rail-store`/`theme-store` usam persist). **Feito 28/08** — `plausibility-dismiss-store.test.ts` (chave `campo:valor`, sobrevive a remount, sem `localStorage`).
- [x] T051 [P] [US4] `apps/web/src/features/calculator/machine-cost.test.ts` (existe, puro) + `machine-readout.test.tsx` (NOVO, render): readout "de R$ 4.000,00 ÷ 3.600 h" nos modos ritmo E ajustar (hoje o ramo `adjustMode` `calculator-form.tsx:431-490` NÃO tem readout; `:416-418` imprime "≈ R$ 0,00" para `lifetimeHours<=0`, `costPerHour` devolve 0); valor 0 ⇒ ressalva verbatim; "Usar estimativa por ritmo" (`:482-485`, o único que sobrescreve `machineLifetimeHours`) com `detectRitmoMode(currentHours) === null` ⇒ diálogo (3 frases) e NADA descartado antes do "sim"; "Ajustar" (`:425`) nunca pede confirmação; o par vira `<Segmented split>` "Estimar"/"Ajustar". `packages/pricing-core/tests/computeCalculator.test.ts`: `PRICING_MODEL_VERSION` continua "4.1.0" e o vetor 27,55/41,33/35,82 intacto (o lado certo do monorepo — `calculator-model.ts` só consome). **Feito 28/08** — `machine-readout.test.tsx` (readout nos 2 modos, radiogroup, ressalva, sem-divisão-por-zero, confirmação Usar/Manter/já-bate-ritmo); `computeCalculator.test.ts` 28/28, versão 4.1.0 intocada.
- [x] T052 [P] [US4] `apps/web/src/features/calculator/fee-seal.test.tsx` (REESCRITA — hoje testa um `<Badge>`, `fee-seal.tsx:100-107`): `FeeSeal` ganha prop `marketplace`; só as variantes `reference`/`catchAll` (com `source`+`reviewedOn`) renderizam `<Alert compact tone action="Ver fonte" onDismiss>`; `adjusted`/`estimate`/`none` viram `<Alert compact>` SEM dispensa (nada a lembrar); `FixedFeeSourceBadge` (`:117-128`, o que carrega `effectiveDate`) também vira `Alert compact` dispensável pela MESMA chave (recomendação aplicada); dispensar grava `${marketplace}::${source}::${effectiveDate ?? reviewedOn}` em `localStorage` (sem uid); mesma chave ⇒ oculto após reload; data diferente ⇒ VISÍVEL; 50 chaves mais recentes. **Feito 28/08** — reescrito (12 vermelhos contra o Badge); `adjusted/estimate/none` continuam Badge (prancheta 13b ganhou da task); dispensa por `${marketplace}::${source}::${data}`; 102/102.
- [x] T053 [P] [US4] `apps/web/src/shared/ui/number-field.test.tsx` (casos novos): `NumberFieldProps` ganha `precision?: number` (default 2); a perda é no `handleBlur` (`number-field.tsx:64-76`, `formatDecimal(n, 2)` HARDCODED — o comentário `:58-63` "nunca muda o valor semântico" é falso para 4 casas): "0,8734" com `precision=4` sobrevive ao blur; "0" com `currency` reabre "0,00"; regressão do agrupamento de milhar (016/B2). `calculator-model.test.ts`: energia = 0,8734 × kW × h com igualdade numérica. **Feito 28/08** — `number-field.test.tsx` +5 (não-vácuo por mutação: 3/13 vermelhos); energia 0,8734 com igualdade numérica.
- [ ] T054 [US4] `apps/web/tests/e2e/calculator-layout.spec.ts` (caso T212): a 390px, com o formulário rolado ao meio e ao fim, `data-testid="price-summary-sticky"` (novo — grep vazio hoje) está dentro do viewport nos DOIS eixos e não cobre a TabBar (`var(--tabbar-h)`, `styles/tokens/spacing.css:41` — `--tf-tabbar-h` não existe); o toaster (`toast.css:6`) continua acima (research §I). **Espera prancheta** (28/08) — o elemento a medir nasce na prancheta do prompt §1.

### Implementation

- [x] T056 [US4] `shared/lib/plausibility-dismiss-store.ts` + hook `useAvisoDeCampo(nome, valorBruto)` (valor COMPROMETIDO no blur via ref; dispensa por chave) aplicado nos TRÊS pontos de `calculator-form.tsx` (`ControlledField :179`, `printTimeHours :302`, `machineLifetimeHours :455`) — o `<span className="tf-field__aviso">` sai do `hint` e vira `<Aviso action={<Button variant="ghost" size="sm">Entendi</Button>}>` IRMÃO do `Field` (o erro não o substitui mais); `AvisoDeResultado` (`:122-131`) vira `<Aviso>` SEM "Entendi"; `formatBRL` no texto; apagar/reescrever o comentário `:169-178`; `widgets/bom-line-editor` herda (conferir e registrar); `data-testid="aviso-${name}"` sobrevive no novo local. **Feito 28/08** — `use-aviso-de-campo.ts` + store; `<Aviso>` irmão do Field nos 3 pontos + bom-line-editor (via ControlledField); `AvisoDeResultado` com `lines`; `fmtMoney`; comentário `:169-178` reescrito. `.tf-field__aviso` fica (consumidor em `bom-line-card.tsx` — follow-up 14e).
- [x] T118 [P] [US4] `apps/web/src/shared/ui/field.css:166` — corrigir o comentário (aponta `features/calculator/plausibilidade.ts`; é `shared/lib`). **Feito 28/08** — comentário de `field.css` corrigido (`shared/lib`).
- [x] T057 [US4] (depois da T056 — mesmo `calculator-form.tsx`) `features/calculator/calculator-form.tsx`: componente `<MachineCostReadout perHour machineValueNum currentHours>` nos DOIS ramos (`:384-430`, `:431-490`): `perHour>0` → "de {formatBRL(valor)} ÷ {horas} h"; `machineValueNum===0 || currentHours<=0` → ressalva (nunca "R$ 0,00/h"; `derivedCaption` vira obsoleta); diálogo (`shared/ui/dialog`, `variant='center'`) SÓ em "Usar estimativa por ritmo" quando `detectRitmoMode(currentHours) === null`; rótulos "Estimar"/"Ajustar" em `<Segmented split>`. `PRICING_MODEL_VERSION` intocado. **Feito 28/08** — `MachineCostReadout` nos 2 modos; Segmented split (nome do grupo = `fields.machineLifetime`, corrigido na revisão); confirmação INLINE (15e) — divergência da task registrada; 15f (`size=sm` ≥1024) não implementada (corte 1024 ≠ `useIsWide` 1280) — ponto para o dono.
- [x] T058 [P] [US4] `features/calculator/fee-seal.tsx` (Badge→Alert compact, ver T052) + `shared/lib/fee-seal-dismiss-store.ts` (`localStorage`, chave de fonte, 50 recentes). NÃO há CSS local de compact a apagar (`fee-seal.css` só tem `.tf-badge.fee-seal` — a T021 já pagou; após a troca, apagar o override do Badge). Guarda `tf-class-uniqueness` continua verde. **Feito 28/08** — Badge→Alert compact; `sourceUrl` no `FeeSealState` (fee-prefill); Ver fonte = Dialog; `fee-seal-dismiss-store.ts` (localStorage, 50 recentes); `.tf-badge.fee-seal` apagado; `tf-badge--accent/--sm` portados; janela 45 dias no comentário.
- [ ] T059 [P] [US4] T212 — ⚠ o DOM real: NÃO existe `pages/calcular/calcular-page.css`; o layout mora em `features/calculator/calculator-form.css` (`.tf-calc-page:10`, `.tf-calc-grid:33`, `.tf-calc-footer:74`); o corte da página é **1024px** (`:25,:40,:65,:81`), não 1280; o bloco de preço é o `.tf-calc-footer`, ÚLTIMO elemento (`calcular-page.tsx:572`). Antes de codar, registrar em dod-evidence a leitura da prancheta `A Conta e os Precos`: QUAL elemento gruda e a relação com o corte de 1024. Se a leitura implicar MOVER o bloco no DOM mobile, a task **PARA** e vai ao dono (é mudança estrutural fora do `sticky` autorizado — research §I/US12). Aplicar `position: sticky` + `data-testid="price-summary-sticky"`; nenhum ancestral `.tf-calc-page → .tf-calc-grid` com `overflow ≠ visible`. **Decidido 28/08 → espera prancheta** (prompt `019-pr-c-correcoes.md` §1: o preço provisório acompanha o preenchimento e se MESCLA com o cartão final ao chegar ao fim). Leitura registrada em dod-evidence §T059: a prancheta 10 não nomeia elemento fixo; `sticky; top` no último elemento do DOM nunca gruda e `sticky; bottom` colide com o toaster (§I) — cumprir o T212 exige mover/criar o resumo no topo do DOM mobile, a mudança estrutural que esta task manda PARAR. Opções (a)/(b)/(c) no dod; recomendada (a).
- [x] T060 [P] [US4] `shared/ui/number-field.tsx` `precision` (T053) atravessando dois MECANISMOS diferentes: a prop do único `ControlledNumber` (`features/catalog/catalog-controls.tsx:68`, usado por `filament-form.tsx:70-83` e pelo formulário de produto da PR-D — que CONSOME, não recria) e um campo novo `precision?: number` em `CalcFieldMeta` (`calculator-schema.ts:346-360`), lido por `ControlledField` (`calculator-form.tsx:157-163`) ao montar `<NumberField>` em `:201-205`; `precision: 4` só na entrada `tariffPerKwh` (`calculator-schema.ts:388-392`); "0"→"0,00" na hidratação em `features/calculator/scenario-bridge.ts` (NÃO `features/scenarios` — fronteira em `scenarios-list-sheet.tsx:50-51`), fechando o follow-up R5 (máscara de milhar perdida na reabertura). **Feito 28/08** — `ControlledNumber.precision`; `FIELD_PRECISION` + `moneyLeafToPtBr(leaf, precision)` no `scenario-bridge` — **bug real do R5**: a tarifa de 4 casas reabria cortada a 2 (valor, não exibição); vermelho→verde 29/29.
- [ ] T144 [US4] (decisão do dono 28/08 — **espera prancheta**, prompt `docs/design/prompts/019-pr-c-correcoes.md` §2, também em `uploads/` do projeto de design) A confirmação de troca de modo vira DIÁLOGO centrado (`shared/ui/dialog`, molde do diálogo de exclusão): transcrever a prancheta nova (foco inicial, ação do X/Escape = "Manter"), trocar o `<Alert tone="warning" role="alertdialog">` inline por `Dialog`, adotar `machine-readout.test.tsx`; copy já aprovada (`confirmTitle/Body/Use/Keep`) não muda.
- [ ] T145 [US4] (**espera prancheta**, prompt §3) A "marca da seção" `{n} avisos` ao lado do título da seção (14c): conta só os avisos ATIVOS (o store de dispensa já sabe), some em zero, flexão 1/n, convivência com erro e com o segmented da 15f a 1024+; copy "1 aviso"/"{n} avisos" a transcrever.
- [ ] T146 [US4] (**espera prancheta**, prompt §4) A linha de peça de kit com `tf-aviso` (14e): `features/bom/bom-line-card.tsx` troca o parágrafo solto `.tf-field__aviso` pelo `<Aviso>` + "Entendi" via `useAvisoDeCampo("quantity", …)` (ou `avisoDeQuantidade`); apagar `.tf-field__aviso` de `field.css` quando ficar sem consumidor (a T118 não pôde); estados recolhido/catálogo/avulsa/dispensado conforme a prancheta.
- [x] T061 [US4] gate:all + e2e; screenshots 1:1 (aviso, readout nos dois modos, confirmação, selo compact aberto/dispensado, T212 no meio da rolagem) em `evidencias/pr-c/`; abrir o PR-C (sem ADR para flipar; "sem bump" registrado). **Feito 28/08** — gate:all verde (1916 unit · 474 backend), e2e completo 354/0 após 3 âncoras adotadas, 16 screenshots em `evidencias/pr-c/` (2 defeitos achados na imagem e corrigidos: '1200 h/ano' e o link do Ver fonte transbordando). Push autorizado (ls-remote = HEAD `4831395`, gate do pre-push verde) e **PR #61 aberto** contra `develop` — CORREÇÃO DECLARADA; T059/T054/T144–T146 esperam as pranchetas do prompt de correção. Sem ADR para flipar; 'sem bump' registrado.

**Checkpoint**: blur-gated nos 3 pontos + bom-line-editor; readout nos 2 modos; selo dispensável; sticky medido nos 2 eixos; `PRICING_MODEL_VERSION` 4.1.0.

---

## Phase 7: US5 — Recálculo do Catálogo (P2 · PR-D · **OPUS**) — FEATURE NOVA

**Meta**: observação de preço (servidor carimba `observed_at`; cliente escreve em lote pós-render, online-only,
sem cache), preço fixado (número final, só avisa), `name_norm` + índice parcial + sufixo "(2)" em silêncio,
duplicar; lista com PREÇO por item (hoje não há — `products-panel.tsx:20`). Autoridade: ADR-0033, `auditoria/05` (SQL literal).

### Transcrição (PRIMEIRA)

- [x] T074 [US5] Baixar `Catalogo - Lista e o Recalculo` e `Catalogo - O Item Aberto` (dois temas) para `design/` e transcrever US13/US14/US15 verbatim para `messages.pt-br.ts` (chaves ausentes hoje: `catalogo.priceChangedCount`, `catalogo.priceWasLabel`, `catalogo.savedAtLabel`, `catalogo.fixedByYou`, `catalogo.unfix`, `productForm.nameConflict`, `catalogo.duplicateCopySuffix`, e a frase do brief US13 AC5 "O cálculo continua grátis" — `scope-brief.md:284`). **Feito 29/08** — 2 pranchetas × 2 temas (hashes no README); ~45 chaves transcritas (lista em dod-evidence §T074); fixture 18→22; 6 leituras registradas (16c/16d superadas pela PR-B; 60→120; a causa do aumento não é derivável; 17c info × spec ATENÇÃO).

### Tests (vermelhos primeiro)

- [x] T062 [P] [US5] `backend/tests/test_migration_0008.py` (NOVO): asserir o estado pós-upgrade (pode usar `migrated_db`, `conftest.py:74-95`) — `price_observations` com `uq_price_observations_subject (owner_uid, subject_kind, subject_id)`, `ck_price_observations_{subject_kind_enum,observed_price_valid,observed_at_finite,model_version_set}`, FK só para `accounts`; `products.seller_fixed_price` NULL-able + `ck_products_seller_fixed_price_valid` + `seller_fixed_at`; `name_norm TEXT NOT NULL` + índice único PARCIAL `uq_{filaments,printers,products,boms}_owner_name_norm … WHERE deleted_at IS NULL` (`bom_lines` NÃO entra — sem `deleted_at`, models `:396`). Backfill com LEGADO — em container PRÓPRIO (`PostgresContainer`, molde `test_migrations.py:63-76`; a fixture de sessão `migrated_db` já está no head e não semeia legado): `upgrade("0007")` → semear linhas sem `name_norm`, DUAS da mesma conta colidindo no norm ("Gancho"/"gancho ") nas 4 tabelas e um nome de 5.000 chars → `upgrade("0008")` CONCLUI: desempata "(2)", grava `name_norm = left(norm, 200)` (o índice btree cabe; o `name` legado fica INTOCADO; nenhum CHECK de comprimento no banco), nada descartado (R6). Acrescentar `"price_observations"` a `_OWNED_TABLES` (`test_migrations.py:41-53` — o round-trip com container próprio `:70-76` é quem prova o `downgrade()`; a fixture de sessão nunca faz downgrade). **Feito 29/08** — 8 testes, não-vácuo por mutação (índice parcial, CHECK, backfill). dod-evidence §0008.
- [x] T063 [P] [US5] `backend/tests/test_name_norm.py` + `apps/web/src/shared/lib/name-norm.test.ts`: leem `specs/019-porte-design/contracts/fixtures/name-norm.json` (pytest: `Path(__file__).resolve().parents[2] / "specs/…"`; falha explícita se não achar). A classe de espaço é escrita com ESCAPES de codepoint idênticos nos dois idiomas — `[ \t\n\r\f\v\u00a0\u1680\u2000-\u200a\u2028\u2029\u202f\u205f\u3000\ufeff]` — NUNCA os caracteres crus (U+2028/U+2029 são LineTerminator em JS e quebram uma regex literal) e NUNCA `\s`/`.strip()`/`.trim()` (Python casa `\x85` e não `\ufeff`; JS o inverso). **NEL (U+0085) fica FORA da classe por decisão** e o fixture o declara PRESERVADO. O gêmeo TS remove Mn com `/\p{Mn}/gu` (flag `u`) após `normalize("NFD")`, na MESMA ordem do Python. Acrescentar ao fixture: NBSP interno, BOM nas pontas, NEL preservado, `İ` (U+0130). `ß` → lower, não casefold. **Feito 29/08** — 22/22 nos dois idiomas, mesmo vetor, mesma classe de espaço por escapes.
- [x] T064 [P] [US5] `backend/tests/test_price_observations.py` (NOVO): `PUT` em lote upserta por `(subjectKind, subjectId)` na linha única da conta; `GET` devolve só o uid do token; portas: `PUT` → `require_entitlement` (`entitlement/__init__.py:92`, `lapsed` ⇒ 403 `ENTITLEMENT_REQUIRED` no envelope); `GET` → `require_catalog_read` (`:105`, `lapsed` ⇒ 200, **`none` ⇒ 403** — a PR-B tolera); `observedPrice` negativo/NaN/> `CEIL_MONEY` ⇒ 422 (`finite_non_negative`, `app/validation.py:55`) **e com mais de 2 casas ⇒ 422 por um `field_validator` NOVO de escala** (`finite_non_negative :55-65` NÃO checa casas; `Numeric(12,2)` arredondaria calado); **lote com o mesmo (kind,id) repetido ⇒ 422, nunca 500** (`ON CONFLICT DO UPDATE` levanta `cardinality_violation`); lote > 500 ⇒ 422; `items: []` ⇒ 200 `{upserted: 0}`; `ownerUid` no corpo ⇒ 422 (`extra="forbid"`); isolamento: conta B faz PUT com `subjectId` de A, A faz GET e não vê; PUT idempotente. O servidor carimba `observedAt` (`now()`) — o corpo não o envia. **Feito 29/08** — 8 funções/20 casos (portas, dedupe 422, escala 422, observedAt do servidor).
- [x] T065 [P] [US5] `backend/tests/test_catalog_name_conflict.py` (NOVO): para CADA tabela (`POST /filaments`, `/printers`, `/products`, `/boms`) criar "Gancho" e depois "gancho " ⇒ 201 com o nome FINAL "(2)" (sem erro — Q5); renomear (PUT) para nome ocupado segue a MESMA regra; nome > 120 chars ⇒ 422 (só no pydantic; sem CHECK de comprimento no banco). **Corrida** (sem precedente no repo; determinística, sem `sleep`): uma conexão psycopg DIRETA insere a linha concorrente com o mesmo `name_norm` e mantém a transação ABERTA; o `POST` do `TestClient` bloqueia no índice único e, ao COMMIT da primeira, recebe `IntegrityError` e renomeia ⇒ uma "(2)", zero perdidas, zero 500; registrar o mecanismo em dod-evidence. **Kit**: `POST /boms` cujo `_materialize` (`boms.py:397`) cria produto colidindo com um vivo ⇒ o kit salva, o materializado ganha "(2)", nenhuma linha perdida — mutação: sem `session.begin_nested()` o `IntegrityError` aborta a transação atômica (ADR-0017) e o kit morre. `_dedup_match` (`boms.py:294-309`) passa a casar por `name_norm` (recomendação aplicada; emenda datada ao ADR-0017 §3 — T129). **Feito 29/08** — 9 funções/24 casos nas 4 tabelas + PUT + 120 + corrida por pg_stat_activity; achado: pieceName 500→422.
- [x] T066 [P] [US5] `backend/tests/test_products_fixed_price.py` (NOVO): a rota NOVA `PATCH /api/v1/products/{id}` (`products.py` só tem `:464/:489/:514/:529/:547`) com corpo `{sellerFixedPrice}` grava e devolve; `null` desfixa e zera `sellerFixedAt` (carimbado pelo SERVIDOR); outra chave ⇒ 422; `lapsed` ⇒ 403; produto de outro dono ⇒ 404 (`_owned`, `:320`). A não-composição é provada por AUSÊNCIA (o backend nunca computa — ADR-0008, `boms.py:4`): `set(ProductOut.model_fields)` CONGELADO = campos de hoje + `sellerFixedPrice` + `sellerFixedAt` (nenhum `price`/`preco*`); `BomLineOut`/`BomOut` (`boms.py:190-227`) NÃO ganham `seller_fixed_price`. **Feito 29/08** — 9 funções/18 casos; não-composição pela AUSÊNCIA da chave em ProductOut.
- [x] T067 [P] [US5] `apps/web/src/entities/catalog/price-observations.test.ts` (NOVO): o módulo é PURO sobre dados — recebe os preços recomputados por argumento e NUNCA importa `@/features` (fronteira `eslint.config.mjs:84`; asserção de grafo); o `PUT` só dispara DEPOIS de TODOS os itens RECOMPUTÁVEIS exibidos terem preço — um produto degradado (`productNeedsAttention`, `entities/catalog/product-summary.ts:34-36`) fica FORA do lote e NÃO impede o PUT dos demais (motor REAL + spy no cliente HTTP — não mockar `@3dprecify/pricing-core`, sem precedente); falha do PUT ⇒ a marca não avança e a próxima visita repete; 403 (lapsed, toda visita) ⇒ SILENCIOSO (nenhum toast/Alert/retry); sem observação ⇒ nenhum texto (nem "0 mudaram"); offline (`use-online.ts`) ⇒ zero PUT e nada no outbox; a resposta NÃO é persistida em `catalog-cache.ts`. **Feito 29/08** — 13 testes com motor real; grafo (sem features/catalog-cache); 403 silencioso; offline zero PUT; sem IDB (varredura em providers.test).
- [x] T068 [P] [US5] `apps/web/src/features/catalog/products-panel.test.tsx` (casos novos): 12 produtos COM PREÇO RECOMPUTADO NA LINHA (injetado por prop — T124), 3 com observação ≠ recomputado ⇒ "3 preços mudaram desde a sua última visita" + "era R$ 38,90" + "Salvo em 12/05" (verbatim; data do `observed_at` no fuso LOCAL, caso que cruza a meia-noite UTC); item fixado mostra "Preço fixado por você" e o valor do vendedor; custoHoje > fixado ⇒ `<Alert tone="warning">` com estado próprio `productPriceOverFixed` (NÃO reusar `productNeedsAttention`, K3, `product-summary.ts:34-36`); "Voltar a acompanhar o custo" ⇒ recomputado; nome repetido ⇒ "Este nome já está no catálogo" no campo ANTES do submit (`nameNorm` local, `shared/lib/name-norm.ts`); duplicar ⇒ "Gancho (cópia)"; cópia de degradado continua degradada; produto DEGRADADO não tem recomputado — sem preço, sem observação, nunca "R$ 0,00". Formatador de data: DUAS cópias hoje (`features/calculator/fee-seal.tsx:40` e `calculator-form.tsx:774` `fmtDatePtBr`) — promover a `shared/lib/format-date.ts` absorvendo as duas; o fuso é o do APARELHO na leitura (a tabela não guarda offset, ao contrário de `snapshots :653-658`) — declarado no teste. **Feito 29/08** — +12 casos em products-panel + 2 em produto-page (recusa antes do submit).
- [x] T125 [P] [US5] Guarda de propriedade (ADR-0033 "um leitor futuro tratar `seller_fixed_price` como o preço"): um teste POR TELA (lista, ficha, kit, orçamento, cenário) afirmando que nenhum caminho de exibição lê `sellerFixedPrice`/observação como fonte do número GRANDE fora do Catálogo. **Feito 29/08** — `fixed-price-property.test.tsx`: leitura do fonte (exceções vazias em pages/features) + render do número grande.
- [x] T127 [P] [US5] `apps/web/src/entities/catalog/catalog-cache.ts` (`:41-51` não versiona o payload; `:61` grava `ProductOut` inteiro): entradas cacheadas ANTES da 0008 voltam com `sellerFixedPrice` `undefined` — `undefined` e `null` = "não fixado" (nunca 0,00); e a observação NÃO ganha cache de dispositivo (senão entraria em `purgeCatalogCache :69-79`) — registrar "sem persistência local" para `price-observations` na tabela de `app/providers.test.tsx:117-177` (a varredura por uid de `providers.tsx:49-53`). Teste + comentário. **Feito 29/08** — `readSellerFixedPrice` (undefined|null ⇒ não fixado) + linha 'sem persistência local' provada na varredura.
- [x] T069 [US5] `apps/web/tests/e2e/catalog-recalculo.spec.ts` (NOVO, stack real; `data-testid`s novos `products-price-changed-banner`, `product-row-fixed`): filamento 100 → produtos → sair/voltar ⇒ nada; editar filamento para 120 → voltar ⇒ contagem e "era" corretos; fixar ⇒ editar filamento ⇒ preço fixado NÃO muda + aviso quando custo > fixado; desfixar; nome duplicado ⇒ "(2)" silencioso; densidade da lista a 390 (`tf-plist`), a 1024/1279 (`tf-table` — DECISÃO 1) e a 1280/1920 (mestre-detalhe, inalterado). Envenenamento: com filaments/printers ainda carregando (`page.route` atrasada; `products-panel.tsx:27-28`), NENHUM PUT sai — a marca só avança quando os dois catálogos de referência resolveram. **Feito 29/08** — 7 cenários na stack real (valores capturados; '(2)' pela API; envenenamento com IDB limpo; 48 passed/6 flaky conhecidos); achou a lista sem `tf-plist` (corrigido).
- [x] T070 [US5] Contrato — os TRÊS comandos do CI, da raiz (`.github/workflows/ci.yml:65-71`): (1) `cd backend && PYTHONPATH=. uv run python scripts/export_openapi.py && cd ..` (2) `pnpm --filter @3dprecify/web gen:api` (não existe `gen:api` na raiz) (3) `git diff --exit-code -- contracts/openapi.json apps/web/src/shared/api`; duas vezes; o docstring da rota vira `description` (regen após QUALQUER edição de docstring); formatar só da raiz. `PATCH` é OPERAÇÃO nova no OpenAPI (símbolo Orval novo — confirmar convenção com um PATCH existente, `scenarios.py`). Asserir que `ProductOut` ganhou APENAS `sellerFixedPrice`/`sellerFixedAt`. **Feito 29/08** — regen idempotente (2× diff vazio); re-provado no T078. Conformidade: forbid no item do array → model_validator (⛔ dono).

### Implementation

- [x] T071 [US5] Migração `backend/alembic/versions/0008_price_observations_fixed_price_name_norm.py` (revision "0008", down "0007") em 6 passos, com o DDL/índices/backfill LITERAIS de `auditoria/05-dados-migracoes.md` §SQL: (1) `create_table("price_observations", …)`; (2) `products.seller_fixed_price` Numeric(12,2) + `seller_fixed_at` timestamptz + CHECK; (3) `name_norm` NULLABLE nas 4; (4) backfill em Python via `op.get_bind()` com a função de normalização COPIADA para dentro do arquivo (migração mergeada é imutável — `scripts/check-migrations.sh`; NUNCA importar `app.lib`) e desempate "(2)","(3)" por (owner_uid, tabela) entre VIVOS; (5) `alter_column(nullable=False)`; (6) 4 índices únicos parciais. `downgrade()` = inverso exato; docstring declara que os VALORES de `seller_fixed_price` morrem (molde `0007_remove_waste.py:14-27`). Modelos: `PriceObservation`, colunas novas, CHECKs em `Product.__table_args__` (`:207-288`), `name_norm` + `Index(unique=True, postgresql_where=text("deleted_at IS NULL"))` nos 4 (precedente `ix_snapshots_owner_active`, `:632-638`), e a REESCRITA do docstring `models/__init__.py:198-204` com ADR-0033 §1. Teto de nome 120 (precedente `scenarios.py:84`) SÓ no pydantic das 4 (escritas novas) — SEM CHECK de comprimento no banco (o legado ficaria inválido); `name_norm` é SEMPRE `left(norm, 200)` (backfill E escritor). No MODELO os CHECKs levam o nome CURTO (`seller_fixed_price_valid`, `subject_kind_enum`, `observed_price_valid`, `observed_at_finite`, `model_version_set`) — a `NAMING_CONVENTION["ck"]` (`models:44-51`, precedente `name="kind_enum"` `:584`) prefixa `ck_<tabela>_`; na migração vale o literal `op.f("ck_…")`; `UniqueConstraint`/`Index` com nome completo (`:583`, `:634`). **Feito 29/08** — `55721ce`, 6 passos literais, backfill com função congelada, reserva do sufixo.
- [x] T072 [P] [US5] `backend/app/api/price_observations.py` (GET com `require_catalog_read`, PUT com `require_entitlement`; `CamelModel` + `model_config = ConfigDict(extra="forbid")` nos DOIS schemas — `CamelModel` não o traz, `errors.py:44-46`; `items` `max_length=500`; dedupe de (kind,id) no lote ⇒ 422; `owner_uid` só de `claims["uid"]`; `observed_at = now()` no servidor; router registrado em `app/main.py` junto de products `:134`) + `backend/app/lib/__init__.py` + `backend/app/lib/name_norm.py` (pura) + contrato `[[tool.importlinter.contracts]]` "dependency-free leaf" em `backend/pyproject.toml` (molde `:134-138` — `uv run lint-imports` é do `gate:be`). Aplicar `name_norm` (`left(norm, 200)`) + sufixo nos **7** sítios reais (NÃO existe `catalog*.py`): `products.py:375` (dentro de `_apply :370` — funil de POST, PUT E da materialização de kit, que reusa `_apply` via `boms.py:52-53,:431`), `filaments.py:161,192`, `printers.py:148,180`, `boms.py:596,642`; `PriceObservationIn` ganha `field_validator` de escala (`value.as_tuple().exponent >= -2`, senão 422 — divergência consciente do resto da casa, registrada em dod-evidence); retry sob `session.begin_nested()` limitado (50 ⇒ 422 `VALIDATION_ERROR`, sem `ErrorCode` novo — `errors.py:35-40`). **Feito 29/08** — `8a78b5f` (`price_observations.py`, router em `main.py`).
- [x] T073 [US5] (mesmo `products.py` da T072; ambas depois da T071) `products.py`: rota NOVA `@router.patch("/products/{product_id}")` com `ProductPatchIn(CamelModel)` (`extra="forbid"`, campo único `seller_fixed_price: Decimal | None` — precedente `scenarios.py:593-596` + rota `:626-636`), porta `require_entitlement`, `_owned` (⇒ 404); `sellerFixedAt` carimbado pelo SERVIDOR (NULL ao desfixar; nunca do corpo); `ProductOut` (`:236-247`) e `_to_out` (`:252`) ganham os 2 campos e NENHUM `price`. **Feito 29/08** — `PATCH /products/{id}`, `ProductPatchIn`, sellerFixedAt do servidor, funil do nome nos 7 sítios (`api/naming.py`).
- [x] T129 [US5] Emenda datada ao ADR-0017 §3: `_dedup_match` (`boms.py:294-309`) casa por `name_norm` (a unicidade nova tornaria o match exato uma armadilha) — registrada no ADR e no dod-evidence (recomendação aplicada; o dono pode reverter para "exato + (2)"). **Feito 29/08** — emenda datada no ADR-0017 §3 aplicada (`_dedup_match` por name_norm).
- [x] T075 [US5] `apps/web/src/entities/catalog/price-observations.ts` (client + hook: `usePriceObservations()`/`useUpsertPriceObservations()`, chave `["price-observations", uid]`, invalidação no molde de `useInvalidateCatalog`; comparar com os preços recebidos por argumento; derivar contagem/"era"; escrever em lote pós-render, online-only, sem cache) + `shared/lib/name-norm.ts` (`nameNorm(raw)`). **Feito 29/08** — `cd78f68`; API: usePriceObservations/derivePriceChanges/useObservePrices(+catalogVersion)/useFixProductPrice.
- [x] T124 [US5] ONDE o recompute da lista roda (fronteiras: `entities` só importa `shared`; `features/catalog` não importa `features/calculator`): compor em `pages/catalogo/catalogo-page.tsx` — `products.items.map(p => computeFromForm(productToForm(p).values, {catalog, source, now}).result?.precoVarejo)` (`features/calculator/product-mapping.ts:68`, `calculator-model.ts:443`, hoje 1× em `produto-page.tsx:142-149`) só quando `useFilaments`/`usePrinters` resolveram; itens degradados ficam fora do mapa (sem 0,00) — e injetar `{id, precoVarejo}[]` por prop nos painéis (recomendação aplicada; alternativa: `entities/pricing`). **Feito 29/08** — recompute na PAGE, degradado fora, envenenamento (nenhum PUT com referência carregando), observe pós-commit só com a lista visível (`5128402`).
- [x] T130 [US5] (PRIMEIRA task de Implementation da PR-D — antes da T076) Emenda datada ao **ADR-0031** (§Emenda 2, 2026-08-27, decisões 1 e 2 do dono): limiares NOMEADOS — `WIDE_QUERY` 1280 (mestre-detalhe, coluna larga de Simulações em `/calcular`), `LIST_DENSE_QUERY` 1024 (só a densidade da lista do Catálogo: `tf-table`), e o corte próprio de layout da Calculadora em 1024 (`calculator-form.css:25,40,65,81`, que já existia sem nome) — todos em `shared/lib/use-is-wide.ts` (hoje exporta `WIDE_QUERY`, `RAIL_FORCADO_QUERY`, `useIsWide`, `useRailForcado`), nunca `matchMedia` solto; o §Emenda 6 ("nenhum 2º gate") é reescrito para "nenhum limiar sem nome fora deste arquivo". **Feito 29/08** — ADR-0031 §Emenda 2 escrita; `LIST_DENSE_QUERY`/`useIsListDense` em `use-is-wide.ts` (`630f1b1`).
- [x] T076 [US5] `features/catalog/catalog-panel.tsx` (genérico, 4 abas): `CatalogPanelProps` ganha `rowPrice?/rowWas?/rowFlag?` e a lista (`:368-432` mobile, `:295-322` mestre) vira `<Plist>` (filaments/printers/kits ganham de graça — recomendação aplicada vs lista dedicada); `products-panel.tsx` + `produto-page.tsx` + `kits-panel.tsx` (`subject_kind=KIT`): faixa "N preços mudaram", "era"/"Salvo em" por item, fixar/desfixar (`PATCH`), aviso warning (`productPriceOverFixed`), duplicar, validação de nome ANTES do submit. `<Table>` (DECISÃO 1, 27/08): entra na faixa **1024–1279px** por um limiar NOMEADO novo (`useIsListDense()` = `(min-width: 1024px)` em `shared/lib/use-is-wide.ts`, criado pela T130, que vem ANTES); ≥1280 o mestre-detalhe do 018 fica como está (a tabela NÃO substitui a coluna-mestre). O formulário de produto CONSOME `precision` (T060). **Feito 29/08** — `33d51f5` + follow-up do nome; kits sem preço (sem função de total na lista); ícone lock inexistente (texto só).
- [x] T077 [US5] `specs/007-e2-catalog-entitlement/spec.md:410` — conferir que a Clarification é TEXTUALMENTE o ADR-0033 §5 (diff palavra a palavra); registrar a escalação opus no ledger. **Feito 29/08** — diff vazio contra o ADR-0033 §5; escalação opus na linha do ledger de 29/08.
- [x] T078 [US5] gate:all + e2e + drift-guard (T070); screenshots 1:1 (faixa de mudados, item fixado com aviso, `tf-plist` 390, `tf-table` 1024/1279, mestre-detalhe 1280 inalterado) em `evidencias/pr-d/`; abrir o PR-D e pedir o flip do **ADR-0033** — que carrega o **§Adendo 2026-08-27** (já escrito pré-flip) com as 4 decisões aplicadas: `observed_at` carimbado no servidor · teto de lote 500 · teto de nome 120 no pydantic + `name_norm` `left(…, 200)` no legado · `_dedup_match` normalizado (cruza T129) — + emenda 0017 + emenda 2 do 0031. **Feito 29/08** — gate final 1994/577 · drift-guard 2× · 34 capturas + medidas · push autorizado pelo dono, **PR #62** aberta (pede o flip do ADR-0033 + emendas 0017/0031 + 10 ratificações).

**Checkpoint**: 0008 sobe em conta com legado; 8 sítios de nome normalizados; kit atômico intacto; lista com preço por item; observação online-only sem cache; `ProductOut` sem preço calculado (conjunto congelado).

---

## Phase 8: US6 — Montar e Enviar (P3 · PR-E · **OPUS**) — FEATURE NOVA, a maior

**Meta**: `computeQuote` 4.2.0 (recusa `channels` em RUNTIME; dinheiro como STRING no documento); construtor
multi-item; desconto no TOTAL; piso avisa; "Válido até" = `quote_validity_days` (coluna, NÃO payload); Enviar =
`useRecordSnapshot` direto + PDF; 0009 = três CHECKs no mesmo ato (NÃO há enum Postgres). Autoridade: ADR-0034,
`auditoria/05` §SQL. DECISÃO 4 (27/08): **Enviar exige conexão** — o construtor funciona offline, o botão Enviar fica desabilitado com o motivo; nada novo entra no outbox (research §K mantida).

### Transcrição (PRIMEIRA)

- [x] T087 [US6] Baixar `Orcamentos - Montar e Enviar` (dois temas) e transcrever US16/US17 verbatim ("Válido até", "Enviar congela este preço", "Voltar a acompanhar não vale para orçamentos enviados", "Abaixo do custo", rótulos do desconto). "10 un. sai mais barato que 9" NÃO é transcrita (US18 retirada). **Feito 29/08** — prancheta 18 × 2 temas congeladas; namespace `quote` (30 chaves) só com o que US6/US16/US17 abrangem; a 18c (US18), os 5 estados, frete, WhatsApp/Copiar/Compartilhar e o prazo de produção ficaram FORA e registrados; `sendOffline` derivada pelo molde 'precisa de conexão' (DECISÃO 4).

### Tests (vermelhos primeiro)

- [x] T080 [P] [US6] PRIMEIRO COMMIT da fatia, com `src/index.ts` INTOCADO: `packages/pricing-core/tests/__fixtures__/generate-equality-4.1.0.mjs` (PRNG `mulberry32(20260827)`, sem `Math.random`/`Date`) emite `tests/__fixtures__/equality-4.1.0.json` = `{generatedAtVersion:"4.1.0", calculator:[{input,result}]×500, bom:[{lines,result}]×200}` cobrindo com/sem `channels` — nos 200 casos `bom`, linhas COM `channels` por linha (`BomLineInput.input: PriceInput`, `:446-449`) e a comparação abrange `result.channels[]` (o rollup, `index.ts:485`, o único lugar onde uma mudança da 4.2.0 vazaria sem ser vista) —, `otherCosts`, `failurePct` alto, meio-centavo (ROUND_HALF_UP, `rounding.ts:12`). `tests/version-equality-4.1-4.2.test.ts` compara campo a campo ignorando apenas `modelVersion`. `__fixtures__` já está fora do coverage no **root** `vitest.config.ts:35` (o `vitest.config.ts` do pacote tem 6 linhas). **Feito 29/08** — `1c49228`: fixture SHA `41122df1…` gerada com o motor 4.1.0 intocado (o gerador recusa rodar depois do bump); 415 rollups comparados; não-vácuo: ROUND_HALF_DOWN e `skippedLines=0` ⇒ vermelho.
- [x] T079 [P] [US6] `packages/pricing-core/tests/computeQuote.test.ts` (NOVO): vetor (3 linhas × quantidades; `PCT` 10% e `AMOUNT` R$ 10); `grossTotal === bom.precoVarejo` (`index.ts:483`); `netTotal === toMoney(gross − discountAmount)`; `costFloor === bom.custoTotal` (`:482`); `belowCost` ESTRITO (empate = false); desconto > total / negativo / pct > 100 / não-finito ⇒ `ValidationError` (`index.ts:196`); **linha com `input.channels` não-vazio ⇒ `ValidationError` field=`lines[i].input.channels`** (o tipo NÃO barra — `PriceInput.channels?` `:173`; recusa em RUNTIME); `PRICING_MODEL_VERSION === "4.2.0"` com `tests/version.test.ts:18` (+ comentários `:12`/`:22`; `:25`, o major "4", permanece) e `package.json:3` mudando JUNTO. **`computeQuote` devolve NÚMEROS** (o regime de `toMoney(): number`, `rounding.ts:11-13` — as igualdades são numéricas); a serialização como STRING decimal é do DOCUMENTO e mora na T133. **Feito 29/08** — `computeQuote.test.ts` 22 testes (belowCost estrito, channels recusado em runtime com field `lines[i].input.channels`, devolve números).
- [x] T081 [P] [US6] `backend/tests/test_migration_0009.py`: `kind='QUOTE'` e `headline_basis='PRECO_ORCAMENTO'` aceitos (são CHECKs em coluna TEXT — `models:584/:586`; não há enum); INSERT QUOTE com `headline_total ≠ payload.totals.precoOrcamento` ⇒ RECUSADO por `ck_snapshots_headline_matches_totals` (sem o ramo o `CASE` dá NULL e PASSA — não-vácuo removendo o ramo); SINGLE/KIT intocadas; UPDATE em QUOTE recusado por `trg_snapshots_immutable` (criado em `0003:192-199`; função vigente = **V2 da `0006:118-144`**, aplicada `:150` — FOR EACH ROW, sem filtro de `kind`); `downgrade()` restaura os CHECKs e **falha por desenho se existir QUOTE** (`trg_snapshots_forbid_delete`, `0006:106-114,:152-159`) — docstring no molde `0007:14-27`. **Feito 29/08** — 10 testes; a mutação do ramo do CASE vive DENTRO do teste (sem o ramo o CHECK passa em silêncio); downgrade irreversível com QUOTE declarado.
- [x] T082 [P] [US6] `backend/tests/test_history_basis_mirror.py` (NOVO): igualdade de conjuntos entre os QUATRO espelhos — `Literal` de `SnapshotIn.headline_basis` (`history.py:119`) == `_BASIS_TOTAL_KEY` (`:71-74`) == `quote_render._BASIS_TOTAL` (`:31`) == `quote_render._BASIS_CAPTION` (`:247`); e `kind` (`:109`) == CHECK; mutação ⇒ vermelho. `backend/tests/test_export.py` (casos novos, reusando `_pdf_text :606`, `build_quote_view`/`render_quote_pdf`): QUOTE exportado imprime `payload.totals.precoOrcamento` (NUNCA o fallback `precoVarejo` de `_basis_key :96-97` — vira erro explícito) e legenda TRADUZIDA (o consumidor `:350` imprime `quote.basis` cru quando a chave falta); itemiza `payload.lines` (hoje `build_quote_view` só ramifica em `if snapshot.kind == "KIT"`, `:195`); bruto→desconto→total só com `discount`; geometria do nome de 300 chars com `_pdf_runs` (`:776`, módulo) + `_assert_no_overprint` (`:894` — hoje ANINHADA num teste: promover a escopo de módulo antes de reusar, refactor sem mudança); adversarial `<b>`/`&amp;` no nome do item e no rótulo do desconto ⇒ o PDF imprime o literal via `_xml` (`:112-127`); CSV com cabeçalho idêntico (`_CSV_FIELDS :57`) e `kind=QUOTE`. **Feito 29/08** — 4+2 espelhos por igualdade de conjuntos E de chaves; test_export +11 (QUOTE lê precoOrcamento, itemiza, bruto→desconto→total, geometria de 300 chars, `<b>`/`&amp;` literais, CSV kind=QUOTE).
- [x] T131 [P] [US6] `backend/tests/test_history.py` (casos novos): QUOTE com inteiro JSON em `discount.amount`, `costFloor`, `lines[0].unitPrice` ⇒ 422 cada (não-vácuo: sem estender `_MONEY_POSITION_KEYS` `validation.py:52` o 1º passa e congela — E4-01); `grossTotal − amount != totals.precoOrcamento` ⇒ 422; **não-regressão**: um KIT de hoje com `lines[0].quantity` inteiro continua 201; `mode` inválido / `PCT` fora de 0..100 / `AMOUNT` > `grossTotal` / string negativa ⇒ 422 (no pydantic, nunca `IntegrityError` — 500 é re-POSTado pelo outbox para sempre, `history.py:136-138`); contrato end-to-end: um payload QUOTE construído pelo cliente é aceito por `SnapshotIn._validate_frozen_document` (`history.py:131-189`) com `kind`, `modelVersion "4.2.0"`, `schemaVersion 1`, `headlineTotal == totals.precoOrcamento`, zero float. **Feito 29/08** — +18 casos (inteiro em cada folha ⇒ 422; KIT com quantity inteiro continua 201; shape do documento QUOTE congelado num teste).
- [x] T083 [P] [US6] `apps/web/src/features/history/quote-builder.test.tsx` (NOVO): N itens do catálogo (produtos E kits) + quantidades; o total exibido é IGUAL a `computeQuote` rodado FORA da tela no próprio teste (igualdade, não espionagem — `vi.spyOn` em export ESM do pacote não tem precedente); desconto % e R$; "Abaixo do custo" (warning) quando `belowCost`; item degradado entra com "(avulsa)" (D6, ADR-0017 §6 — o construtor precisa desse leitor); produto FIXADO entra pelo preço do MOTOR, nunca pelo fixado (ADR-0033 §3); antes de enviar nada congela; "Válido até" é texto de `quoteValidityDays`. Só compila após T086 + regen (tipos Orval `SnapshotInKind`/`SnapshotInHeadlineBasis`) e T133 (o envelope alargado). **Feito 29/08** — o contrato ficou vermelho por ausência e virou verde com o T088: 15/15; única adaptação: `toLineInput` injetado (decisão de fronteira).
- [x] T133 [P] [US6] (edita PRODUÇÃO: tipo + teste no mesmo commit, vermelho antes) `apps/web/src/entities/history/frozen-payload.ts` — `kind` (`:140`) admite `"QUOTE"`; `FrozenTotals` (`:70-73`) ganha `precoOrcamento?`; nascem `FrozenQuoteDiscount {mode, value, amount, grossTotal}`, `costFloor?`, `FrozenQuoteLine {name, quantity, unitPrice, subtotal, origin}` (`FrozenKitLine :111` NÃO é idêntica); `frozen-payload.test.ts` prova que `FROZEN_PAYLOAD_SCHEMA_VERSION` continua **1** e que as fixtures pré-019 (`__fixtures__/`, molde `frozen-payload-pre-016.json`) leem idêntico. A conversão de TODO dinheiro do documento (`unitPrice`, `subtotal`, `amount`, `grossTotal`, `costFloor`) E de `discount.value` para STRING decimal mora AQUI (precedente `toMoneyString :200-201`) — `validation.py:106-110` rejeita float; o motor devolve números (T079). **Feito 29/08** — `26bfb50`: `buildQuotePayload` (todo dinheiro string; schemaVersion 1; fixtures pré-019 idênticas); `FrozenQuoteLine`/`FrozenQuoteDiscount` novos; `lines` como união de ARRAYS com leitores estreitos.
- [x] T135 [P] [US6] (edita PRODUÇÃO: decisão por arquivo + teste no mesmo commit) Varredura da união alargada nos 8 consumidores: `pages/historico/{snapshot-detail-page,historico-page,recalc-today,compare-today}.tsx` (o `Record<SnapshotInHeadlineBasis,…>` de `recalc-today.tsx:53-56` deixa de compilar — é ali que se decide que "Recalcular hoje"/"comparar hoje" NÃO valem para QUOTE, copy da US17), `features/history/{record-snapshot-sheet,export-sheet}.tsx`, `entities/history/outbox.ts`, `features/scenarios/scenario-context-bar.tsx` — por arquivo, o que um QUOTE faz e NÃO faz, com teste. **Feito 29/08** — 8 (+1: `history-format.ts`) consumidores decididos com teste (tabela em dod-evidence): detalhe itemiza + 'válido até' texto + `noUnfixForSent`; Recalcular/Comparar hoje ausentes; record-sheet exclui QUOTE por TIPO; export só sincronizado (`pdfWaitsSync`); outbox sem ramo; context-bar ignora.
- [x] T084 [US6] (depende de T086 — 0009 + rotas — e de T088 — o construtor) `apps/web/tests/e2e/quote-builder.spec.ts` (NOVO, stack real): 3 itens × (1,2,10) → total == `computeQuote`; desconto até o piso ⇒ aviso; Enviar ⇒ registro em Orçamentos `kind=QUOTE`; editar ⇒ imutável; PDF `GET /api/v1/history/{id}/quote.pdf` 200 (a geometria fina é do pytest, T082). OFFLINE (DECISÃO 4): montar funciona; "Enviar" está `disabled` com o motivo verbatim (transcrito na T087, mesma família de `history-export` offline: "a affordance é desabilitada COM a razão"); zero entrada no outbox; ao reconectar, Enviar habilita e grava — online o registro passa TRANSITORIAMENTE pelo outbox (durable-first, `use-history.ts:89-108`) e é drenado na mesma chamada: o e2e asserta um POST para ESTE orçamento com a fila vazia no início; enquanto `syncState !== "synced"` não há PDF (`export.py:80-84` exige o id do servidor) e a tela diz isso. **Feito 30/08** — 10/10 × 2 projetos na stack real; as 3 causas eram do TESTE (rótulo opcional, toast empilhado, `?construir=true` — o router serializa booleano cru); item PARADO só existe via kit materializado (FR-310) — helper adotado.

### Implementation

- [x] T085 [US6] `packages/pricing-core/src/index.ts`: `computeQuote` (Decimal/`toMoney` de `./rounding.ts`, ADR-0008; recusa de `channels` em runtime; `ValidationError` para desconto inválido; saída de dinheiro como string) + `PRICING_MODEL_VERSION = "4.2.0"` (`:29`) + `package.json:3` + `tests/version.test.ts:18` com o PORQUÊ (MINOR aditiva; precedente 4.0.0→4.1.0; `:25`, o major, permanece) — **só depois** de T080 verde. Imports relativos com extensão `.ts` (`boots-under-node.test.ts`, 014/US4); ratchet 100% (root `vitest.config.ts:38`). **Feito 29/08** — `computeQuote` em Decimal; 4.2.0 no package.json + version.test com o porquê; varredura 4.1→4.2 verde; cobertura 100%. `QuoteResult` é SUPERSET do esboço do ADR-0034 (+`lines[]`, +`modelVersion`) — registrado.
- [x] T086 [US6] Migração `backend/alembic/versions/0009_snapshot_quote.py` (revision "0009", down "0008"): DROP+ADD dos TRÊS CHECKs no MESMO ato (`ck_snapshots_kind_enum`, `ck_snapshots_headline_basis_enum`, `ck_snapshots_headline_matches_totals` com `WHEN 'PRECO_ORCAMENTO' THEN 'precoOrcamento'` — SQL literal em `auditoria/05`). `backend/app/api/history.py`: `_BASIS_TOTAL_KEY` (`:71-74`) + `Literal`s (`:109`, `:119`) + validador do desconto (T131). `backend/app/validation.py:52`: `_MONEY_POSITION_KEYS` ganha as FOLHAS `unitPrice`, `subtotal`, `costFloor`, `amount`, `grossTotal` — **NUNCA `lines` nem `discount`** (a marca é de SUBÁRVORE, `validation.py:96-99`: `lines` recusaria `quantity` inteiro de TODO KIT existente — regressão —, `discount` marcaria `value`, percentual em PCT); o KIT de hoje continua 201 (T131). `backend/app/services/quote_render.py`: `_BASIS_TOTAL` (`:31`), `_BASIS_CAPTION` (`:247`; e o consumidor `:350`), `_basis_key` (`:96-97`) sem fallback, ramo `kind == "QUOTE"` em `build_quote_view` (`:179-241`) itemizando `lines` por `subtotal`, bloco bruto→desconto→total, todo texto do vendedor por `_xml` + `Paragraph` (`:112-127`). CSV intocado (coluna nova reabriria a decisão de injeção de fórmula do E4). **Feito 29/08** — `6fd47eb`: 0009 três CHECKs no mesmo ato; history.py/validation.py/quote_render.py; contrato 2× idêntico; Schemathesis 54 s; 622 passed.
- [x] T088 [US6] `apps/web/src/features/history/quote-builder.tsx` (+ `.css`) e a entrada em `pages/historico/` por rota de **1 segmento** `/historico?construir=1` (molde `/catalogo?produto=novo`, `products-panel.tsx:70` — armadilha `base:'./'`): seleção de produtos e kits (leitor de degradação D6), quantidade, desconto (% | R$), readout bruto/desconto/total/piso, "Válido até" (`quoteValidityDays`), "Enviar" grava via `useRecordSnapshot()` (`use-history.ts:78`) DIRETO, montando o `SnapshotIn` (o `RecordForm` de `record-snapshot-sheet.tsx:113-124` faz `if (!total) return` e re-perguntaria rótulo/validade — não é reutilizado), com teste de que Enviar produz UMA requisição; offline (`use-online.ts`) o botão fica `disabled` com o motivo e NADA é enfileirado (DECISÃO 4); online o registro passa transitoriamente pelo outbox (durable-first — `useRecordSnapshot` SEMPRE chama `enqueueSnapshot`, `use-history.ts:96`, e drena `:100`) e o export só abre com `syncState === "synced"` (sem id do servidor não há PDF); produto FIXADO entra pelo preço do MOTOR, nunca pelo fixado (ADR-0033 §3). `data-testid`s para T084. **Feito 29/08** — `1a46513` (+ o título/total do 18e): QuoteBuilder puro por props; `toLineInput` na page (`quote-line-input.ts`, reusa `bomLineToInput` exportado de recalc-today); 18d·2 OMITIDO (sem limiar decidido — follow-up de design); guarda de reentrância (sendingRef+sentRef); a guarda T125 pegou um vazamento de observedPrice em pages/historico — corrigido ('parado desde' lê product.updatedAt).
- [ ] T089 [US6] Contrato pelos três comandos do CI (T070), duas vezes; gate:all + e2e; screenshots 1:1 (construtor 390/1280, aviso de piso, documento enviado, PDF) em `evidencias/pr-e/`; ledger com a escalação opus; abrir o PR-E e pedir o flip do **ADR-0034**.

**Checkpoint**: fixture 4.1.0 antes do bump; três CHECKs no mesmo ato; quatro espelhos iguais; PDF do QUOTE lê `precoOrcamento` e itemiza; dinheiro do documento como string; um Enviar = uma requisição.

---

## Phase 9: US7 — Simulações desktop + as divergências (P3 · PR-F)

**Meta**: Simulações ≥1280px (prancheta 20g, MESMO componente, extraído do privado `ScenarioListBody`); D1 guarda
das 7 ocorrências de "Premium pausado"; D2 JÁ FEITO (guarda por mutação); A11-r só medição; Q1/Q2. DECISÃO 2 (27/08): o hospedeiro é a **coluna larga de `/calcular`**
(emenda 2 do ADR-0031 — T130 — nomeia os dois limiares que convivem nessa tela).

### Transcrição + baseline (PRIMEIRO)

- [x] T094 [US7] Baixar `Simulacoes - A Estrategia Viva` + `As Escritas Congeladas` (dois temas) para `design/`; capturar baseline mobile 390/360 em `evidencias/pr-f/baseline-mobile/` e a razão `widthRatio()` de `/calcular` a 1280/1440/1920 (`pages-desktop-width.spec.ts:17-29`) ANTES de tocar código. **Feito 29/08** — 4 pranchetas congeladas (claras conferidas com as remotas); baseline `evidencias/pr-f/baseline-mobile/` (8 PNGs) + widthRatio 93,8/93,3/66,7% (1280/1440/1920), overflow 0 — `4a1b632`.

### Tests (vermelhos primeiro)

- [x] T090 [P] [US7] `apps/web/src/shared/i18n/premium-pausado-trio.test.ts` (NOVO, D1): as ocorrências REAIS de "Premium pausado" são SETE — `conta.planLapsed` (`:550`), `catalogo.lapsedTitle` (`:645`, apagada ou não pela T038), `bom.lapsedTitle` (`:791`) + `bom.lapsedBanner` (`:794`), `historico.lapsedBanner` (`:967`; `historico` NÃO tem `lapsedTitle`), `scenarios.lapsedTitle` (`:1083`) + `scenarios.writeLapsed` (`:1100`). O teste NOMEIA cada chave, compara as STRINGS RENDERIZADAS a um snapshot conjunto, e registra no próprio arquivo por que cada uma entra ou fica fora; mudar UMA ⇒ vermelho (mutação). **Feito 29/08** — `premium-pausado-trio.test.ts`: 6 vivas + `catalogo.lapsedTitle` apagada (PR-B) nomeadas; snapshot conjunto; mutação provada (1 string ⇒ 2 vermelhos).
- [x] T091 [P] [US7] `apps/web/src/features/scenarios/rename-key.test.ts` (NOVO, D2 — ANTI-REGRESSÃO, não correção): `scenarios-list-sheet.tsx:388/:149` e `scenario-context-bar.tsx:233/:205` JÁ leem `t.renameSheetTitle`/`t.rename` (medido 27/08). O teste fixa por referência de chave; prova por mutação (bifurcar uma chave manualmente ⇒ vermelho; reverter). **Feito 29/08** — `rename-key.test.ts`: as duas folhas leem `t.renameSheetTitle`/`t.rename` (referência no fonte); mutação provada.
- [ ] T096 — RETIRADA (D2 já satisfeito; não há o que unificar).
- [x] T092 [P] [US7] `apps/web/src/features/scenarios/scenarios-wide.test.tsx` (NOVO): `ScenariosList` NÃO existe — extrair o privado `ScenarioListBody` (`scenarios-list-sheet.tsx:478`) para export no MESMO arquivo; com `matchMedia` largo (`installMatchMedia(VIEWPORT.desktopLarge)`), o hospedeiro largo monta a MESMA instância (uma só); estreito, a gaveta de hoje; `useIsWide` é o único gate; o ramo `showTeaser` (`:462`) fica em UM lugar só. **Feito 29/08** — `ScenariosList` exportado do mesmo arquivo; `scenarios-wide.test.tsx`: largo = 1 instância direta, 0 `<dialog>`; estreito = a gaveta; nota: o ramo `showTeaser` já tinha saído na PR-B (vazio didático/premiumGate em um lugar só).
- [x] T093 [US7] `apps/web/tests/e2e/pages-desktop-width.spec.ts` + `overflow-geometria.spec.ts`: Simulações a 1280/1440/1920 — `widthRatio()` (o MESMO método de `:17-29`, não px soltos) antes (T094) e depois; zero transbordo nos dois eixos; mobile idêntico ao baseline. **Feito 29/08** — `porte-pr-f-t093.spec.ts`: widthRatio 93,8/93,3/66,7% = baseline (a coluna vive DENTRO da section); overflow 0 nos dois eixos; gaveta 390/360 idêntica ao baseline por geometria (sem pixelmatch no lockfile).

### Implementation

- [x] T095 [US7] Composição ≥1280px (DECISÃO 2): `pages/calcular/calcular-page.tsx` monta o `ScenariosList` que a T092 extrai de `ScenarioListBody` (`scenarios-list-sheet.tsx:184`, usado em `:478`; props `onOpenScenario`/`onClose`/`lapsed`) na coluna larga (`useIsWide`), convivendo com o corte de layout de **1024** de `calculator-form.css:25,40,65,81` — os dois nomeados na emenda 2 do ADR-0031 (T130, já aplicada na PR-D); reler a medida do sticky da PR-C (T059) nessa composição; sem duplicar a lista; nenhuma rota nova, nenhum item novo no menu. **Feito 29/08** — `3e5a4c1`: ≥1280 a lista monta num `<aside>` sticky (320px) ao lado da calculadora, a gaveta NÃO monta; 'Minhas simulações' rola+foca a coluna; `scenarios-wide.css` (`tf-scenarios-wide*`); corte 1024 da Calculadora intocado.
- [x] T097 [US7] A11-r: só MEDIR itens visíveis sem rolar a 1024/1279 (`tf-table`, entrou na PR-D) e a 1280/1920 (mestre-detalhe) antes/depois; registrar. **Feito 29/08** — `evidencias/pr-f/a11r.json`: tf-table 1024 = 9 · 1279 = 9 · mestre-detalhe 1280 = 7 · 1920 = 14 itens sem rolar (só medição).
- [x] T098 [US7] Q1/Q2 (27/08): `catalogo.searchEmptyTitle/Body` (`:621-622`, 2 strings sem `{termo}`) colapsam em `catalogo.searchEmpty` (molde `historico.searchEmpty :931`) — o ramo é `catalog-panel.tsx:281-293` (só ≥1280, onde há busca); atualizar testes que referenciam as chaves antigas (e os usos que a PR-D tiver introduzido); a ressalva `catalogo.staleHint` por linha SAI dos DOIS pontos (`catalog-panel.tsx:316` mestre e `:391` mobile), ficando só a faixa "Modo leitura offline" (`:437-441`). Teste: vazio com termo; zero `staleHint` por linha nos dois ramos. **Feito 29/08** — `catalogo.searchEmpty` com `{termo}` (molde historico); `staleHint` por linha fora dos dois ramos; testes novos em catalog-panel/catalog-master-detail.
- [x] T141 [US7] (decisão do dono 28/08 — a prancheta 10 entra na PR-F) Transcrever `Calculadora - A Conta e os Precos` (cópia congelada da PR-C já em `design/`; hashes no README): `markup 50% no varejo · 30% no atacado` (cabeçalho), "Preços por marketplace" (kicker de seção), "Varejo"/"Atacado" (segmented), "O peso de cada custo no total. As cores são as das bolinhas acima — metade do seu custo é material." (legenda da barra — a metade é EXEMPLO, não copy fixa: registrar), a linha-resumo "Atacado · markup 30%". **Feito 29/08** — 4 chaves novas em `results` (`markupHeader`, `proportionCaption` — só a parte fixa; a 'metade é material' é EXEMPLO, registrada —, `marketplaceLevelHint`, `summaryLine`); as frases da 10c JÁ existiam (acentos do aviso de atacado já corrigidos).
- [x] T142 [US7] `features/calculator/calculator-form.tsx` `PriceResults`: as linhas "Preço varejo/atacado" SAEM da conta (termina no custo total); o markup sobe para o cabeçalho da seção; barra de proporção dos custos sob a conta (cores = bolinhas das linhas — as bolinhas voltam, revertendo o 016/US5 FR-907-AC2: registrar a reversão datada); "Preços por marketplace" vira seção própria ANTES dos cartões finais; `<Segmented split>` Varejo|Atacado governa o cartão grande (um preço por vez; varejo accent, atacado neutro) + a linha-resumo do outro + os dois números de cada marketplace; a 10b (360px, seis dígitos) e a 10d (1280, 720px centralizado) medidas; testes de `PriceResults` adotados. **Feito 29/08** — `6978489`: conta termina no custo total, markupHeader, barra de proporção (bolinhas de volta — reversão datada do 016/US5 FR-907-AC2), seção própria de marketplaces + nota, Segmented Varejo|Atacado governando cartão (tf-price--neutro no atacado) + linha-resumo + números; sem Premium a seção não existe; 15 casos novos, não-vácuo por mutação.
- [x] T143 [US7] e2e `calculator-layout.spec.ts`: 360/390/1280/1920 sem transbordo nos dois eixos com R$ 950.096,00 (10b); screenshots 1:1 em `evidencias/pr-f/`. **Feito 29/08** — `calculator-layout.spec.ts`: R$ 950.096,00 sem transbordo (X e Y) a 360/390/1280/1920; 720px centralizado a 1280 (±2px); 14 capturas + medidas fundidas; 4 adoções por marcação em calculator.spec.
- [x] T099 [US7] gate:all + e2e; screenshots 1:1 (Simulações 1280/1920 dois temas; mobile idêntico) em `evidencias/pr-f/`; abrir o PR-F e pedir o flip da **emenda do ADR-0031**. **Feito 29/08** — gate 2028/577 · e2e completo (4 classes tratadas + regressão da PR-D na tf-table corrigida e guardada) · 36 capturas · push autorizado, **PR #63** aberta (pede o flip da emenda 2026-08-26 do ADR-0031 + 7 ratificações).

**Checkpoint**: uma instância da lista; D1 com as 7 chaves nomeadas; D2 guardado; Q1/Q2; mobile byte-idêntico ao baseline.

---

## Phase 10: Polish & fechamento

- [ ] T100 [P] Rodar o vetor canônico (27,55/41,33/35,82 — `computeCalculator.test.ts`/`channels.test.ts`/`computeBom.test.ts`) e `packages/pricing-core/tests/band-dominance.artifact.test.ts` (o nome real — `band-dominance.test.ts` não existe) após TODAS as fatias: nenhum valor existente mudou (SC-007); `catalogVersion` intocado.
- [ ] T101 [P] Confirmar que nenhum medidor de foco sobrou: zero `_diag-foco*` (a T029 apagou; o diretório tem `_diagnostico.spec.ts`) e zero asserção de anel em `apps/web/src/**/*.test.*` e `apps/web/tests/**` — a guarda viva é `focus-none.spec.ts`.
- [ ] T139 [P] Dar consumidor de produto — ou declarar mortos por escrito no dod-evidence — a `tf-btn--full/--half`, `tf-segmented--split` (a T057 é o consumidor previsto) e `tf-badge--warning`: hoje só existem em teste (`button.test.tsx:15-21`, `badge.test.tsx:15`).
- [ ] T140 [P] Precache do PWA para qualquer asset novo das fatias (ilustração de vazio, ícone do construtor): o glob do `vite.config` cobre; provar no `dist/sw.js` (lição 016/Polish e 009/T016-N5).
- [ ] T102 Atualizar `specs/019-porte-design/dod-evidence.md` com as evidências das 6 fatias, as escalações opus, e fechar o `docs/token-ledger.md` (real vs. estimado por fatia). SC-1907: consolidar a geometria de TODAS as telas tocadas nos 4 cortes (360/1280/1440/1920) — nenhuma fatia sozinha varre tudo.
- [ ] T103 Registrar em `docs/homologacao/rodadas/` que as 6 fatias estão em **CORREÇÃO DECLARADA** aguardando a Rodada 1 fechar (D5) — sem abrir cenário novo.
- [ ] T104 Confirmar com o dono os flips (ADR-0032 / 0033 / 0034 / emenda 0031 / emenda 0017) e atualizar `docs/adr/README.md`: 0032–0034 já estão no índice (`:34-36`) — só o status muda; preencher as linhas `0024–0031` (hoje uma linha-resumo em `:33`) e corrigir o status stale do 0023 (`:32` diz "Proposed"; foi aceito no gate do E6 PR-A).

---

## Dependências e paralelismo

```
Phase 1 (V0) → Phase 2 (guardas + transcrição) → Phase 3 (US1) → Phase 4 (US2)  ═══ PR-A
                                                                      │
                     ┌──────────────┬──────────────┬─────────────────┼──────────────┐
                  Phase 5 (US3)  Phase 6 (US4)  Phase 7 (US5, opus)  Phase 9 (US7)  │
                     PR-B           PR-C           PR-D                 PR-F         │
                                                     │                               │
                                                  Phase 8 (US6, opus) — PR-E          │ (depende de A e da 0008 — NÃO de F)
                                                     │
                                                  Phase 10 (Polish)
```

- Dentro de cada fase, os `[P]` são arquivos diferentes sem dependência entre si — podem rodar em
  paralelo (ex.: T011–T016 juntos; T062–T068 juntos).
- **Testes antes**: em cada fase, a seção Tests roda e fica VERMELHA antes da seção Implementation.
- **Ordem decidida pelo dono: B → C → D → F → E.** As 5 decisões da auditoria (27/08) estão em spec
  §Clarifications; a transcrição da prancheta é a PRIMEIRA task de cada fase.
- Escalação: T062–T078 e T079–T089 rodam em **opus** (leaf de dinheiro / pricing-core / schema —
  ADR-0022); registrar a escalação no ledger por operação.

## Estratégia de entrega

1. **MVP = V0 + PR-A**: a fundação sozinha já entrega valor (guardas de folha, tom ATENÇÃO, marca,
   vocabulário) e destrava tudo.
2. Depois de A, o dono escolhe a ordem de B/C/D/F pelo valor que quer ver primeiro (recomendação: B).
3. E sai por último e é tudo-ou-nada; a rota de fuga "E vira 020" fica disponível até a T079.
4. Nada fecha antes da Rodada 1: cada PR entrega evidência pronta para a segunda passada do dono.
